# TB2E E2E Test Plan

Long-term roadmap for Playwright E2E coverage of the Torchbearer 2nd Edition system. Each checkbox is one spec file. Agents are expected to work through this list incrementally, one (or a handful of) checkboxes per run.

## How to use this plan

**For the orchestrator (me):**
- Pick one or more unchecked boxes whose agent briefings are unambiguous.
- Spawn an Explore or general-purpose agent with the relevant section's briefing inlined into the prompt, plus instructions to (a) write the spec, (b) run it until green, (c) tick the box in this file.
- After the agent reports back, verify the spec runs in isolation (`npx playwright test tests/e2e/<file>`) and as part of the suite, then commit with jj.

**For the worker agent:**
- Read the agent-briefing block at the top of the section you were assigned.
- Reuse POMs in `tests/e2e/pages/` — add new POMs only when no existing one fits.
- Follow the conventions established in the existing specs (`login.spec.mjs`, `character-creation.spec.mjs`, `compendium-drag.spec.mjs`, `auth.setup.mjs`).
- Cite rule sources in comments where behavior depends on rules-as-written (`// DH p.53`, `// SG p.69`).
- Tests must be independent: use `Date.now()` suffixes on actor/item names, never depend on ordering.
- Role-based locators first (`getByRole`/`getByLabel`); CSS only inside POMs, never in specs.
- If Foundry's DOM differs from what the briefing expects, update the briefing before moving on.
- When a spec passes reliably (run `--repeat-each=3`), tick the checkbox.

**File path convention:** `tests/e2e/<area>/<feature>.spec.mjs` (e.g., `tests/e2e/conflict/disposition.spec.mjs`). Keep the flat `tests/e2e/*.spec.mjs` layout for infra/scaffolding specs only.

---

## 0. Infrastructure

Already shipped. Listed for completeness.

- [x] `tests/e2e/login.spec.mjs` — GM can join the world
- [x] `tests/e2e/character-creation.spec.mjs` — create a blank character via sidebar dialog
- [x] `tests/e2e/compendium-drag.spec.mjs` — drag Gerald from iconic-characters into Actors

---

## 1. Character Creation

### Agent briefing

**Files:**
- `module/applications/actor/character-wizard.mjs` — 12-step wizard
- `module/applications/actor/character-sheet.mjs` — "Launch Wizard" button in sheet header
- `module/data/actor/character.mjs` — data model
- Wizard steps in order: class → stock → upbringing → hometown → hometown skill → home trait → social → specialty → wises → nature → circles → gear pack → weapons → armor → finishing
- Rules: DH pp.25–47, LMM pp.9–14 (Gather Round)

**Patterns:**
- Wizard data-actions: `openWizard`, `selectClass`, `selectStock`, `selectUpbringing`, `selectHometown`, `selectHometownSkill`, `selectHomeTrait`, `selectSocial`, `selectSpecialty`, `selectRequiredWise`, `answerNature`, `selectNatureWise`, `selectNatureHomeTrait`, `answerCircles`, `selectPackType`, `rollSpells`, `rollRelics`, `selectWeapon`, `toggleShield`, `selectArmor`, `selectEquipment`, `finish`
- Wizard writes to actor on `finish`; each step persists incrementally.
- **Upbringing step** is dynamically filtered out for non-human/changeling stocks (`shouldSkipUpbringing` in chargen.mjs) — elves, dwarves, halflings get a 11-step sequence.
- **rollSpells / rollRelics** are NOT dedicated wizard steps — they surface as sub-sections of step 9 (Gear) when the class is Scholar / Theurge / Shaman. Buttons render conditionally on `needsSpellRoll` / `needsRelicRoll`; clicking rerenders the step with `.relic-list` + `.invocation-list` badge lists.
- **Theurge relic roll**: 3d6 → `THEURGE_RELIC_TABLE[total]` → always 2 relics + 2 invocations; `#applyToActor` resolves names against `tb2e.theurge-relics` / `tb2e.theurge-invocations` compendiums and creates embedded Items. On compendium miss, falls back to stub `{ type: "relic", system: { tier: "minor" } }` — note the `tier` vs schema's `relicTier` drift. Shaman mirrors this with `SHAMAN_RELIC_TABLE` + `tb2e.shamanic-*` packs.
- **Magician (Scholar) spell roll**: 2d6 → `SPELL_SCHOOL_TABLE`; entries 2–10 yield 3 spells each grouped by school; entries 11–12 set `school: "Choose"` with empty `spells[]` (GM picks manually — no dialog in the wizard, user strands at 0 spells). Resolved from `tb2e.spells`. **No stub fallback** — missing names silently drop. Roll button disappears after first roll (no re-roll). For deterministic tests, stub `CONFIG.Dice.randomUniform = () => 0.5` before click; each d6 maps to face 3, total 6 → Conjuration.
- **CLASS_DEFS keys** (from `chargen.mjs`): `ranger`, `magician` (not "scholar"), `theurge`, `shaman`, `warrior`, `outcast`, `burglar`, etc. Check `CLASS_DEFS` before writing specs that reference class keys.
- **Armor step** is a no-op for classes with no starting armor or with `autoLeather` pre-set (Ranger, Theurge): `#isStepComplete("armor")` delegates to `"weapons"` and the step displays only an informational note.
- **Launch the wizard directly** via `new CharacterWizard(actor).render(true)` (imported from `/systems/tb2e/module/applications/actor/character-wizard.mjs`) to avoid opening the character sheet first.

**Checkboxes:**

- [x] `tests/e2e/character/wizard-walkthrough.spec.mjs` — Ranger/elf happy path (upbringing auto-skipped for non-human stocks); all 11 effective steps walked; asserts class/stock/abilities/traits/wises/items
- [x] `tests/e2e/character/wizard-theurge-relics.spec.mjs` — `rollRelics` is a sub-section of step 9 (Gear), not a dedicated step; 3d6 → THEURGE_RELIC_TABLE → 2 relics + 2 invocations; noted schema drift (stub uses `tier`, compendium relics use `relicTier`)
- [x] `tests/e2e/character/wizard-shaman-invocations.spec.mjs` — Shaman mirrors Theurge: same 3d6 roll shape (2 relics + 2 invocations) against `SHAMAN_RELIC_TABLE`, resolved from `tb2e.shamanic-relics` / `tb2e.shamanic-invocations`; narrower weapon list (`Dagger | Hand Axe | Sling | Staff`); class trait "Between Two Worlds"
- [x] `tests/e2e/character/wizard-scholar-spells.spec.mjs` — class key is actually `magician` (not "scholar"); 2d6 `SPELL_SCHOOL_TABLE` (entries 2–10 give 3 spells, 11–12 yield `school:"Choose"` + empty spells[]); no stub fallback (missing names silently drop); no re-roll path; weapon list `["Dagger","Staff"]`; `requiresMentor: true`. Test stubs `CONFIG.Dice.randomUniform` for determinism.
- [x] `tests/e2e/character/import-from-iconic.spec.mjs` — 9 iconics (not 11 — original count was stale): Beren, Gerald, Karolina, Nienna, Rörik, Taika, Tiziri, Ulrik, Varg. One UI-drag path (Taika) + one bulk programmatic import asserting class/stock against a ground-truth table. `afterEach` deletes tagged actors via `flags.tb2e.e2eTag`.
- [x] `tests/e2e/character/create-monster.spec.mjs` — Create Actor dialog, type=monster, assert monster sheet opens
- [ ] ~~skipped~~ `tests/e2e/character/create-npc.spec.mjs` — **PRODUCTION BUG**: `module/applications/actor/npc-sheet.mjs:43` references `templates/actors/character-conflict.hbs` which doesn't exist; NPC sheets throw ENOENT on render. Spec + POM are written and ready behind `test.fixme()`; remove the fixme once the bug is fixed.

---

## 2. Character Sheet

### Agent briefing

**Files:**
- `module/applications/actor/character-sheet.mjs` — sheet class (2100+ lines)
- `templates/actor/character/*.hbs` — tab templates
- Tabs: Identity, Abilities, Skills, Traits & Wises, Inventory, Magic, Biography
- Conditions (DH p.53): fresh, hungry, angry, afraid, exhausted, injured, sick, dead

**Patterns:**
- Data-actions: `toggleCondition`, `toggleBubble`, `setTraitLevel`, `addTrait`, `deleteTrait`, `addRow`/`deleteRow` (generic; used for wises and several other arrays — read `data-array` attr), `resetSession`, `conserveNature`, `recoverNature`, `removeFromSlot`, `dropItem`, `pickUpItem`, `consumePortion`, `drinkDraught`, `consumeLight`, `toggleDamaged`, `toggleLiquidType`, `splitBundle`
- Wises: `system.wises` array on the actor (not an Item type), capped at 4 slots; each entry has `{ name, pass, fail, fate, persona }`. Add via `addRow[data-array="wises"]`, delete via `deleteRow[data-array="wises"][data-index="<i>"]`. Rename via `input[name="system.wises.<i>.name"]`. Rows are keyed by array index (re-index on delete).
- Sheet root: `form.application.sheet.tb2e.actor.character` (title = `Character: <name>`)
- Inventory slots: head, neck, hand-L, hand-R, torso, belt, feet, pocket + custom container slots

**Gotchas:**
- Ability/skill rating inputs are `input[name="system.abilities.<key>.rating"]`
- Will / Health / Circles / Resources: schema min=0 max=10 (via `advancementField`)
- Nature: rating 0–7, max 0–7; the Nature rating HTML input caps at current `max` (dynamic `max="{{nature.max}}"`) — set `max` *before* `rating` or the browser silently rejects
- Nature crisis triggers at rating=0
- Skill rating input is replaced by an `✕` icon when `rating===0 && learning>0` — default actors have `learning=0`, so the input is present; if a spec puts a skill into "learning" mode, switch to the appropriate UI
- Condition toggles on the sheet do a **direct** `document.update()` (no mailbox, no chat card). Toggling `fresh` clears all negative conditions. The `pendingGrindApply` mailbox is a separate path used by the grind-tracker's "Apply" button (grind-tracker.mjs); the `updateActor` hook in `tb2e.mjs` consumes + clears it.
- Conditions strip lives at sheet top; selector `nav.conditions-strip button.condition-btn[data-condition="<key>"]`. No tab switch needed.
- Traits: `module/data/item/trait.mjs` has `level` 1–3 only (min=1, max=3, integer, initial=1). **No flawed flag, no -1 level.** The "used against" state is a separate `usedAgainst: BooleanField`, written by `module/dice/versus.mjs`, not via the bubble UI.
- `addTrait` data-action has **no dialog** — it unconditionally creates an Item named "New Trait" with level 1 via `Item.create(..., { parent: actor })`.
- Traits tab template: `templates/actors/tabs/character-traits.hbs`. Each row has three `.level-pip` buttons (levels 1/2/3) with `data-action="setTraitLevel"` and `data-level`.
- **Session usage tracking lives on the trait Item, not the actor.** `system.beneficial` = *remaining* uses this session (not consumed count): L1 max=1, L2 max=2, L3 max=0/unlimited (`TraitData.maxBeneficial` getter). `resetSession` (in `module/session.mjs` `resetTraitsForSession`) does **three things**: restores `trait.beneficial` to max, clears `trait.usedAgainst`, and ALSO resets `spell.cast` and `invocation.performed` flags. Gated by `DialogV2.confirm` (yes / no buttons with `data-action`).
- The `resetSession` button lives in `character-header.hbs` (always visible on the sheet header; no tab switch needed).
- `conserveNature` (on Abilities tab, Nature row): behind `DialogV2.confirm`; guards `nature.max > 1`. Yes path: `max -= 1`, `rating = new max` (slammed, not preserved), `pass = 0`, `fail = 0`. `recoverNature`: no dialog; guards `rating < max`; `rating += 1`. Neither touches anything outside `system.abilities.nature`.
- **Inventory slot system** (`module/data/item/_fields.mjs`): every inventory Item has `system.slot` (string key, "" = unassigned), `system.slotIndex` (int, for multi-slot groups), `system.dropped` (bool, ground state), plus `slotOptions` (per-location cost map). Body slot groups: head(1), neck(1), hand-L/hand-R(2 each, Worn+Carried), feet(1), pocket(1), torso(3), belt(3). Plus a 12-slot `cache` group + dynamic container groups. **Unassigned section** = `slot === "" && dropped === false`. Actions: `removeFromSlot` (clears slot/slotIndex, keeps dropped); `dropItem` (clears slot + sets dropped=true, cascades to container children, preserving their slot); `pickUpItem` (clears dropped only — does NOT reassign slot). `placeItem` is separate for assigning into slots.
- Drop button is only exposed on Unassigned items and on container slot-group headers (NOT on items placed in ordinary body slots).
- Data-attribute selectors like `[data-item-id="..."]` match many elements per card (placement + action buttons nested inside). Scope via `.dropped-item` / `.unassigned-item` row classes.
- **Supply item type** (`module/data/item/supply.mjs`): `system.supplyType` enum = `food` | `light` | `spellMaterial` | `sacramental` | `ammunition` | `other`. Food uses `system.quantity` / `quantityMax`; light uses `system.turnsRemaining` + `system.lit`. Items are NOT deleted at 0 — they stay for refill. Unassigned items render NO consume button; items must be in a real slot with matching `slotOptions`.
- `consumePortion` = `quantity -= 1` (floor at 0) + clears `conditions.hungry` if true. No chat card.
- `consumeLight` = `turnsRemaining -= 1` (floor at 0). **Does NOT flip `lit: false`, does NOT post chat card, does NOT trigger `pendingLightExtinguish` mailbox.** The light-extinguish / torch-expired flow is a separate path: `updateItem` hook watches for `changes.system?.lit === false`. Button persists as a no-op past turnsRemaining=0 (intentional).
- **Draughts are on CONTAINERS, not supplies.** Type `container` with `CONFIG.TB2E.containerTypes.<kind>.liquid === true` (waterskin, bottle, jug, barrel, cask, clayPot, woodenCanteen). `system.liquidType` ∈ `water` | `wine` | `oil` | `holyWater` (default water). `drinkDraught` handler: decrement `quantity`; `water` clears hungry; `wine` opens `DialogV2.wait` with quench / bolster choice (bolster sets `flags.tb2e.wineBolster`); `oil` / `holyWater` decrement only.
- **Bundles** are `container` items with `quantityMax > 1` (e.g. stacked rations, arrows). `isSplittableBundle` = `type==="container" && quantityMax > 1`. `splitBundle` handler peels ONE off per click (no dialog): clones `item.toObject()` with `quantity=1, quantityMax=1, slot="", slotIndex=0, containerKey=""` (lands in Unassigned); source: `quantity -= 1, quantityMax -= 1`. Button only surfaces in the **occupied** branch of `.inventory-slot` cells (not Unassigned / Dropped). A container with `quantityMax === 1` becomes its OWN slot-group (backpack, etc.); a bundle is always an occupant inside another slot. `belt` forbids bundles; `torso` accepts them.
- **Biography tab** renders: `textarea[name="system.bio"]` (plain StringField — no ProseMirror), an allies table (`system.allies.<i>.name | location | status`), and a level-choices table (`system.levelChoices.<level>`).
- **Convictions** (`belief` / `creed` / `goal` / `instinct`) ARE on the character sheet — in the **identity** tab's "What You Fight For" fieldset, not biography. Rendered via `convictionFields` array built in `character-sheet.mjs` `_prepareConvictionFields()`; template `templates/actors/tabs/character-identity.hbs`. Each is a `<textarea name="system.<field>" rows="2">`. Header also renders `system.goal` as a badge (read-only).

**Checkboxes:**

- [x] `tests/e2e/sheet/edit-identity.spec.mjs` — edit name/level/home, verify persistence after reload (TB2E has no "alignment" field — substituted `system.home`)
- [x] `tests/e2e/sheet/edit-abilities.spec.mjs` — set Will, Health, Nature rating/max; verify data model constraints (ranges, clamping)
- [x] `tests/e2e/sheet/edit-skills.spec.mjs` — set rating for each relevant skill; verify persistence
- [x] `tests/e2e/sheet/toggle-conditions.spec.mjs` — toggle each condition (DH p.53); sheet does a direct update (no mailbox/chat card from the toggle itself); also covers `pendingGrindApply` mailbox clearing by GM hook
- [x] `tests/e2e/sheet/trait-crud.spec.mjs` — add a trait, set level, demote, delete; verify bubble UI state (traits are level 1–3 only; no flawed/-1 bubble — `usedAgainst` is a separate boolean set by versus system)
- [x] `tests/e2e/sheet/wise-crud.spec.mjs` — add a wise, rename, delete (wises are an actor-field array, not Items; capped at 4 per DH p.87; generic `addRow`/`deleteRow` actions with `data-array="wises"`)
- [x] `tests/e2e/sheet/session-reset.spec.mjs` — run resetSession (behind DialogV2.confirm); verify trait `beneficial` restored + `usedAgainst` cleared; also resets spell `cast` / invocation `performed` flags
- [x] `tests/e2e/sheet/nature-tax.spec.mjs` — use `conserveNature` (dialog; -1 max, rating slammed to new max, pass/fail zeroed) / `recoverNature` (no dialog; rating +1 toward max); verify guards
- [x] `tests/e2e/sheet/inventory-slots.spec.mjs` — drop / pickUp / removeFromSlot verified against `system.slot`, `system.slotIndex`, `system.dropped` fields; dropItem cascades to container children; pickUp clears `dropped` only (does NOT reassign slot — item lands in "Unassigned")
- [x] `tests/e2e/sheet/inventory-supplies.spec.mjs` — consumePortion (decrement quantity + clear hungry), drinkDraught (on containers!), consumeLight (decrement turnsRemaining); items never auto-delete at 0
- [x] `tests/e2e/sheet/inventory-bundle-split.spec.mjs` — bundles are `container` items with `quantityMax > 1`; splitBundle is "peel one off" (no dialog); creates qty=1 Item in Unassigned, decrements source. Buttons hidden at qty<2.
- [x] `tests/e2e/sheet/biography-edit.spec.mjs` — `system.bio` is plain textarea (not ProseMirror); covers bio + allies table rows + level-choices table (NOTE: convictions belief/creed/goal/instinct are NOT on this tab — they're on Identity tab; see edit-convictions box below)
- [x] `tests/e2e/sheet/edit-convictions.spec.mjs` — edit belief / creed / goal / instinct textareas on the Identity tab's "What You Fight For" fieldset; verify persistence + header badge updates for `system.goal`

---

## 3. Rolls (Ability & Skill Tests)

### Agent briefing

**Files:**
- `module/dice/tb2e-roll.mjs` — main roll entry, modifier gathering
- `module/dice/roll-utils.mjs` — wise aiders, nature tax, advancement logging
- `module/dice/help.mjs` — help eligibility
- `module/dice/post-roll.mjs` — chat card post-roll actions
- `templates/chat/roll-card.hbs`, `less/chat/roll-card.less`

**Rules:**
- Core test: DH pp.56–62. Pool = rating + mods; count 4–6 as successes; compare vs obstacle.
- Conditions modify (afraid: -1D to Will; injured: -1D to physical actions etc. — see DH p.53).
- Help (DH p.63): +1D per helper; KO'd / dead / afraid cannot help.
- Wise aid (DH p.87): post-roll, spend Persona, pick relevant wise; may advance wise on milestone.
- Nature tax (DH p.119): offered post-roll; taxing to 0 triggers nature crisis card.
- Fate / Persona (DH p.47): spend in roll dialog or post-roll.

**Patterns:**
- `rollTest` data-action on ability/skill rows triggers the roll dialog.
- Dialog has modifier inputs, help-dice checkbox, Fate/Persona spinners, submit button.
- Chat card actions: `fateReroll`, `personaDeeperUnderstanding`, `natureTax`, `spendWise`, `acceptHelp`, `finalizeVersus`.

**Checkboxes:**

- [x] `tests/e2e/roll/ability-test-basic.spec.mjs` — roll Will vs Ob 3 with stubbed dice (all-3s FAIL path + all-6s PASS path); verifies pool/successes/outcome on the chat card. Foundational RollDialog + RollChatCard POMs shipped.
- [x] `tests/e2e/roll/skill-test-basic.spec.mjs` — roll Fighter vs Ob; verify chat card + pass/fail pips ticked on sheet
- [x] `tests/e2e/roll/roll-dialog-modifiers.spec.mjs` — open roll dialog, add +1D modifier manually, submit; verify added to pool
- [ ] ~~skipped~~ `tests/e2e/roll/condition-modifiers.spec.mjs` — **TEST-PLAN / RAW MISMATCH**: afraid does NOT impose -1D per RAW (SG p.48 says afraid blocks help + BL only; -1D is for injured/sick per SG pp.49-52). `module/dice/tb2e-roll.mjs` `gatherConditionModifiers` correctly implements RAW (fresh +1D, injured/sick -1D, afraid = no dice mod). Spec is written behind `test.fixme()` with the expected-pass shape; to green it, either retarget the entry to injured/sick OR add afraid → -1D as an explicit deviation from SG p.48 in `gatherConditionModifiers`.
- [x] `tests/e2e/roll/help-accept.spec.mjs` — Character A opens roll dialog with Helper B eligible (scene token, same-ability rating > 0); toggle Help in the dialog, verify +1D added to A's pool (DH p.63). Note: help is a pre-roll dialog toggle in this codebase, not a post-roll chat-card action — only `synergy` (helper fate-for-advancement) lives on the chat card.
- [x] `tests/e2e/roll/help-blocked-when-ko.spec.mjs` — KO'd helper is filtered out of the pre-roll eligible-helpers pool (no `.helper-toggle` renders); gate is `conflictHP?.max > 0 && conflictHP.value <= 0` in `isBlockedFromHelping` (help.mjs line 57, DH p.63).
- [x] `tests/e2e/roll/wise-aid-persona.spec.mjs` — pre-roll pick wise on dialog, post-roll click "Ah, Of Course!" to spend 1 Persona; verifies Persona decrement, the reroll of all wyrms (DH p.77 — reroll all failed dice, not flat +1s), the `wise.persona` advancement tick, and the wise-advancement card posting when the 4th of pass/fail/fate/persona is marked (DH p.78). Extends RollDialog POM with `selectWise()` and RollChatCard POM with `clickOfCourse()`.
- [x] `tests/e2e/roll/fate-reroll.spec.mjs` — post-roll "Fate: Luck" (data-action `fate-luck`) spends 1 Fate, rerolls every 6 (sun) in the original pool, **cascades on new 6s** (post-roll.mjs line 141-146 — the loop re-filters each reroll batch for suns and re-rolls until none remain), appends luck dice tagged `isLuck: true`, and recalculates pass (DH p.47 / SG p.87). Button gated on `hasFate ∧ hasSuns`; `luckUsed: true` flag hides it after use. Spec covers the happy path (2 suns → reroll with non-6 success faces → +2 successes, FAIL→PASS flip, fate decrement) plus a no-fate gating test (button absent when fate.current = 0). Extends RollChatCard POM with `fateLuckButton` + `clickFateLuck()`.
- [x] `tests/e2e/roll/persona-deeper-understanding.spec.mjs` — **RAW-corrected:** the "Deeper Understanding" mechanic is SG p.87, spending **1 Fate** (not Persona; not "+2s") to reroll a **single wyrm in place on a wise-related roll**. Data-action is `deeper-understanding`; handler is `_handleDeeperUnderstanding` in `module/dice/post-roll.mjs` (line 181-249). Button gating: `!deeperUsed && !luckUsed && hasFate && wiseSelected && hasWyrms` (roll-result.hbs lines 90-99). `_handleDeeperUnderstanding` replaces the first wyrm IN PLACE tagged `isRerolled: true` (line 208 — not appended like Luck/Of Course), deducts 1 Fate, flips `wises[i].fate = true` (DH p.78 advancement box), and sets `flags.tb2e.deeperUsed = true`. Spec covers happy path (3D all-3s FAIL → reroll-to-6 PASS flip, fate decrement, wise.fate flip, in-place dice count preserved, `isRerolled` tag) and a no-fate gating test. Extends RollChatCard POM with `deeperUnderstandingButton` + `clickDeeperUnderstanding()`.
- [x] `tests/e2e/roll/nature-tax-decrement.spec.mjs` — post-roll Nature tax prompt after a Channel Nature spend (DH p.119 / SG p.87). Data-actions are `nature-yes` (within descriptors → 0 tax) and `nature-no` (outside descriptors → tax applies); handler is `_handleNatureTax` in `module/dice/post-roll.mjs` (lines 328-365). Tax amount is `calculateNatureTax` from roll-utils.mjs line 68-71 (pass → 1, fail → `max(obstacle - finalSuccesses, 1)`); decrement clamped to >= 0 rating. Prompt gating: `showNatureTax = channelNature && !natureTaxResolved` (post-roll.mjs line 892 / tb2e-roll.mjs line 1518). Side-effects: `flags.tb2e.natureTaxResolved = true` + `natureTaxAmount` on the message (lines 359-362); `system.abilities.nature.rating` updated; `system.abilities.nature.max` **unchanged** (max is only touched by the sheet `conserveNature` flow and by crisis resolution). Rating-to-0 posts a nature-crisis chat card via `_postNatureCrisis` (line 600-632) — deep crisis-card assertions deferred to §11. Spec covers (1) pass-path decrement rating 3→2 with max/pips unchanged, (2) "Yes" within-descriptors no-tax path, (3) rating 1→0 triggers a nature-crisis ChatMessage (minimal shape check only). Extends `RollChatCard` with `natureTaxYesButton`/`natureTaxNoButton` + `clickNatureTaxYes()`/`clickNatureTaxNo()` and `RollDialog` with `toggleChannelNature()`.

---

## 4. Advancement

### Agent briefing

**Files:**
- `module/dice/advancement.mjs` — dialog
- `module/applications/actor/advancement-chat-card.mjs` (if exists)
- Pass/fail pip auto-trigger: in `post-roll.mjs`, when pip thresholds are met per DH p.84

**Rules:**
- Rating N advances requires N passes + N-1 fails (routine N, D and Ch for higher tiers; DH p.84).
- Auto-trigger on the roll that fills the final pip.

**Checkboxes:**

- [x] `tests/e2e/advancement/auto-trigger.spec.mjs` — stage Fighter rating 2 at (1P, 1F) one pass-pip below the (2P, 1F) threshold (DH p.84); PASS roll + Finalize fills the final pip and auto-opens the advancement dialog (label/current→new rating asserted); negative control at (0, 0) confirms the dialog stays closed
- [x] `tests/e2e/advancement/accept.spec.mjs` — stage Fighter rating 2 at (1P, 1F), PASS roll + Finalize opens the dialog; clicking Advance bumps rating 2 → 3 and hard-resets pips to (0, 0) per advancement.mjs:57-61 (NOT overflow-carry), and a celebration chat card (advancement-result.hbs) is posted speaker-scoped to the actor
- [x] `tests/e2e/advancement/cancel.spec.mjs` — cancel advancement should leave rating unchanged and pips at their threshold-met values (the pip tick in `_logAdvancement` fires pre-dialog and cancel is a no-op), plus no celebration chat card. **Landed as `test.fixme` — production bug in advancement.mjs:45-54**: the cancel button has no `callback`, so DialogV2's `_onSubmit` resolves `wait()` with the truthy string `"cancel"` (dialog.mjs:242 `?? button?.action`), which passes the `if (!result) return` guard and runs the accept mutation. Fix: add `callback: () => false` to the cancel button OR tighten the guard to `if (result !== true) return`
- [x] `tests/e2e/advancement/skill-open.spec.mjs` — **RAW-corrected:** the "skill opens on success" framing in the checkbox description is a briefing-ism. Per DH p.75 ("Pass or fail doesn't matter when learning new skills, just the number of tests") and `_logBLLearning` in tb2e-roll.mjs:213-232, a skill opens at rating 2 after `natureMax` BL attempts regardless of pass/fail outcome. Spec stages Fighter rating 0 with `learning = natureMax-1` so the next BL attempt trips the open path; a negative-control test at `learning = 0, natureMax = 4` asserts a sub-threshold BL attempt just bumps `learning` to 1 without opening. Also asserts the dialog's live summary reflects the BL halving (`baseDice = 4` Health pre-halving, summary shows `2D` post-halving per tb2e-roll.mjs:647-663 / DH p.58 "Beginners Roll Half"). Skill-opened card is scoped via `speaker.actor === actorId` + the unique `.card-accent--green` + "DH p. 75" reference-bar text from templates/chat/skill-opened.hbs. No advancement dialog fires for BL (`logAdvancementForSide` at roll-utils.mjs:194-199 delegates to `_logBLLearning` and bypasses `_logAdvancement` entirely), so `card.clickFinalize()` works without the native-click deadlock workaround used in the §4 auto-trigger/accept specs.
- [x] `tests/e2e/advancement/wise-advancement.spec.mjs` — **perk-choice flow** on an already-posted wise-advancement card (milestone detection itself is covered in `roll/wise-aid-persona.spec.mjs`). All three perks (`wise-change`, `wise-bl`, `wise-skill-test`) are pure mark-resets at post-roll.mjs lines 781-805 — they zero the four advancement boxes (pass/fail/fate/persona) on `actor.system.wises[i]`. `wise-change` ALSO clears the wise `name` (line 783); the other two leave it intact. No perk opens a follow-up dialog or writes to a mailbox; the card re-renders as resolved with `flags.tb2e.wiseAdvResolved = true` gating the listener from re-firing. Note: wises have no numeric rating field — the briefing's "rating bump" is actually the mark-reset (the wise is ready to accrue a new advancement cycle). Spec covers `wise-change` (name cleared + marks reset + card resolved) and `wise-bl` (name preserved + marks reset + card resolved); `wise-skill-test` has identical data semantics to `wise-bl` (only the localized resolved-text string differs at post-roll.mjs line 800 vs 794) and is not separately exercised.

---

## 5. Versus Tests

### Agent briefing

**Files:**
- `module/dice/versus.mjs` — PendingVersusRegistry, finalize, tie-break
- `templates/chat/versus-pending.hbs`, `versus-resolution.hbs`, `versus-tied.hbs`
- Mailbox: `flags.tb2e.pendingVersusFinalize`

**Rules:**
- Versus (DH pp.56, 89): both sides roll; highest successes wins. Tie: either side with trait level 3+ can spend to win.

**Patterns:**
- Initiator opens a roll dialog and marks it as versus → posts `versus-pending` card.
- Opponent clicks "Respond" on card → rolls → posts resolution or tied card.
- Tied card: "Spend Trait to Win" button for each side.

**Checkboxes:**

- [x] `tests/e2e/versus/initiate-respond.spec.mjs` — A rolls versus, B responds; GM-driven end-to-end (both actors GM-owned; single browser session). Versus is a **dialog mode toggle** (`.roll-dialog-mode-toggle` cycles `independent` → `versus` → `disposition`) + challenge dropdown (`select[name="challengeMessageId"]` inside `.roll-dialog-challenge`, live-populated by the `createChatMessage` hook in tb2e-roll.mjs line 1032-1045). There is NO "Respond" button — the opponent opens their own roll dialog and picks the initiator's message id from the dropdown. Finalize is the shared `data-action="finalize"` on the roll-result card; `_handleFinalize` (post-roll.mjs line 506-522) versus-branches into `processVersusFinalize` (GM path, skipping the mailbox). The resolution card is rendered from `templates/chat/versus-resolution.hbs` with `flags.tb2e.versus.type === "resolution"` + `winnerId`; margin is NOT a rendered field — computed via `Math.abs(iS - oS)` in versus.mjs line 170 and asserted against the per-side `.versus-successes` text in the POM. Winner-name banner has `text-transform: uppercase` CSS so `textContent` (not `innerText`) is used to preserve the actor's cased name. Cards render twice (chat log + `#chat-notifications`) — POM pins to `.first()`. New `tests/e2e/pages/VersusCard.mjs` exports `VersusPendingCard`, `VersusResolutionCard`, `VersusDialogExtras` sized for §5's remaining 3 checkboxes.
- [x] `tests/e2e/versus/tie-break.spec.mjs` — force a tie via equal rolls (or mocked), A spends a level-3 trait, verify A wins. Tie-break is wired via a distinct chat card (template `templates/chat/versus-tied.hbs` + flag `versus.type === "tied"`) posted by `_handleVersusTied` (versus.mjs line 309-394) when `_executeVersusResolution` detects `iSuccesses === oSuccesses` (line 149-158). The tied card renders per-side L3 "Win the tie" buttons (`[data-action="level3-break-tie"]` with `data-actor-id` + `data-trait-id`) for each `level === 3` trait — only actors with L3 traits see buttons; empty-state shows `.tied-no-traits` (template line 42/60). Handler `handleLevel3TraitBreakTie` (versus.mjs line 485-518) guards on `level === 3`, `tiedResolved === false`, and `_wasTraitUsedOnRoll` (once-per-test), then routes to `_resolveFromTied` which posts a `type: "resolution"` card with `winnerId = actingActor`. L3 beneficial is **unlimited per session** (DH p.80 / SG p.33) — the handler does NOT mutate the trait (`usedAgainst` stays false, `beneficial` unchanged); spec asserts this. Tie used PRNG stub `u=0.001` for both sides → 3D Will × all-6s = 3 successes each. Extended `VersusCard.mjs` POM with `VersusTiedCard` (banner + `level3BreakTieButton(actorId, traitId)` + `level3BreakTieButtonsFor(actorId)` + native-click `clickLevel3BreakTie`). Verified stable under `--repeat-each=3`.
- [x] `tests/e2e/versus/tie-no-trait.spec.mjs` — tie where NEITHER side has a Level 3 trait, so the `[data-action="level3-break-tie"]` button list is empty for both sides (versus-tied.hbs `{{else}}` branches line 41-43 / 59-61 render `.tied-no-traits`). The concession path is the code-level "compromise" for this case: `handleTraitBreakTie` (versus.mjs line 435-476) accepts ANY trait level — `_getEligibleTieBreakTraits` (line 278-282) filters only on `!system.usedAgainst`, not on level — so per SG p.33 "Breaking Ties" the actor using a trait against themselves concedes the tie and earns 2 checks. Concession semantics verified: (1) `trait.system.usedAgainst = true` flips to gate once-per-session reuse, (2) `trait.system.checks += 2` on the trait item itself, (3) `actor.system.checks += 2` on the character actor (versus.mjs line 463-467; only for `actor.type === "character"`), (4) `winnerId = OTHER actor` (line 471 — the conceder LOSES), (5) tied card's `flags.tb2e.tiedResolved: true` flips (line 569), (6) resolution card posts via `_resolveFromTied`. Both sides' L3 button lists asserted empty; both sides' single-trait concede buttons asserted visible; winner's checks/trait untouched. Extended `VersusCard.mjs` POM with `traitBreakTieButton(actorId, traitId)`, `traitBreakTieButtonsFor(actorId)`, and `clickTraitBreakTie(actorId, traitId)` using the same native-click evaluate pattern as the L3 variants. PRNG stub `u=0.001` both sides → 3D Will all-6s = 3 successes each (tie). Verified stable under `--repeat-each=3` (3/3 green, ~31-35 s each).
- [x] `tests/e2e/versus/finalize-via-mailbox.spec.mjs` — exercise the `pendingVersusFinalize` mailbox end-to-end with no second connected client. The E2E harness only auths as GM (`tests/e2e/auth.setup.mjs`), so `_handleFinalize` always hits the GM fast-path (`post-roll.mjs` line 513-516) when Finalize is clicked in-UI — that's what the other three §5 specs exercise. To drive the non-GM branch (`post-roll.mjs` line 517-519) without impersonating a user, simulate the exact two-step player-side sequence via `page.evaluate`: (1) `message.update({ "flags.tb2e.resolved": true })` mirroring `post-roll.mjs` line 508 (required or `_executeVersusResolution` short-circuits at `versus.mjs` line 126), then (2) `actor.setFlag("tb2e", "pendingVersusFinalize", { messageId })` mirroring line 518. Payload shape: `{ messageId: <finalized versus roll message id> }` — the ONLY field `processVersusFinalize` reads (versus.mjs line 103). GM hook dispatcher at `tb2e.mjs` line 185-192 guards on `!game.user.isGM` (line 186), picks off `changes.flags?.tb2e?.pendingVersusFinalize`, and calls `processVersusFinalize(actor, pending)` when `.messageId` is truthy. Clearing semantics: `processVersusFinalize` at `versus.mjs` line 102-129 unsets the flag at line 106 BEFORE any partner-message work, so the mailbox is drained even when the partner hasn't finalized yet (idempotent clear — no double-processing on duplicate writes). Spec asserts: (a) pre-write `getFlag("tb2e", "pendingVersusFinalize")` is null, (b) no resolution card exists pre-write, (c) resolution card with `versus.type === "resolution"` + matching `initiatorMessageId`/`opponentMessageId` posts within 10 s, (d) `pendingVersusFinalize` is null post-processing (polled via `expect.poll`), (e) resolution card flags + winner/successes/margin all correct, (f) both roll messages flip to `versus.resolved === true` (versus.mjs line 225-226), (g) `PendingVersusRegistry` entry removed (line 229). Reuses `VersusPendingCard` / `VersusResolutionCard` / `VersusDialogExtras` POMs unchanged — mailbox assertions are flag-level page.evaluate, not DOM selectors. Stable under `--repeat-each=3` (3/3 green, ~28-32 s each).

---

## 6. Spells

### Agent briefing

**Files:**
- `module/dice/spell-casting.mjs` — three casting types
- `module/applications/item/spell-sheet.mjs`
- `module/data/item/spell.mjs` — castingType: fixed / factors / versus / skillSwap
- Spellbook: `module/data/item/spellbook.mjs`
- Scroll: `module/data/item/scroll.mjs`
- Rules: DH pp.99–101, p.116; Arcanist skill

**Patterns:**
- `castSpell` data-action on Magic tab.
- Fixed casting: roll Arcanist vs spell's `fixedObstacle`.
- Factor casting: factor picker dialog → computed obstacle → roll.
- Versus casting: target the opponent; resolves via versus system.
- Materials/focus: +1D each when marked on the item.

**Checkboxes:**

- [x] `tests/e2e/spell/cast-fixed-obstacle.spec.mjs` — cast a fixed-Ob spell; verify roll vs correct Ob, chat card has spell source (Beast Cloak `a1b2c3d4e5f63003`, `fixedObstacle: 3`; castSpell→`#onCastSpell` at character-sheet.mjs:1396 → `castSpell` at spell-casting.mjs:11 → rolls Arcanist; spell label on card from `flags.tb2e.testContext.spellName` rendered via roll-result.hbs:21-26 `.roll-card-spell`)
- [x] `tests/e2e/spell/cast-factors.spec.mjs` — cast a factors spell; factor dialog opens, selections compute Ob, roll resolves (Wyrd Lights `a1b2c3d4e5f60010`, factors Number/Duration; `castSpell`→`_showFactorDialog` at spell-casting.mjs:59-64,77-139 renders templates/dice/spell-factors.hbs; `cast` callback sums all `input[type='radio']:checked` values at spell-casting.mjs:104-111 and passes total as `testContext.obstacle`; roll dialog pre-fills obstacle from `testContext.obstacle` at tb2e-roll.mjs:513-515; selecting Number=3 + Duration=1 ⇒ Ob 4, Arcanist rating 4 vs Ob 4 PASS)
- [x] `tests/e2e/spell/cast-versus.spec.mjs` — cast a versus spell; verify versus-pending card, opponent responds, resolution consumes memory-sourced spell (Wizard's Bane `a1b2c3d4e5f6200c`, `castingType: versus`; `castSpell`→spell-casting.mjs:66-69 stamps `testContext.isVersus` → tb2e-roll.mjs:929-937 pre-sets the dialog mode to versus and un-hides the challenge block → `_handleVersusRoll` at tb2e-roll.mjs:1630-1652 posts a roll-result card with `flags.tb2e.versus = { type: "initiator" }` + full `flags.tb2e.testContext` spell payload; opponent cycles `.roll-dialog-mode-toggle` and selects via `challengeMessageId` per VersusCard POM; `_executeVersusResolution` at versus.mjs:261-262 calls `processSpellCast` for the winning caster, flipping `system.cast=true, memorized=false` per spell-casting.mjs:162-173. The plain-versus finalize branch at post-roll.mjs:507-522 does NOT consume the spell — only resolution does, asserted by checking state both before and after the opponent's finalize)
- [x] `tests/e2e/spell/materials-focus-bonus.spec.mjs` — toggle materials+focus on spell; verify +2D added to roll (Beast Cloak `a1b2c3d4e5f63003` with truthy `system.materials` + `system.focus` → `castSpell` pushes two pre-timing dice modifiers per spell-casting.mjs:27-43 with labels from lang/en.json:851-852 `Materials (+1D)` / `Focus (+1D)`; roll dialog keeps `input[name="poolSize"]` at Arcanist rating 3 while `.roll-dialog-modifiers` renders both rows and `updateSummary` at tb2e-roll.mjs:939-962 shows 5D; `rollTest` at tb2e-roll.mjs:1316-1319 computes `poolSize = baseDice + diceBonus = 5`; chat card flags prove `baseDice: 3, poolSize: 5, successes: 5` vs Ob 3 PASS)
- [x] `tests/e2e/spell/scroll-one-use.spec.mjs` — cast from a scroll; verify scroll is consumed / marked burned (Beast Cloak `a1b2c3d4e5f63003` + a `scroll` Item whose `system.spellId` points at the embedded spell per module/data/item/scroll.mjs:9; spell row's `canCast` flips true via the scroll-count branch at character-sheet.mjs:725-728 → `castSpell` button in templates/actors/tabs/character-magic.hbs:44-47 → `#onCastSpell` at character-sheet.mjs:1396-1433 treats scroll as the sole source and passes `opts.scrollItemId`; `castSpell` at spell-casting.mjs:45-56 stamps `testContext.castingSource: "scroll"` + `scrollItemId`; on Finalize `processSpellCast` routes the scroll branch FIRST at spell-casting.mjs:153-158 — sets `spell.system.cast=true`, posts a `spell-source` chat card with `.spell-source-confirm` button; clicking the button runs `activateSpellSourceListeners` at spell-casting.mjs:248-289 which deletes the scroll Item and flips `flags.tb2e.spellSource.resolved = true`. Asserts pre-Finalize state, post-Finalize scroll-still-present + spell.cast=true, and post-confirm scroll-deleted + card resolved)
- [x] `tests/e2e/spell/spellbook-source.spec.mjs` — cast from spellbook; verify chat card shows spellbook as source (Beast Cloak `a1b2c3d4e5f63003` + a `spellbook` Item per module/data/item/spellbook.mjs:9; spell's `system.spellbookId` set to the spellbook Item id per module/data/item/spell.mjs:63 → `canCast` flips via the `inSpellbook` branch at character-sheet.mjs:726-728 → `castSpell` button in templates/actors/tabs/character-magic.hbs:44-47 → `#onCastSpell` at character-sheet.mjs:1409,1418-1420 pushes a `"spellbook"` source and short-circuits the chooser (no `opts` needed — scroll is the only source that sets `scrollItemId` at character-sheet.mjs:1432); `castSpell` at spell-casting.mjs:45-56 stamps `testContext.castingSource: "spellbook"`, `scrollItemId: null`; Finalize → `processSpellCast` spellbook branch at spell-casting.mjs:168-173 flips `system.cast=true` and posts a `spell-source` chat card via `_postSpellSourceCard` — the spellbook branch at spell-casting.mjs:199-210 looks up the spellbook name via `actor.items.get(spell.system.spellbookId)` and renders templates/chat/spell-source.hbs:19 with flavor text from lang/en.json:866 `TB2E.Spell.SpellbookSourceText` embedding the spellbook name; card flags carry `flags.tb2e.spellSource = { type: "spellbook", spellbookName, ... }` per spell-casting.mjs:236; clicking `.spell-source-confirm` runs `activateSpellSourceListeners` at spell-casting.mjs:263-265 which clears `spell.system.spellbookId` (the spell Item remains; only the binding severs, matching "ink burns away from the page" per DH p.100). Asserts flag payload, DOM text contains the spellbook name, and post-confirm spellbookId is `""` while the spell + spellbook Items still exist)
- [x] `tests/e2e/spell/cast-skill-swap.spec.mjs` — skillSwap casting type; verify success chat posted with no roll (Wizard's Ægis `a1b2c3d4e5f6000e`, `castingType: skillSwap`; `canCast` flips true unconditionally via the skillSwap disjunct at character-sheet.mjs:727-728 — memorized/spellbookId/scrolls all empty and the Cast button still renders; `#onCastSpell` short-circuits at character-sheet.mjs:1402-1404 with `castSpell(actor, item, "memory")` — NO source chooser; `castSpell` returns at spell-casting.mjs:16-24 after a single `ChatMessage.create` with localized `TB2E.Spell.SkillSwapActive` content (lang/en.json:856) scoped via `speaker.actor`, NO `rollTest` call so NO roll dialog (`.roll-dialog` count 0), NO `flags.tb2e.roll`, NO `flags.tb2e.testContext`, NO `flags.tb2e.spellSource`, `isRoll=false`; `processSpellCast` never runs so `spell.system.memorized` and `spell.system.cast` stay exactly as seeded — no state mutation from the cast)

---

## 7. Invocations & Relics

### Agent briefing

**Files:**
- `module/dice/invocation-casting.mjs`
- `module/applications/item/invocation-sheet.mjs`
- `module/data/item/invocation.mjs`, `relic.mjs`
- Packs: `theurge-invocations`, `theurge-relics`, `shamanic-invocations`, `shamanic-relics`
- Relic auto-detection: great relics match by circle; lesser by invocation name link; relic must be slotted (not dropped)

**Checkboxes:**

- [x] `tests/e2e/invocation/perform-basic.spec.mjs` — perform a Theurge invocation with no relic; verify burden applied (Bone Knitter, packs/_source/theurge-invocations/Bone_Knitter_b2c3d4e5f6a7b8c9.yml — fixedObstacle 3 → Ob 4 with no-relic bump per invocation-casting.mjs:52; burden 2 added to `actor.system.urdr.burden` via processInvocationPerformed invocation-casting.mjs:231-262, wired from Finalize in post-roll.mjs:582-584; handler `CharacterSheet.#onPerformInvocation` at character-sheet.mjs:1372-1377)
- [x] `tests/e2e/invocation/perform-with-relic.spec.mjs` — relic slotted + auto-detected; verify reduced burden (`burdenWithRelic`) applied (Bone Knitter invocation `b2c3d4e5f6a7b8c9` + Bone Knitting Needles relic `e02a2b3c4d5e6f7a` — lesser/minor auto-detect via `linkedInvocations.includes(name)` at invocation-casting.mjs:92-93; slotted (`slot: "head", dropped: false`) satisfies `findApplicableRelic` guard at invocation-casting.mjs:88-89; with hasRelic=true the `obstacle += 1` at invocation-casting.mjs:52 is skipped so Ob stays at fixedObstacle 3; `burdenWithRelic: 1` picked over `burden: 2` at invocation-casting.mjs:39 and applied via processInvocationPerformed invocation-casting.mjs:231-262)
- [x] `tests/e2e/invocation/perform-without-relic-penalty.spec.mjs` — versus invocation, no relic; verify -1s applied (Wrath of the Lords of Law `a00000000000001b` — `castingType: versus`, `versusDefense: nature`, `burden: 3`; `performInvocation` at invocation-casting.mjs:64-76 stamps `testContext.isVersus=true` and, when `!hasRelic`, pushes a post-timing `success` modifier with value -1 + label `TB2E.Invocation.NoRelicPenalty` "No Relic (-1s)" (lang/en.json:896); dialog pre-selects versus mode via tb2e-roll.mjs:929-937 and renders the `.roll-modifier` row via tb2e-roll.mjs:517-544; `_handleVersusRoll` at tb2e-roll.mjs:1566-1653 stores raw successes 4 on `flags.tb2e.roll.successes` and adjusted `finalSuccesses: 3` via tb2e-roll.mjs:1577-1583; resolution reads `iRoll.finalSuccesses ?? iRoll.successes` at versus.mjs:146-147 so caster wins 3 vs 0 margin 3. Caster dice stub `u=0.001` → all-6s = 4D = 4 raw → 3 after -1s; opponent stub `u=0.5` → all-3s = 0. Burden/performed side-effects are out of scope: post-roll.mjs:507-521 takes the versus branch and returns BEFORE processInvocationPerformed fires at post-roll.mjs:582-584; versus.mjs:258-266 only calls processSpellCast — so for versus invocations those side-effects are a separate production gap)
- [x] `tests/e2e/invocation/sacramental.spec.mjs` — sacramental flag behavior on appropriate invocation (Breath of the Burning Lord `c3d4e5f6a7b8c9d0` — `castingType: fixed`, `fixedObstacle: 2`, `sacramental: "A bit of blubber or fine incense"`, `burden: 2`. `performInvocation` at invocation-casting.mjs:30-37 pushes a pre-timing `dice` context modifier `{value: 1, source: "invocation"}` with label `TB2E.Invocation.SacramentalBonus` "Sacramental (+1D)" (lang/en.json:895) whenever `invocation.system.sacramental` is truthy (StringField — empty string falsy, any material name truthy). The modifier flows through `_showRollDialog` contextModifiers → `_collectAllModifiers` (tb2e-roll.mjs:549-551) so the `.roll-modifier` row pre-renders in the dialog; `updateSummary` (tb2e-roll.mjs:939-962) sums `pre/dice` into `diceBonus` so the summary shows `5D vs Ob 3`; `rollTest` (tb2e-roll.mjs:1316-1319) computes `poolSize = baseDice + diceBonus = 5` on the chat card and serializes the sacramental modifier entry onto `flags.tb2e.roll.modifiers` (tb2e-roll.mjs:1431). No-relic path bumps Ob 2 → 3 (invocation-casting.mjs:52); dice stub `u=0.001` → all-6s → 5 successes PASS. Finalize still applies the full no-relic `burden: 2` via processInvocationPerformed invocation-casting.mjs:231-262 (sacramental bonus is orthogonal to burden selection at invocation-casting.mjs:39). No supply-item decrement — sacramental is a pure dice-bonus feature in the current codebase)
- [x] `tests/e2e/invocation/shaman-invocation.spec.mjs` — shaman invocation end-to-end (Hound of the Hunt `c1c2c3c4c5c61003` from `tb2e.shamanic-invocations`, Lore Master's Manual p.42 — `castingType: fixed`, `fixedObstacle: 3`, `burden: 2`, `burdenWithRelic: 1`, `sacramental: ''`, chosen as the shaman analogue of Bone Knitter for number-for-number parity with line 261. Actor created with `system.class: "shaman"` as a self-documenting marker. Shaman/theurge code path parity: `performInvocation` (invocation-casting.mjs:9-77) contains NO class branching — no `actor.system.class` read, no shaman-specific dispatch — so the fixed-Ob switch at invocation-casting.mjs:50-55 (+1 Ob when `!hasRelic`) and the Finalize side-effects via `processInvocationPerformed` (invocation-casting.mjs:231-262) run identically for shaman-class actors. Burden lives on the shared `actor.system.urdr.burden` field (character.mjs:155-157), common to all character classes. No-relic path → Ob 4 Ritualist rating 4 → all-6s PRNG stub PASS (4 successes). Pre-Finalize `urdr.burden=0, performed=false`; post-Finalize `urdr.burden=2, performed=true`. `flags.tb2e.testContext.invocationId` matches the embedded item's id, confirming the shamanic pack entry flows through the shared handler unchanged)
- [x] `tests/e2e/invocation/relic-dropped-not-detected.spec.mjs` — dropped relic NOT auto-detected (direct inverse of line 262; Bone Knitter `b2c3d4e5f6a7b8c9` + Bone Knitting Needles relic `e02a2b3c4d5e6f7a`). Relic embedded with `system.slot: "head", slotIndex: 0, dropped: true` — mirrors the player-dropped steady state. `findApplicableRelic` guard at invocation-casting.mjs:88-89 (`!relic.system.slot || relic.system.dropped`) flags the dropped half of the OR → returns undefined → `_askRelicStatus` shows the fallback `RelicPrompt` dialog (invocation-casting.mjs:111-116), not the auto-detect `HasRelic` prompt (invocation-casting.mjs:108-109). Clicking "Without Relic" resolves `hasRelic=false` → `obstacle += 1` bump runs (invocation-casting.mjs:52) → Ob 3 → 4; `burdenAmount = invocation.system.burden` at invocation-casting.mjs:39 picks full `burden: 2` not `burdenWithRelic: 1`. PRNG `u=0.001` all-6s: Ritualist 4 vs Ob 4 PASS (4 successes). Preflight inlines the production guard to prove `autoDetectMatch` is `null` despite `linkedInvocations: ["Bone Knitter"]`, `slot: "head"`. Flag assertions: `hasRelic: false`, `burdenAmount: 2`. Post-Finalize: `urdr.burden` 0 → 2, `performed: true`

---

## 8. Compendiums

### Agent briefing

**Files:**
- Foundry core compendium behavior
- `module/config.mjs` — pack registrations
- POMs: `pages/CompendiumSidebar.mjs`, `pages/CompendiumWindow.mjs`

**Checkboxes:**

- [x] `tests/e2e/compendium/open-each-pack.spec.mjs` — open every pack under system.json, assert window renders with expected entry count range (programmatic sweep over `game.system.packs` + per-pack floor table in `EXPECTED_MIN_ENTRIES`; UI sanity test opens `tb2e.monsters` via sidebar and asserts rendered rows)
- [x] `tests/e2e/compendium/drag-weapon-to-inventory.spec.mjs` — drag the `Sword` entry (id `026b10bdba9bf1a4`) from `tb2e.weapons` onto the character sheet's inventory tab; programmatic drop via `CharacterSheet#_onDropItem` (module/applications/actor/character-sheet.mjs:2008) with a synthetic `DragEvent` whose target is the inventory section (no `[data-slot-key]` ancestor → unassigned). Asserts the embedded `weapon` Item is created with matching `cost`, `wield`, and `slotOptions` copied from the pack source, lands with `system.slot === ""` / `system.slotIndex === 0` / `system.dropped === false`, and shows up in the unassigned section row. Native Playwright drag-and-drop was tried first but is flaky against AppV2 sheet windows (works for the Actors sidebar in tests/e2e/compendium-drag.spec.mjs, but not reliably for the sheet's drop zone).
- [x] `tests/e2e/compendium/drag-spell-to-magic-tab.spec.mjs` — drag the `Arcane Semblance` entry (id `a1b2c3d4e5f60003`) from `tb2e.spells` onto the character sheet's magic tab; programmatic drop via `CharacterSheet#_onDropItem` (module/applications/actor/character-sheet.mjs:2008) with a synthetic `DragEvent` whose target is `section[data-tab="magic"].active`. The magic tab has no `[data-slot-key]` ancestors, so the override's slot-assignment branch is a no-op for spell drops (`super._onDropItem` creates the embedded Item). The Magic tab is always rendered regardless of class (`#prepareMagicContext` has no class gating) — no need to stage `system.class = "magician"`. Asserts the embedded `spell` Item is created with matching `circle`, `castingType`, `fixedObstacle`, `materials`, `scribeObstacle`, `learnObstacle`, and `factors.length` copied from the pack source; per-character tracking state starts clean (`library/memorized/cast === false`, `spellbookId === ""`); and the Arcane Spells table row for the new id renders on the magic tab. Mirrors drag-weapon-to-inventory.spec.mjs structure (programmatic drop, `pack.render(true)` to bypass sidebar folder nav, entry id not name substring).
- [x] `tests/e2e/compendium/drag-monster-to-scene.spec.mjs` — drag the `Kobold` entry (id `a1b2c3d4e5f60001`) from `tb2e.monsters` onto a freshly created + activated scene (the seed world ships scene-less). Programmatic drop via Foundry core's `TokenLayer#_onDropActorData` (foundry/client/canvas/layers/tokens.mjs:681) with a synthetic `DragEvent` + `{type:"Actor", uuid, x, y}` payload — the same shape the sidebar drag source builds. The handler imports the pack actor to the world (`Actor.implementation.create(actorData, {fromCompendium: true})`, tokens.mjs:697-698), builds the prototype token via `actor.getTokenDocument({}, {parent: canvas.scene})` (tokens.mjs:702 + actor.mjs:301), and persists it with `TokenDocument.create` (tokens.mjs:714). Asserts a new unlinked TokenDocument lands on the scene (`scene.tokens.size` goes 0 → 1), `token.actorLink === false`, `token.name === "Kobold (1)"` (Kobold's prototype has `appendNumber: true`, Kobold_a1b2c3d4e5f60001.yml:75 — handler at actor.mjs:317-321 picks the lowest free N), the synthetic actor exposed via `token.actor` is a `monster` named "Kobold" (per CLAUDE.md §Unlinked Actors — read per-token state via `token.actor`, not `game.actors.get`), and no uncaught page errors fire. Cleanup deletes the scene (cascades tokens) and any world actors tagged via `flags.tb2e.e2eTag`. Canvas drag via Playwright is not feasible (drop target is WebGL, not a DOM element); the handler only reads `.altKey` / `.shiftKey` from the event (tokens.mjs:703, 709), so a synthetic `DragEvent` exercises the identical path.
- [x] `tests/e2e/compendium/drag-relic-to-slot.spec.mjs` — drag the `Pearl` entry (id `b000000000000007`) from `tb2e.theurge-relics` directly into a specific body slot on the character sheet; programmatic drop via `CharacterSheet#_onDropItem` (module/applications/actor/character-sheet.mjs:2008) with a synthetic `DragEvent` whose target is the `.inventory-slot[data-slot-key="pocket"][data-slot-index="0"]` cell so the override's slot-placement branch (character-sheet.mjs:2010 `event.target.closest("[data-slot-key]")` + `#assignSlot` at character-sheet.mjs:1910) writes `system.slot="pocket"` / `system.slotIndex=0` / `system.dropped=false` (character-sheet.mjs:1968). Pearl is a `minor` Lords-of-Life-and-Death relic with `slotOptions: { pocket: 1 }` — picked because `pocket` is a fixed capacity-1 body slot (character-sheet.mjs:1990), making the placement assertion unambiguous. Asserts the embedded `relic` Item is created with `relicTier`/`immortal`/`linkedInvocations`/`slotOptions` copied verbatim from the pack source, lands slotted (not dropped — required for invocation-relic auto-detection per CLAUDE.md §Conflict), and the target slot cell renders occupied (`data-item-id=<new>`, not `.empty`, contains the item name). Unassigned and dropped sections stay absent. Mirrors drag-weapon-to-inventory.spec.mjs structure (programmatic drop, `pack.render(true)` to bypass sidebar folder nav, entry id not name substring).
- [x] `tests/e2e/compendium/search-filter.spec.mjs` — type a name substring into the compendium window's header search input and assert the visible entry rows are reduced to a known matching subset, then cleared back to baseline. Opens `tb2e.monsters` via `pack.render(true)` (same idiom as sibling drag specs), reads the baseline visible row count (40 entries), types `"troll"` (matches `Troll_{Bat,Haunt,Rat}_*.yml` — 3 stable rows), polls via `expect.poll` for the filter to settle below baseline (SearchFilter debounces 200ms — `foundry/client/applications/ux/search-filter.mjs:60`), then asserts filtered count is `>= 3` and `< baseline` and that every visible row's `.entry-name` contains the query (case-insensitive). The filter toggles `element.style.display = "none"` on non-matching rows (`foundry/client/applications/sidebar/document-directory.mjs:678` — `_onMatchSearchEntry`); we target `li.directory-item[data-entry-id]:not([style*="display: none"])` for a deterministic visible-row count (not `:visible`, which depends on viewport). Clearing the input restores the baseline count, exercising the other half of the filter contract. The CompendiumWindow POM gained `searchInput`, `entryRows`, `visibleEntryRows`, `search(query)`, `clearSearch()` — all keyed off the header template's `<search> <input type="search">` (`foundry/templates/sidebar/directory/header.hbs:23`). Sidebar-based open is not duplicated here (covered by open-each-pack.spec.mjs).

---

## 9. Loot Tables

### Agent briefing

**Files:**
- `module/documents/loot-table.mjs` — `TB2ELootTable`, chain trace
- Pack: `loot-tables` (52 subtables)
- Templates: `templates/chat/loot-draw.hbs`
- Recursion: max depth 5; results can be another table

**Rules:**
- Scholar's Guide loot tables (various pages). Subtable drawing cascades until a terminal item.

**Checkboxes:**

- [x] `tests/e2e/loot/draw-terminal-table.spec.mjs` — roll a non-recursive loot table (e.g. "Coins"); verify single chat card with terminal result (Coins Subtable 1 `lt00000000000010`; `table.draw()` via `TB2ELootTable` override in `module/documents/loot-table.mjs:83`; scopes by `.loot-card` from `templates/chat/loot-draw.hbs:2` + `flags.tb2e.lootDraw`)
- [x] `tests/e2e/loot/draw-recursive-chain.spec.mjs` — roll a top-level table that chains to a subtable (e.g. "Treasure Type" → "Enchanted Weapon"); verify chain trace shown in single card (Loot Table 1 `lt00000000000001` → Books & Maps Subtable `lt00000000000006`; `CONFIG.Dice.randomUniform = () => 0.999` locks every die to face 1 so 2d6→2→"Books & Maps" and 3d6→3→"Accurate Map (Dungeon Level)"; asserts one chat message, chain length 2, connector count 1, `loot-chain-link--last` only on last link, `flags.core.RollTable` is TOP id per `module/documents/loot-table.mjs:172`; reuses `LootDrawCard` POM unchanged)
- [x] `tests/e2e/loot/draw-max-depth.spec.mjs` — verify recursion stops at depth 5 (guard at `module/documents/loot-table.mjs:27-29` — `if (_depth > 5) throw new Error(...)`, so depths 0..5 permitted and depth 6 throws; pack has no chain 7+ deep so test uses Approach B — constructs 7 synthetic world RollTables `T0..T6` each with one result pointing at the next via world UUID `RollTable.<id>`, `T6` terminal text; fakes loot-table origin via `_stats.compendiumSource = "Compendium.tb2e.loot-tables.RollTable.<id>"` so `isLootTable` at `module/documents/loot-table.mjs:76-81` routes through `_toLootMessage`; asserts `table.draw()` rejects with `/Maximum recursion depth exceeded/`, no loot card posted (the throw propagates out of `draw()` before `_toLootMessage` at line 100 runs), no `pageerror` events; cleans up all 7 tables + sweeps `flags.tb2e.e2eMaxDepth`-tagged stragglers in afterEach)
- [x] `tests/e2e/loot/draw-page-refs.spec.mjs` — verify page references appear in chat card (reuses the recursive-chain draw: top `Loot Table 1` → sub `Books & Maps Subtable` → terminal `Accurate Map (Dungeon Level)` with the `() => 0.999` PRNG stub; asserts top table's YAML `description` — "Scholar's Guide, p. 152" — renders as `.card-subtitle` via `table.pageRef` at `module/documents/loot-table.mjs:156` and template `templates/chat/loot-draw.hbs:7-9`, and the terminal Item's `system.description` — "Scholar's Guide, p. 153" — renders as a single `.loot-drop-page` via `pageRef: linkedDoc?.system?.description` at `module/documents/loot-table.mjs:138` and template `templates/chat/loot-draw.hbs:65-67`; per-chain-link pageRefs are not asserted since the chain template at `templates/chat/loot-draw.hbs:19-28` doesn't render them; extends `LootDrawCard` POM with `dropPageRefs`/`dropPageRefTexts()`)

---

## 10. Grind Tracker

### Agent briefing

**Files:**
- `module/applications/grind-tracker.mjs` — singleton HUD
- Rules: DH p.53 (conditions), p.75 (phases), grind turn counter
- Mailbox: `flags.tb2e.pendingGrindApply` (player → GM condition application)
- Consolidated condition chat card (recent commit)

**Checkboxes:**

- [x] `tests/e2e/grind/advance-turn.spec.mjs` — GM advances turn counter; verify display updates for all clients (state: `game.settings` `tb2e.grindTurn` world-scoped, registered in `tb2e.mjs` L17-19; advance handler at `module/applications/grind-tracker.mjs` L305-360; HUD singleton opened via `GrindTracker.getInstance()` at `module/applications/grind-tracker.mjs` L51-53; Advance button `button.advance-btn[data-action="advanceTurn"]` in `templates/grind-tracker.hbs` L38-41)
- [x] `tests/e2e/grind/set-phase.spec.mjs` — cycle phases; verify dropdown/state (phase enum hard-coded in `module/applications/grind-tracker.mjs` L407 as `["adventure", "camp", "town"]`; `setPhase` handler L406-418 cycles + resets turn on wrap-to-adventure L412-414; UI is a single cycle button `button.phase-btn[data-action="setPhase"]` in `templates/grind-tracker.hbs` L10-13 with label `.phase-btn-label` containing `{{phaseLabel}}` mapped in `_prepareContext` L124; non-adventure phases render `.phase-name-large` instead of the turn/advance block per template L22/L42-44)
- [x] `tests/e2e/grind/apply-condition-mailbox.spec.mjs` — simulates the player-side dual-write at `module/applications/grind-tracker.mjs` L537-543 (`actor.update({ 'system.conditions.<cond>': true, 'flags.tb2e.pendingGrindApply': <messageId> })`) after authoring a consolidated grind ChatMessage with the shape `#postConsolidatedGrindCard` posts (L390-399: `flags.tb2e = { grindCondition: true, turn, entries: [{actorId, condKey, applied: false}] }`). Payload shape for the mailbox is the bare `messageId` string (L541 + L595 `game.messages.get(messageId)`). GM hook dispatches in `tb2e.mjs` L245-246; processor `processGrindApplyMailbox` at `module/applications/grind-tracker.mjs` L595-599 calls `_applyGrindEntry` (L579-587) to flip `entries[i].applied` true then `actor.unsetFlag('tb2e', 'pendingGrindApply')`. Asserts all three observable effects — condition=true, mailbox=null, entry applied=true. Complements sheet/toggle-conditions.spec.mjs L178-217 (bogus-message clear path) by exercising the happy-path apply+clear with a real message
- [x] `tests/e2e/grind/consolidated-card.spec.mjs` — seeds two scene-resident characters with `fresh` baselines, sets `tb2e.grindTurn` to 3, then clicks the HUD Advance button so `#onAdvanceTurn` (module/applications/grind-tracker.mjs L305-360) crosses `cyclePos === maxTurns === 4` (L344) and invokes the grind branch (L345-355). Snapshots `game.messages.contents` before/after to prove EXACTLY ONE `flags.tb2e.grindCondition === true` ChatMessage is posted (regression guard: per-actor posting would show up as 2). Asserts the consolidated shape matches `#postConsolidatedGrindCard` (L390-399): `flags.tb2e = { grindCondition: true, turn: 4, entries: [{ actorId, condKey, applied: false } × 2] }`. Both entries carry `condKey: 'hungry'` — first-missing in GRIND_ORDER (L3) for a fresh character. Also asserts the rendered DOM shape from `templates/chat/grind-consolidated.hbs`: one `.grind-entry[data-actor-id]` per entry (L13), exactly one `[data-action="applyAllGrindConditions"]` affordance (L30-36), and one `[data-action="applyGrindCondition"]` per unapplied entry. Button-click wiring (Apply → condition applied) is deliberately out of scope — covered by apply-condition-mailbox.spec.mjs L326. Scene + two actors + message cleaned up in afterEach alongside grind settings reset
- [x] `tests/e2e/grind/light-extinguish.spec.mjs` — two linked legs of the torch-extinguish flow. (a) End-to-end: seeds a holder character with a lit `supply` + `supplyType: "light"` + `turnsRemaining: 1` in hand-R, plus a scene-resident covered character wired via `flags.tb2e.grindCoveredBy = holderId` with `system.lightLevel = "full"`. Advance via the HUD button crosses turnsRemaining→0, which hits the decrement branch at `module/applications/grind-tracker.mjs` L320-323 (flips `lit: false`, sets `turnsRemaining: 0`) AND L337 (`#postTorchExpiredCard` → `ChatMessage.create` with the `templates/chat/torch-expired.hbs` body — asserts exactly ONE message carrying `.grind-torch-card` class posted). The `updateItem` hook (`tb2e.mjs` L251-271) then runs the GM branch L260-267 and writes `system.lightLevel: "dark"` on the covered actor. (b) Mailbox: bypasses the end-to-end flow and directly simulates the non-GM write at `tb2e.mjs` L269 (`holder.setFlag("tb2e", "pendingLightExtinguish", true)`) — same harness-constraint idiom as `apply-condition-mailbox.spec.mjs` L172-185, because all Playwright sessions authenticate as GM. The `updateActor` hook at L232-243 darkens covered scene actors (L240) and unsets the mailbox (L241). Asserts the three observable effects: flag cleared, covered `lightLevel === "dark"`, baseline sanity `{coveredLight: 'full', mailbox: null}` pre-write. Scene+tokens use `Hooks.once('canvasReady', ...)` gate (same pattern as `compendium/drag-monster-to-scene.spec.mjs` L101-103) so the TokenDocument render pipeline is fully drawn before the actor-update cascade touches `RenderFlags.set`. Cleanup sweeps both mailbox + `grindCoveredBy` flags, all created actors + scene + messages, and resets `grindTurn/Phase/Extreme` in afterEach

---

## 11. Nature Crisis

### Agent briefing

**Files:**
- `module/dice/post-roll.mjs` — nature tax trigger
- `templates/chat/nature-crisis.hbs`
- Rule: DH p.119

**Checkboxes:**

- [x] `tests/e2e/nature/crisis-triggered.spec.mjs` — deep shape of the pending nature-crisis chat card emitted when a post-roll tax drops rating to 0 (`_postNatureCrisis` post-roll.mjs:600-632 / nature-crisis.hbs): flags (`natureCrisis:true`, `actorId`, no `crisisResolved`), DOM (amber card accent, header name + `{name}'s Nature Crumbles` label, crisis text containing DH p.119 rule intent), select listing non-class traits only (class traits filtered by post-roll.mjs:609), placeholder `<option>—</option>`, new-name input, `.nature-crisis-confirm` button; asserts rating=0, max unchanged (resolve path NOT exercised — next checkbox)
- [x] `tests/e2e/nature/recovery.spec.mjs` — resolve path for the nature-crisis chat card. Stages line 343's crisis trigger (Will 2, Nature 1/4, Persona 1, one non-class trait "Curious" L2 beneficial=1; PRNG all-6s → 3D Ob 2 → PASS → tax 1 → rating 0 → `_postNatureCrisis` posts card). Then fills `.crisis-trait-select` + `.crisis-new-name` and clicks `.nature-crisis-confirm`, driving the resolve handler `activateNatureCrisisListeners` (post-roll.mjs L639-708). Asserts the four observable effects of L665-706: (1) `flags.tb2e.crisisResolved` flipped true (L680); (2) nature mutated to `{rating:3, max:3, pass:0, fail:0}` — max decremented (L671), rating reset to newMax (L674), pass/fail zeroed (L675-676), per DH p.119 "Maximum Nature is reduced by 1 and all advancement progress is lost"; (3) trait RENAMED in place (`traitItem.update({ name: newName })` L668) — same itemId, level/beneficial/isClass preserved, trait count unchanged (NOT delete+create); (4) card DOM transitioned to resolved branch (templates/chat/nature-crisis.hbs L10-24) — `.crisis-form` + `.nature-crisis-confirm` gone, `.crisis-resolved` + `.card-banner.banner-amber` "Resolved" present (non-retired since newMax=3), resolved text cites new trait name + "Nature max is now 3" (template L12). Retirement path (newMax=0) explicitly NOT covered; isClass filter covered by L343

---

## 12. Conflict: Setup

### Agent briefing

**Files:**
- `module/applications/conflict/conflict-panel.mjs` — 7-tab wizard
- `module/applications/conflict/conflict-tracker.mjs` — sidebar scoreboard
- `module/documents/combat.mjs` — TB2ECombat (extends Combat)
- `templates/conflict/panel-setup.hbs`
- Conflict types: config.mjs `CONFIG.TB2E.conflictTypes` (14 types: Kill, Capture, Chase, DriveOff, Flee, Convince, ConvinceCrowd, Trick, Negotiate, Abjure, Riddle, War, Journey, Manual)

**Gotchas:**
- `combatant.actor` not `game.actors.get(combatant.actorId)` (CLAUDE.md synthetic token section)
- Combatant data is `system.pending*` mailboxes for cross-permission writes

**Checkboxes:**

- [x] `tests/e2e/conflict/setup-create-conflict.spec.mjs` — create a new conflict, assert panel opens on setup tab, tracker appears in sidebar (cites `conflict-tracker.mjs:267-269`, `combat.mjs:20-48`, `conflict-panel.mjs:17,40-41,510-526`, `tb2e.mjs:67`)
- [x] `tests/e2e/conflict/setup-add-combatants.spec.mjs` — add two characters + two monsters via the panel, assert combatant list (cites `conflict-panel.mjs:2127-2158` select-path, `conflict-panel.mjs:2165-2198` drop-path, `conflict-panel.mjs:655-660` availableActors filter, `combat.mjs:20-35` default-groups seed, `panel-setup.hbs:83-136` combatant-list DOM, top-level `combatant._source.group` grouping field)
- [x] `tests/e2e/conflict/setup-assign-captain.spec.mjs` — assign captain per side via the crown button; verify `combat.system.groupDispositions[groupId].captainId` persists, DOM `.is-captain` reflects the choice, "Next" (`canBeginDisposition`) gates on both captains assigned, and reassignment is last-write-wins (cites `panel-setup.hbs:82-113,141` UI, `conflict-panel.mjs:1479-1486` #onSetCaptain + `638-639,646,709-710` context, `combat.mjs:101-106` setCaptain storage)
- [x] `tests/e2e/conflict/setup-assign-boss.spec.mjs` — assign monster boss via the shield button; verify `combatant.system.isBoss` persists, DOM `.setup-boss-btn.active` reflects the state, the button is monster-only (hidden on party rows via `{{#if this.isMonster}}`), and the handler is a per-combatant toggle so multiple monsters can be boss simultaneously and a second click clears (cites `panel-setup.hbs:97-103` UI + `648` isMonster derivation, `conflict-panel.mjs:1494-1501` #onSetBoss + `647` context, `module/data/combat/combatant.mjs:8` schema field)
- [x] `tests/e2e/conflict/setup-select-type.spec.mjs` — cycle all 14 conflict types via the UI select; verify each persists on `combat.system.conflictType`, `combat.getEffectiveConflictConfig()` returns the config.mjs mapping (`dispositionAbility`, `dispositionSkills`, all four `actions`), and the `.setup-manual-config` block is gated on `conflictType === "manual"` (cites `panel-setup.hbs:5-14,38-80` UI, `conflict-panel.mjs:201-207` change handler + `585-589` type-options + `663-707` manual-config context, `combat.mjs:42-48` seed + `70-89` getEffectiveConflictConfig, `config.mjs:188-386` conflictTypes)

---

## 13. Conflict: Disposition

### Agent briefing

**Files:**
- `module/applications/conflict/conflict-panel.mjs` — `rollDisposition`, `distribute`, `setFlatDisposition`
- `templates/conflict/panel-disposition.hbs`
- `module/documents/combat.mjs` — `distributeDisposition()`
- Mailboxes: `system.pendingDisposition`, `system.pendingDistribution` on combatant

**Rules:**
- Disposition roll: captain rolls selected skill or ability bonus + conflict-type skill; total = team HP (DH pp.120–122)
- Monster disposition is typically flat from stat block.
- Distribution: captain splits total across teammates.

**Checkboxes:**

- [x] `tests/e2e/conflict/disposition-roll-captain.spec.mjs` — GM captain rolls disposition, verify `conflict.hp` set on each combatant (kill conflict, fighter+health; stubs PRNG → 3 successes + 4 health = 7; clicks Finalize → `storeDispositionRoll`; Distribute writes `conflict.hp.{value,max}` on party members; monster side stays at 0 as negative control; cites `conflict-panel.mjs#L1543-1574`, `L1582-1653`, `L1661-1683`, `L791-820`; `combat.mjs#L144-174`, `L201-242`; `post-roll.mjs#L483-504`; `tb2e-roll.mjs#L1659-1739`; `config.mjs#L200-211`)
- [x] `tests/e2e/conflict/disposition-flat-monster.spec.mjs` — monster side uses flat disposition; verify HP set without roll (Bugbear captain + Goblin — both have `Kill` stat-block hp=7; panel computes `isListedConflict=true` and `suggestedDisposition = 7 + 1 = 8` per `conflict-panel.mjs#L736-761`; roll button suppressed by `canRoll` gating at `L838-840`; clicking `setFlatDisposition` button dispatches to `#onSetFlatDisposition` (`L1509-1521`) which calls `combat.storeDispositionRoll` (`combat.mjs#L201-210`) directly — no `rollTest`, no roll dialog, no chat card posted; Distribute writes `conflict.hp.{value,max}=4` on each monster's synthetic token actor via `combat.mjs#L219-242`; party side stays at 0 HP as the negative control; Bugbear stat block at `packs/_source/monsters/Bugbear_a1b2c3d4e5f60005.yml#L23-29`, Goblin at `packs/_source/monsters/Goblin_a1b2c3d4e5f6000c.yml#L23-29`; monster data model at `module/data/actor/monster.mjs#L30-33`)
- [ ] ~~skipped~~ `tests/e2e/conflict/disposition-distribution-player.spec.mjs` — **PRODUCTION BUG**: `module/documents/combat.mjs#L489` clears `system.pendingDistribution` with `combatant.update({ "system.pendingDistribution": {} })`, but Foundry deep-merges the empty object over the `ObjectField` (`module/data/combat/combatant.mjs#L20`), leaving the `{ groupId, distribution }` payload intact. Spec is written and ready behind `test.fixme()`; the write + GM-hook + processing chain otherwise works (HP set on each member via `combat.mjs#L219-242`; `groupDispositions[partyGroup].distributed` flips at `combat.mjs#L236-241`). Same `{}`-over-ObjectField anti-pattern affects the `pendingDisposition` (L475) and `pendingActions` (L516) clears, so a fix should sweep all three. Switch the production handler to the `system.-=pendingDistribution` deletion idiom (or call `combatant.updateSource` to reset the schema field), then remove the fixme. Hook dispatcher: `combat.mjs#L431-462` (picks off `changes.system.pendingDistribution.groupId` at L445-447). Processor: `#processDistribution` at `combat.mjs#L485-490`. Player-side write site: `combat.mjs#L220-225` (payload `{ groupId, distribution }` keyed by combatantId).
- [ ] ~~skipped~~ `tests/e2e/conflict/disposition-order-of-might.spec.mjs` — **PRODUCTION GAP**: SG p.80 says the Order of Might +1s/point advantage applies to "all successful tests in these conflicts" — which includes the disposition test (first roll of a kill/capture/driveOff conflict). `computeOrderModifier` (`module/dice/conflict-roll.mjs#L312-345`) correctly returns a +Ns success modifier (type `"success"`, timing `"post"`) for `MIGHT_CONFLICT_TYPES={"kill","capture","driveOff"}` at `conflict-roll.mjs#L185`, and `_handleDispositionRoll` DOES apply `postSuccessMods` to `finalSuccesses` at `module/dice/tb2e-roll.mjs#L1681-1687` — so the plumbing exists end-to-end. The gap is that `ConflictPanel.#onRollDisposition` (`module/applications/conflict/conflict-panel.mjs#L1582-1653`) builds its own `contextModifiers` array at L1615 but only pushes the monster group-help dice bonus (L1616-1632); it never invokes `computeOrderModifier`, so the disposition roll's success total never receives the advantage. `computeOrderModifier` IS consumed for volley action rolls at `conflict-panel.mjs#L1927-1933`. Fix mirrors that block: compute `opponentGroupId` from `combat.groups`, call `computeOrderModifier({ conflictType: combat.system.conflictType, ourGroupId: groupId, opponentGroupId, combat })`, and push the result (if non-null) into `contextModifiers`. Spec is written and ready behind `test.fixme()` — fixture: Kill conflict, party captain Might=5 / fighter=3 / health=4, two Kobolds (Might=1) on the other side; PRNG stubbed to all-6s; expected disposition = 3 base successes + 4 Order of Might + 4 health = 11 (currently computes 7). The pre-flight call to `computeOrderModifier` in the spec asserts `{ value: 4, type: "success", timing: "post" }` so when the fix lands, it's the dispatcher plumbing — not the math — that the fixme removal is guarding.
- [ ] ~~skipped~~ `tests/e2e/conflict/disposition-precedence.spec.mjs` — **PRODUCTION GAP (same dispatcher gap as L393)**: SG p.82 "Aura of Authority" applies +1s per point of Precedence advantage to all successful tests in Convince/ConvinceCrowd/Negotiate conflicts — which includes the disposition test (first roll of the conflict per DH pp.120-122). `computeOrderModifier` (`module/dice/conflict-roll.mjs#L312-345`) already handles Precedence at L321-328 via PRECEDENCE_CONFLICT_TYPES=`{"convince","convinceCrowd","negotiate"}` (`conflict-roll.mjs#L188`); character precedence is read from `system.abilities.precedence` (`character.mjs#L83`, NumberField max 7), monster precedence from the string `system.precedence` field parsed by `parsePrecedence` (`conflict-roll.mjs#L198-231`); `getTeamPrecedence` (`conflict-roll.mjs#L287-295`) computes the team max; `_handleDispositionRoll` applies `postSuccessMods` to `finalSuccesses` at `module/dice/tb2e-roll.mjs#L1681-1687`. The gap is identical to L393 — `ConflictPanel.#onRollDisposition` (`module/applications/conflict/conflict-panel.mjs#L1582-1653`) builds its own `contextModifiers` array at L1615 but only pushes the monster group-help dice bonus (L1616-1632); it never invokes `computeOrderModifier`, so the disposition roll's success total never receives the Precedence advantage. `computeOrderModifier` IS consumed for volley action rolls at `conflict-panel.mjs#L1927-1933`. One fix closes both L393 and L394 — mirror the volley block: compute `opponentGroupId` from `combat.groups`, call `computeOrderModifier({ conflictType, ourGroupId: groupId, opponentGroupId, combat })`, and push the result (if non-null) into `contextModifiers`. Spec is written and ready behind `test.fixme()` — fixture: Convince conflict, party captain persuader=3 / will=4 / precedence=5, two Kobolds (precedence="1" per `packs/_source/monsters/Kobold_a1b2c3d4e5f60001.yml#L9`) on the other side; PRNG stubbed to all-6s; expected disposition = 3 base successes + 4 Aura of Authority + 4 will = 11 (currently computes 7). Convince config at `module/config.mjs#L275-293` (`dispositionSkills: ["persuader"]`, `dispositionAbility: "will"`). Label "Aura of Authority +{n}s" at `lang/en.json#L503`. Pre-flight call to `computeOrderModifier` in the spec asserts `{ value: 4, type: "success", timing: "post" }` so the fixme removal is guarding the dispatcher plumbing — not the math.

---

## 14. Conflict: Weapons

### Agent briefing

**Files:**
- `conflict-panel.mjs` — weapons tab handlers
- `templates/conflict/panel-weapons.hbs`
- Each conflict type has `conflictWeapons` (e.g., "Blackmail" for social conflicts); some assignable bonuses

**Checkboxes:**

- [x] `tests/e2e/conflict/weapons-assign-per-combatant.spec.mjs` — assign a weapon per combatant via dropdown; verify state persists through scripting (Kill conflict, `usesGear=true`, two chars + two monsters; captain gets a real weapon Item attached, charB picks `__unarmed__`, monA picks `__monster_0__` (Bugbear stat-block weapon), monB picks `__unarmed__` — the four dropdown shapes per conflict-panel.mjs L933-971; the `.weapon-select` change listener at conflict-panel.mjs L146-167 fires `combat.setWeapon` (combat.mjs L268-274) without awaiting, so the POM `selectWeapon` polls on `combatant.system.weaponId` (the first awaited write inside `setWeapon`). Asserts combatant `weapon`/`weaponId` for all four and the actor mirror `system.conflict.{weapon,weaponId}` only for characters — monsters carry `conflict.{hp,team}` only at `monster.mjs` L46-52 so the mirror is silently dropped by the schema; characters have those fields per `character.mjs` L161-168. Advances via `beginScripting` button (panel-weapons.hbs L52-56, gated by `allArmed` at conflict-panel.mjs L978-979) → `combat.beginScripting` (combat.mjs L282-312), then re-reads storage and asserts identity. Staging bypasses the roll UI by calling `combat.storeDispositionRoll` + `distributeDisposition` directly (DispositionRoll-roll-captain spec L390 already covers the roll path end-to-end). Extends `ConflictPanel.mjs` with `weaponsContent` / `weaponGroup` / `weaponSelect` / `weaponOptionValues` / `selectWeapon` / `beginWeaponsButton` / `clickBeginWeapons` / `beginScriptingButton` / `clickBeginScripting` helpers.)
- [x] `tests/e2e/conflict/weapons-improvised.spec.mjs` — use "improvised" weapon with custom name; verify stored and displayed (Kill conflict, `usesGear=true`; one character picks `__improvised__` from the weapon dropdown — rendered by conflict-panel.mjs L945 on the gear branch — which surfaces the sibling `input.weapon-improvised-input` (panel-weapons.hbs L24-29) via the change handler at conflict-panel.mjs L152-160 that strips the `hidden` class. The input's own `change` handler at conflict-panel.mjs L181-189 calls `combat.setWeapon(id, trimmed, "__improvised__")` (combat.mjs L268-274), writing `combatant.system.{weapon,weaponId}` AND mirroring `actor.system.conflict.{weapon,weaponId}` (character-only — monster.mjs L46-52 drops the mirror). Asserts: default-name path (empty input → localized "Improvised" fallback per conflict-panel.mjs L157); custom-name path using a whitespace-padded label `"  Broken Chair Leg  "` that exercises the trim branch at L186; DOM echo via `select.weapon-select` value + `input.weapon-improvised-input` `value` attribute rehydrated from `{{this.weapon}}` at panel-weapons.hbs L26; persistence across the weapons → scripting phase transition (combat.mjs L282-312 only touches phase/round state). Extends `ConflictPanel.mjs` with `weaponRow` / `improvisedInput` / `setImprovisedName` helpers — `setImprovisedName` fills + dispatches `change` (Playwright's `fill` only fires `input`, but the handler listens for `change`) and polls `combatant.system.weapon` until the trimmed value lands. Staging is minimum-viable: 2 characters (party) + 1 Bugbear (GM captain), flat-set disposition on both sides bypasses the roll UI; charB/monA armed with sentinel weapons purely to unblock `canBeginScripting`.)
- [x] `tests/e2e/conflict/weapons-assignable-bonus.spec.mjs` — assignable conflict weapon (Blackmail) grants +1D to the target action chosen by the player (Convince conflict — non-`usesGear` with a `conflictWeapons` list per config.mjs L275-293; the character dropdown builds on the `conflictWeapons.length` branch at conflict-panel.mjs L952-962, which lists Blackmail + the five other social weapons + `__improvised__` + `__unarmed__`). The assignable flag on the config entry (`{ bonuses: [{ type: "dice", value: 1, assignable: true }], assignable: true }` at config.mjs L278) drives `isAssignable = conflictWeapons.find(w => w.id === weaponId)?.assignable` at conflict-panel.mjs L894, which sets `showAssignment` (L905), which the template gates the extra `<select class="weapon-assignment-select">` on at panel-weapons.hbs L30-37. The change handler at conflict-panel.mjs L170-178 writes `combatant.system.weaponAssignment = <action>` (field declared at `module/data/combat/combatant.mjs` L12). Asserts: the assignment select is absent before the assignable weapon is picked and present after; storage of `weaponId=blackmail` + actor mirror on the character; storage of `weaponAssignment="attack"` after the user picks the target action. Then replays the bonus-resolution logic from `#onRollAction` conflict-panel.mjs L1935-1965 inline via `page.evaluate` using `combat.getEffectiveConflictConfig()` and the stored combatant state — the rule under test is `targetAction = bonus.assignable ? resolvedCombatant.system.weaponAssignment : bonus.action` (L1953) followed by `if (targetAction !== actionKey) continue` (L1954). Verifies the resulting modifier set for two action keys: for `"attack"` (the assigned action) it surfaces exactly `{ label: "Blackmail", type: "dice", value: 1, source: "conflict" }` and for `"defend"` it is empty. Then reassigns to `"defend"` and re-verifies the modifier follows — ensuring the bonus is driven by the live assignment, not pinned to the originally selected action. Staging bypasses the roll UI via `combat.storeDispositionRoll` + `distributeDisposition` (covered e2e elsewhere — TEST_PLAN.md L390-391); scripting/resolve is not entered (covered by §15-§16). Extends `ConflictPanel.mjs` with `weaponAssignmentSelect` / `selectWeaponAssignment` helpers.)

---

## 15. Conflict: Scripting

### Agent briefing

**Files:**
- `conflict-panel.mjs` — `beginScripting`, `lockActions`, `peekActions`
- `templates/conflict/panel-script.hbs`
- Mailbox: `system.pendingActions`, `system.pendingActionsLocked`
- 4 action types: Attack, Defend, Feint, Maneuver (config.mjs 392–397)

**Checkboxes:**

- [x] `tests/e2e/conflict/script-assign-actions.spec.mjs` — each player selects A/D/F/M per volley; verify `pendingActions` mailbox updated (Kill conflict, 2 characters + 2 monsters; stages through weapons via `combat.setWeapon` direct writes — the weapons-tab UI is covered by the §14 specs L409-411. Once on the scripting tab asserts three `.script-slot[data-slot-index]` rows and four action-card buttons per slot — one per key in `CONFIG.TB2E.conflictActions` (config.mjs L392-397: attack/defend/feint/maneuver). Exercises two paths: (1) UI path — clicks action-card + picks combatant for volleys 0/1/2 (attack/defend/feint), which runs through `#onActionCardClick` conflict-panel.mjs L228-250 → `#syncPendingActions` L418-442 (300ms debounced) → `combat.setActions` combat.mjs L324-333. Our Playwright session is GM so `setActions` takes the GM branch and calls `#applyActions` directly (L498-518), writing `system.rounds[currentRound].actions[partyGroupId]`. Asserts the stored array matches `[{action:attack,combatantId:captain}, {defend,charB}, {feint,captain}]` and the `.selected` class lands on each chosen action-card. (2) Mailbox path — simulates the non-GM write branch by directly issuing `captain.update({"system.pendingActions": [{action:maneuver,…}, {attack,…}, {defend,…}]})`, the exact payload `combat.mjs` L329 emits on the player branch. The in-browser GM hook at `_onUpdateDescendantDocuments` L431-462 — gated on `game.user.isGM` L433 — observes `changes.system.pendingActions?.length` (L449) and dispatches to `#applyActions` with `mailboxId` (L498-518). Asserts `system.rounds[n].actions[partyGroupId]` now holds the mailbox payload AND the captain's `_source.system.pendingActions` is drained to `[]`. The ArrayField schema (data/combat/combatant.mjs L21) means the `[]` clear at combat.mjs L516 replaces atomically — unlike the sibling `pendingDisposition`/`pendingDistribution` `ObjectField` mailboxes whose `{}` clear deep-merges (flagged fixme in conflict/disposition-distribution-player.spec.mjs L392). Extends `ConflictPanel.mjs` with `scriptContent` / `scriptGroup` / `scriptSlot` / `actionSelectInput` / `actionCard` / `combatantSelect` / `clickAction` / `selectSlotCombatant` / `lockActionsButton` helpers. Coverage: all four A/D/F/M keys exercised across the two paths.)
- [x] `tests/e2e/conflict/script-lock.spec.mjs` — lock actions; verify `pendingActionsLocked` set, UI shows locked state (Kill conflict, 2 characters + 2 monsters; stages through weapons + scripting via direct `combat.setWeapon` + `combat.setActions` writes — per §14/L427 the weapons + action-assign UI is covered elsewhere. Pre-seeds party+GM groups with 3 full A/D/F/M slots so `#applyLockActions`'s all-slots-filled gate at combat.mjs L532-536 opens. Exercises two paths: (1) GM UI path — clicks `lockActionsButton` on the party group, which dispatches to `ConflictPanel.#onLockActions` conflict-panel.mjs L1735-1774 → `combat.lockActions(groupId)` L341-350. Because our viewer is GM, L342 falls through to `#applyLockActions(groupId)` L349 directly (no mailbox). Asserts `system.rounds[n].locked[partyGroupId] === true` and the captain's `_source.system.pendingActionsLocked` stays `false` (never written on GM path). Then asserts DOM: `.script-locked-badge` visible (panel-script.hbs L10-14), `button[data-action="lockActions"]` gone (L68 `{{#if this.canLock}}` → L1152 flips false), `.script-slots.locked` container rendered (L107), AND no `button.action-card` in the group at all (the read-only view L108-126 emits only `.script-card-flip` faces). (2) Mailbox path — simulates the non-GM write branch for the GM captain by `mon.update({"system.pendingActionsLocked": true})` — the exact payload combat.mjs L346 emits. The GM hook at `_onUpdateDescendantDocuments` L431-462 (gated on `game.user.isGM` L433) observes `pendingActionsLocked` L452 and dispatches to `#applyLockActions(groupId, mailboxId)` L525-546. Asserts `round.locked[gmGroupId]` flips true AND the captain's `_source.system.pendingActionsLocked` is drained back to `false` (L544). The `BooleanField` schema at data/combat/combatant.mjs L22 means the `false` clear is an atomic scalar assignment — unlike the sibling `ObjectField` mailboxes (`pendingDisposition`, `pendingDistribution`) flagged in `conflict/disposition-distribution-player.spec.mjs`. Also asserts `rounds[n].actions[gmGroupId]` survives the lock untouched (lock is orthogonal to action-assign), and that `canBeginResolve` (conflict-panel.mjs L1169-1171 → panel-script.hbs L167-172) flips true once both sides are locked — surfaces the downstream effect. Extends `ConflictPanel.mjs` with `scriptLockedBadge` + `scriptSlotsLocked` helpers.)
- [x] `tests/e2e/conflict/script-peek-gm.spec.mjs` — GM peek shows scripted actions; verify player view remains hidden (Kill conflict, 2 characters + 2 monsters; stages through the setup→disposition→weapons→scripting→lock flow via `combat.setWeapon` / `combat.setActions` / `combat.lockActions` direct writes so the non-peek UI paths stay off the critical path — prior art from L427/L428. Once both groups are locked the scripting tab emits the peek affordance: panel-script.hbs L100-105 / L132-137, rendered iff `gmCanPeek = game.user.isGM && isPartyGroup` (conflict-panel.mjs L1164). `isPartyGroup` is derived at L1124-1127 from any member actor's `hasPlayerOwner`, so the spec first spins up a throwaway `Player`-role user (the seed world ships with only a Gamemaster — `auth.setup.mjs` L23 / the empty `worlds/tb2e-e2e/` seed) and grants that user OWNER rights on both party characters at create-time; without this scaffold `hasPlayerOwner` is always false and the peek button is never rendered. Clicking the button dispatches to `ConflictPanel.#onPeekActions` (L2311-2320) which toggles the group's membership in the panel's private `#gmPeekGroups` `Set<string>` (L31-32) and calls `this.render()`. The toggle surfaces on the button as `.active` via `gmIsPeeking = #gmPeekGroups.has(group.id)` (L1165 → L101/L133). Spec asserts: (a) peek button visible on party group, absent from GM group (gmCanPeek false there because the GM group has no player-owner member); (b) pre-click `.active` off; (c) first click → `.active` on; (d) second click → `.active` off (idempotent toggle); (e) combat + both captains' `_source.system.pendingActions`/`pendingActionsLocked` + `combat.flags` deep-equal snapshots taken before peek, after peek, and after unpeek — verifying the "player view remains hidden" half of the briefing structurally: no document is mutated, so a player client reading the same documents (the harness can't drive a non-GM session — all Playwright sessions authenticate as GM via `auth.setup.mjs` L14-35) produces identical DOM before and after the GM's click. The spec does **not** assert the card-front reveal count (the DOM signal most directly tied to `canViewActions` flipping): Foundry's `Document#testUserPermission` (common/abstract/document.mjs L404-412) gives GMs OWNER permission over every document unconditionally, so `actor.isOwner` is always true for the GM, which makes `isMember` at conflict-panel.mjs L1118-1121 always true, which short-circuits `canViewActions` at L1136-1137 to true regardless of peek state — the card-front faces are therefore ALWAYS emitted to a GM. The `.active`-class assertion is the GM-observable half of the peek toggle, and the no-mutation assertion covers the non-GM half. Also: `combat.setActions` at combat.mjs L332 calls `#applyActions` without `await` — the spec polls for `round.actions[groupId]` to stabilise before firing `lockActions`, or `#applyLockActions`'s all-filled gate at L534 silently no-ops. Extends `ConflictPanel.mjs` with `peekActionsButton` helper.)
- [x] `tests/e2e/conflict/script-change-before-lock.spec.mjs` — changing an action before locking updates the mailbox (Kill conflict, 2 characters + 2 monsters; stages through disposition + weapons via `storeDispositionRoll` / `distributeDisposition` / `combat.setWeapon` direct writes — prior art from L427/L428. Once on the scripting tab the spec exercises both write paths and asserts the "change" replays cleanly. **UI path**: select-combatant + click `attack` on volley 0 for the captain (runs through `#onActionCardClick` conflict-panel.mjs L228-250 → `#syncPendingActions` L418-442 (300ms debounced) → `combat.setActions` combat.mjs L324-333 → GM-branch `#applyActions` L498-518), poll `round.actions[partyGroupId][0].action === "attack"` to wait out the debounce + write. Then click `defend` on the SAME slot (no lock in between) and poll for the replacement. Asserts (a) stored entry flips to `{action: "defend", combatantId: captain}`, (b) `.selected` class migrates — defend HAS it, attack does NOT (L239-241 toggles exclusive), (c) captain's `_source.system.pendingActionsLocked` stays `false` and `round.locked[partyGroupId]` stays `false` throughout (the "before lock" invariant), (d) the `lockActionsButton` is still visible (canLock still true per L1152) and `.script-locked-badge` absent. **Mailbox path**: simulates the non-GM write branch by issuing `captain.update({"system.pendingActions": [3x feint]})` — the exact payload combat.mjs L329 emits. The in-browser GM hook at `_onUpdateDescendantDocuments` L431-462 (gated on `game.user.isGM` L433) observes `changes.system.pendingActions?.length` (L449) and dispatches to `#applyActions` with `mailboxId`. Asserts first write lands (all-feint) + mailbox drains to `[]`. Then re-writes `captain.update({"system.pendingActions": [3x maneuver]})` WITHOUT locking in between — exercises `#applyActions`'s lock-gate at L503 (`if round.locked?.[groupId] return`) which silently no-ops once locked but here accepts the re-write because lock is still false. Asserts stored actions end on `maneuver` (not `feint`), mailbox drains again. Covers all four A/D/F/M config.mjs L392-397 keys across the two paths (attack + defend via UI, feint + maneuver via mailbox). Final sanity: re-clicks an attack on volley 1 via UI AFTER the two mailbox rewrites to confirm the editable surface + `#pendingSelections` cache survived. `pendingActions` is an `ArrayField` (data/combat/combatant.mjs L21) so `[]` clear at combat.mjs L516 replaces atomically — immune to the `ObjectField` deep-merge bug flagged on sibling mailboxes `pendingDisposition`/`pendingDistribution` in `disposition-distribution-player.spec.mjs` L392. No new POM helpers — reuses L427 `clickAction`/`selectSlotCombatant`/`actionCard`/`scriptSlot` and L428 `scriptLockedBadge`/`scriptSlotsLocked`/`lockActionsButton`.)
- [x] `tests/e2e/conflict/script-independent-ko-sub.spec.mjs` — if a combatant is KO'd mid-round, scripting UI handles substitution (see `swapActionCombatant`) (Kill conflict, 2 characters + 2 monsters; stages through disposition/weapons/scripting/lock via direct writes — prior art from L427/L428/L430. Design note: although the checkbox description says "scripting UI", the `needsSwap`/`swapCandidates` computation at conflict-panel.mjs L1212-1222 guards on `isCurrent && !volley.result`, and panel-resolve.hbs L36-53 emits the `.resolve-pre-swap` block only inside `.resolve-action.current` + `!isRevealed`. The scripting tab (panel-script.hbs) itself has no swap control — once locked it renders a read-only card view. The spec therefore transitions to `phase === "resolve"` via `combat.beginResolve` (combat.mjs L357-373) which is the earliest point the pre-swap UI appears. Pre-seeds `partyActions = [attack→captain, defend→charB, feint→captain]` (captain scripted for volleys 0 AND 2 so the actionIndex-scoped swap can be verified), locks both sides via `combat.lockActions` direct writes (GM branch — L349), calls `combat.beginResolve` to flip `currentAction = 0`. Precondition: no `.resolve-pre-swap` block emitted at full HP. **Act 1**: `captain.actor.update({"system.conflict.hp.value": 0})`. The panel's `updateActor` hook (conflict-panel.mjs L126-129) re-renders. Asserts (a) `.resolve-ko-warning` (skull icon + "Knocked Out" label, panel-resolve.hbs L41) visible on volley 0's party side, (b) `.resolve-swap-select[data-group-id=partyGroupId]` visible, (c) option values = `['', charB]` — captain excluded (self, L1219), monA/monB excluded (different group, L1217), charB present (alive same-group teammate, L1218-1221), leading empty placeholder from panel-resolve.hbs L44; (d) GM group's swap-select absent (captain's KO scoped to party group); (e) `round.locked[partyGroupId] === true` preserved (swap is a **post-lock** re-route per combat.mjs L413-424 which never touches `locked`). **Act 2**: drives the `.resolve-swap-select` change event (selectOption charB), which fires the handler at conflict-panel.mjs L363-373 → `combat.swapActionCombatant(0, partyGroupId, charB)` (combat.mjs L413-424, gated on isGM L414 — our session is GM). Asserts full `round.actions[partyGroupId]` structure: `[{attack,charB}, {defend,charB}, {feint,captain}]` — only `actionIndex=0`'s `combatantId` mutated (L420-422 writes the single entry), volleys 1+2 untouched, GM group's actions preserved. `round.locked` still `{p: true, g: true}`. **Act 3**: after swap, volley 0's side reads HP from charB (alive), so `needsSwap` flips false on re-render — asserts `.resolve-ko-warning` + `.resolve-swap-select` both retract. **Act 4**: calls `combat.swapActionCombatant(0, partyGroupId, captain)` directly to re-point volley 0 back to the still-KO'd captain — the method does NOT re-check lock state or HP (L413-424 only gates on isGM + round existence), which is intentional: swap is the post-lock re-route path. Asserts the re-point lands, lock still true, and the skull warning + select re-emerge. The direct-call path is the same entry-point the DOM handler invokes (L371), so it covers the programmatic contract. Adds `ConflictPanel.mjs` helpers: `resolveContent`, `resolveAction`, `resolveSwapSelect`, `resolveKoWarning`, `selectResolveSwap` (polls `round.actions[groupId][idx].combatantId` to stabilise after the async `this.update`). All Playwright sessions authenticate as GM (auth.setup.mjs L14-35), which matches the `isGM` gate on both `swapActionCombatant` (L414) and the DOM swap-select emission at L1216 — the player branch is structurally unreachable in this harness and the checkbox's scope ("substitution handling") is fully covered by the GM path.)

---

## 16. Conflict: Resolve / Versus

### Agent briefing

**Files:**
- `conflict-panel.mjs` — `beginResolve`, `revealAction`, `rollAction`, `resolveAction`
- `module/dice/conflict-roll.mjs` — `calculateMargin`, `computeOrderModifier`
- `templates/conflict/panel-resolve.hbs`, `conflict-action-reveal.hbs`
- Interaction matrix (config.mjs 407–424): versus / independent / none
- Playing-card animation CSS in `less/conflict/cards.less`

**Rules:**
- DH pp.120–127: action vs action resolution.
- Attack vs Defend = versus; Attack vs Attack = independent with Ob 0/3.
- Feint vs Attack/Defend = none (defender/feinter doesn't roll).

**Checkboxes:**

- [ ] ~~skipped~~ `tests/e2e/conflict/resolve-attack-vs-defend.spec.mjs` — versus resolution, higher successes wins; HP reduced by margin on loser (spec written + `test.fixme` for the HP-reduction half; the resolve pipeline itself IS wired — `combat.beginResolve` combat.mjs L357-373 flips phase + currentAction, `ConflictPanel.#onRevealAction` conflict-panel.mjs L1796-1838 reveals + posts a conflict-action-reveal card, `#onRollAction` L1847-1995 stamps `testContext.isVersus=true` when `getInteraction("attack","defend")==="versus"` per the matrix at config.mjs L409, the pair of versus-tagged roll cards flows through `_handleVersusRoll` tb2e-roll.mjs L1580-1650 → `_executeVersusResolution` versus.mjs L137-267 which posts the versus-resolution card with `winnerId`/margin from finalSuccesses, and `#onResolveAction` L2003-2096 writes `round.volleys[i].result={resolved,sides,interaction,interactionLabel,timestamp}` via `combat.resolveVolley` combat.mjs L772-782 + auto-advances `currentAction`. The spec exercises all of those paths end-to-end with PRNG stubs (0.001/0.5 for all-6s/all-3s) and asserts winner id, chat card posting, reveal interaction classname, resolve metadata. **The production gap** flagged by TEST_PLAN L453's "HP reduced by margin on loser" half: nothing in `_executeVersusResolution` or `#onResolveAction` mutates the loser's `system.conflict.hp.value`. `resolveActionEffect` conflict-roll.mjs L155-178 correctly returns the `{type:"damage",amount:margin}` descriptor for attacks but is imported only as dead code at conflict-panel.mjs L3 — no call site. The only HP write paths are the manual GM roster input conflict-panel.mjs L341-360, the captain-edit `pendingConflictHP` mailbox tb2e.mjs L193-204, and the initial disposition distribution combat.mjs L219-242 — none auto-applied from volley resolution. Checkbox TEST_PLAN L500 (§18 `hp-damage-reduces.spec.mjs`) is where the HP auto-write will be encoded once wired. Guard removal: when `_executeVersusResolution` or `#onResolveAction` starts consuming `resolveActionEffect` with the conflict testContext (both roll messages carry `isConflict/conflictAction/combatId/combatantId/groupId/opponentGroupId` per conflict-panel.mjs L1983-1993) and applying the mutation to the loser-group combatants via the `pendingConflictHP` mailbox (CLAUDE.md §Mailbox Pattern) — including not cascading damage to the attacker on a defend-loss — drop `test.fixme` and flip to `- [x]`.)
- [x] `tests/e2e/conflict/resolve-attack-vs-attack.spec.mjs` — independent roll vs Ob 0; both may hit
- [x] `tests/e2e/conflict/resolve-feint-vs-attack.spec.mjs` — feinter rolls, defender does not; feinter hits on any successes (spec asserts the production matrix behavior, which is the OPPOSITE of the checkbox prose: `feint:attack` = "none" at config.mjs L416 — the feinter's side carries sideInteraction "none" and does NOT roll, while the attacker side carries "independent" via `attack:feint` at config.mjs L410 and rolls at Ob 0. Consistent with the §16 briefing "Feint vs Attack/Defend = none (defender/feinter doesn't roll)" and with `tests = interaction !== "none"` at conflict-roll.mjs L41. Spec verifies: `combat.getVolleyInteraction(0)` returns "none" for party-feint-first; the reveal card renders `.card-interaction.interaction-none`; the party (feint) side's roll button renders with `.invisible` + `disabled` per panel-resolve.hbs L103-106; the GM-only side-interaction `<select>` on the feint side reads "none"; the GM (attack) side rolls with `obstacle=0`, `isVersus:false`, lands in `_handleIndependentRoll` posting a standard roll-result card with a pass banner; no feinter roll message exists (`testContext.conflictAction==="feint"` with `actorId===captainId` count = 0); no versus card exists; `#onResolveAction` writes `round.volleys[0].result = { resolved:true, interaction:"none", sides:2 }` via `combat.resolveVolley` combat.mjs L772-782 and auto-advances `currentAction` to 1. The "feinter hits on any successes" damage-on-success half of the checkbox is the SAME production gap flagged at L453 — `resolveActionEffect` conflict-roll.mjs L155-178 returns the `{type:"damage",amount:margin}` descriptor for feints but is imported only as dead code at conflict-panel.mjs L3; HP auto-write from volley margins is §18 L500 scope.)
- [x] `tests/e2e/conflict/resolve-feint-vs-feint.spec.mjs` — versus resolution between two feints (spec asserts the production versus pipeline for symmetric Feint vs Feint: `combat.getVolleyInteraction(0)` returns "versus" via matrix "feint:feint" at config.mjs L418; both sides roll skill:fighter per kill conflict config.mjs L208; the reveal card renders `.card-interaction.interaction-versus` via `getInteraction(sides[0].action, sides[1].action)` at conflict-panel.mjs L1822; both captains' `#onRollAction` (conflict-panel.mjs L1917-1919) sees sideInteraction "versus" and stamps `testContext.isVersus = true` at L1992, landing both in `_handleVersusRoll` tb2e-roll.mjs L1580-1650 which tags `flags.tb2e.versus.type = "initiator"|"opponent"` and registers in `PendingVersusRegistry` versus.mjs L18-20; Finalize on both cards routes via `_handleFinalize` post-roll.mjs L506-522 into `_executeVersusResolution` versus.mjs L137-267 which posts the resolution card with `flags.tb2e.versus.type = "resolution"`, `winnerId` = higher-successes actor (L160), and margin = |iSuccesses − oSuccesses| (L170); `#onResolveAction` conflict-panel.mjs L2003-2096 writes `round.volleys[0].result = { resolved:true, interaction:"versus", sides:[...] }` via `combat.resolveVolley` combat.mjs L772-782 and auto-advances `currentAction`. Fixture: two character captains on opposing sides — initiator fighter=4 (−1 unarmed = 3D, all-6s PRNG → 3 successes), opponent fighter=2 (−1 unarmed = 1D, all-3s PRNG → 0 successes) → initiator wins, margin=3. Caveat: `testContext.isVersus` is NOT persisted to message flags (`_buildRollFlags` tb2e-roll.mjs L1461-1479 whitelists a subset of testContext fields — isVersus is not in the list), so the spec evidences versus routing via `flags.tb2e.versus.type` + the dialog's pre-set `mode="versus"` instead. HP damage is the SAME production gap flagged at L453 / L500 — `resolveActionEffect` conflict-roll.mjs L155-178 is imported only as dead code at conflict-panel.mjs L3, no call site writes to HP from volley margins — and is deliberately NOT asserted here per §16 briefing "HP damage is the known §18 gap — don't assert HP change; mirror line 453's scoping note".)
- [x] `tests/e2e/conflict/resolve-maneuver-vs-defend.spec.mjs` — versus resolution; MoS opens `maneuver-spend-dialog.mjs` (spec stages a Kill conflict — party captain health=4 maneuvers, GM captain health=2 defends, both unarmed, PRNG 0.001 then 0.5 → 3 vs 0 successes, margin=3 — and asserts the full versus pipeline: `combat.getVolleyInteraction(0)` returns "versus" via matrix `maneuver:defend` at config.mjs L421; reveal card renders `.card-interaction.interaction-versus` via `getInteraction(sides[0].action, sides[1].action)` at conflict-panel.mjs L1822; both `#onRollAction` calls (conflict-panel.mjs L1909-1921) stamp `testContext.isVersus=true` at L1992 landing both rolls in `_handleVersusRoll` tb2e-roll.mjs L1580-1650 and tagging `flags.tb2e.versus.type = "initiator"|"opponent"`; Finalize on both cards routes via `_handleFinalize` post-roll.mjs L506-522 into `_executeVersusResolution` versus.mjs L137-267 which posts the resolution card with `winnerId=captainA` and `maneuverSpend` payload populated per versus.mjs L171-182 (since winner's testContext.conflictAction==="maneuver" and margin>0); the resolution card renders the `button[data-action="spend-maneuver"]` from versus-resolution.hbs L39-45; clicking the button dispatches through `activatePostRollListeners` post-roll.mjs L21-27 into `_handleManeuverSpend` post-roll.mjs L63-107 which synchronously renders `ManeuverSpendDialog` at L106; the dialog mounts with id "maneuver-spend-dialog" (DEFAULT_OPTIONS L78) and exposes the MoS-3 combo list (SPEND_COMBINATIONS[3] at maneuver-spend-dialog.mjs L19-24 → impede/position/impedePosition/disarm — 4 combos asserted); finally `#onResolveAction` writes `round.volleys[0].result = { resolved, interaction:"versus", sides:2 }` via `combat.resolveVolley` combat.mjs L772-782 and auto-advances `currentAction` to 1. HP damage + the spend itself (mailbox write at maneuver-spend-dialog.mjs L265-271) are deliberately NOT asserted — §17 L466-474 covers the 7 per-combo spend paths and §18 L500 covers the HP gap flagged at L453/L456. Spec creates a `ManeuverSpendDialog` POM at `tests/e2e/pages/ManeuverSpendDialog.mjs` with open/close + combo-radio helpers sized for §17 reuse.)
- [x] `tests/e2e/conflict/resolve-card-animation.spec.mjs` — assert the playing-card DOM elements appear on reveal (spec asserts the reveal DOM transition on volley 0 of a staged Kill conflict — two characters per side, both captains scripting ATTACK → `attack:attack` = "independent" per the matrix at config.mjs L408. No PRNG stub needed since no roll is triggered. **Pre-reveal**: `.resolve-action.current` renders only `button[data-action="revealAction"]` (panel-resolve.hbs L55-58 GM-only gate), no `.resolve-matchup` or `.resolve-action-card` elements exist yet (template L35 `{{#unless this.isRevealed}}` branch). **Post-reveal (panel DOM)**: clicking the Reveal button dispatches to `#onRevealAction` conflict-panel.mjs L1796-1838 which calls `combat.revealVolley(0)` (flipping `round.volleys[0].revealed = true`), triggering the updateCombat hook at conflict-panel.mjs L120-129 to re-render; the `{{else}}` branch at panel-resolve.hbs L65 swaps the Reveal button out and `.resolve-matchup` in (L67-114) with exactly two `.resolve-side` blocks (one `{{#each this.sides}}` iteration per CombatantGroup, L69), each containing `.resolve-action-card.action-card-attack` (L82-85 — the "playing-card" DOM per the §16 briefing) with a Font Awesome `<i>` action icon (L83) and a non-empty `.action-card-label` span (L84), plus exactly one `.resolve-vs` "vs" separator between them (L112 `{{#unless @last}}`). **Post-reveal (chat DOM)**: `#onRevealAction` L1825-1837 renders `templates/chat/conflict-action-reveal.hbs` and posts a ChatMessage whose content includes `.conflict-reveal-card` (L2), `.card-matchup` (L7) with two `.card-matchup-action.action-attack` chips (L11 — action key from the side's `entry.action` at conflict-panel.mjs L1815) plus exactly one `.card-matchup-vs` separator (L13), and `.card-interaction.interaction-independent` (L16 — interaction key from `getInteraction(sides[0].action, sides[1].action)` at conflict-panel.mjs L1822, which resolves to "independent" via the matrix lookup for `attack:attack`). Scope: DOM presence only — no animation timing, no visual regression, no roll/resolve mechanics. The script-phase 3D-flip cards at `.script-card-flip` / `.script-card-face` / `.script-card-back` / `.script-card-front` in less/conflict/panel.less L1180-1299 are a DIFFERENT surface — they live in panel-script.hbs, not the resolve tab; the briefing's "playing-card" label refers loosely to the resolve-tab post-reveal action cards above. Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/resolve-monster-nature.spec.mjs` — monster uses Nature for all action rolls (conflict-roll.mjs lines 49–53) (spec stages a Kill conflict with a character captain fighter=5 and a Goblin captain Nature=3, both scripting Attack on volley 0. Kill conflict's attack = skill:fighter per config.mjs L206 for characters — the configured path the monster branch MUST override. After reveal, the party captain's roll dialog pre-fills pool=5 and the roll message carries `flags.tb2e.roll = { type:"skill", key:"fighter", baseDice:5 }` with `directNatureTest:false` — the regression-guard side showing the character path is untouched. The monster's roll dialog then pre-fills pool=3 (Nature), NOT 0 (monsters have no `system.skills` block) — the `#onRollAction` branch at conflict-panel.mjs L1866-1869 forced `{ type:"ability", key:"nature" }` before calling `rollTest` L1974-1994, and the `_resolveRollData` monster short-circuit at tb2e-roll.mjs L45-48 resolved the pool from `actor.system.nature` (the flat scalar on monster.mjs L8, NOT the `{rating,pass,fail,max}` block character abilities use). The monster's message flags confirm `flags.tb2e.roll = { type:"ability", key:"nature", baseDice:3 }` with `flags.tb2e.directNatureTest:true` (tb2e-roll.mjs L1453 stamps true iff `type==="ability" && key==="nature"`). The conflict-roll.mjs L49-53 branch is the twin of L1866-1869 used by pre-render contexts (`buildResolutionContext`); since `#onRollAction` has no per-action gate the positive check on Attack is sufficient evidence for A/D/F/M uniformly per the briefing. HP damage is §18 L500 scope and is deliberately NOT asserted. Stable 3×3 on `--repeat-each=3`.)

---

## 17. Conflict: Maneuver MoS Spends

### Agent briefing

**Files:**
- `module/applications/conflict/maneuver-spend-dialog.mjs`
- Mailbox: `system.pendingManeuverSpend` on combatant
- Rule: SG p.69; MoS combos 1–4
  - 1 MoS: Impede (-1D opponent next action)
  - 2 MoS: Position (+2D team next action)
  - 3 MoS: Disarm (remove weapon/trait/gear)
  - 4 MoS: Rearm (equip dropped weapon) or Impede+Disarm

**Checkboxes:**

- [x] `tests/e2e/conflict/mos-impede.spec.mjs` — winning maneuver with 1 MoS; choose Impede; verify -1D applied on next volley (spec stages a Kill conflict — party captain health=2/fighter=3 MANEUVERS vs GM captain health=2/fighter=3 DEFENDS on volley 0, party DEFENDS vs GM ATTACKS on volley 1, all unarmed, PRNG 0.001 then 0.5 → 1 vs 0 successes, margin=1. Asserts the full impede pipeline: versus resolution card carries `flags.tb2e.maneuverSpend = { margin:1, combatId, combatantId:captainA, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:0 }` (versus.mjs L174-182); clicking `spend-maneuver` opens the dialog with exactly 1 combo (SPEND_COMBINATIONS[1] = impede only at maneuver-spend-dialog.mjs L12-14) — position/disarm/rearm radios all absent; selecting `impede` + submit writes the mailbox (maneuver-spend-dialog.mjs L265-271) which triggers GM-side `_onUpdateDescendantDocuments` (combat.mjs L455-456) → `#applyManeuverSpend` (combat.mjs L559-699) which stamps `round.effects.pendingImpede[gmGroupId] = { amount:1, targetVolley:1 }` (L619-624) and `round.effects.maneuverSpends[0].spent = true` (L665-671). After resolving V0 the impede entry is preserved (targetVolley=1 ≠ just-resolved volleyIndex=0 per `consumeResolvedManeuverEffects` combat.mjs L715-720). On V1 the GM captain's Attack roll dialog shows the Impede modifier row (localized from `TB2E.Conflict.Maneuver.Impede` = "Impede" at lang/en.json L559), summary pool = fighter(3) − unarmed(1) − impede(1) = 1D, and the posted roll message carries `flags.tb2e.roll.poolSize = 1` with a `{ type:"dice", value:-1, source:"conflict", label:/Impede/ }` entry in `flags.tb2e.roll.modifiers` (tb2e-roll.mjs L1431 serializes `allModifiers`; the impede was injected into testContext at conflict-panel.mjs L1881-1889 then flowed through `rollTest` contextModifiers at tb2e-roll.mjs L1255-1258 → pool math at L1316-1319). **Known quirk** (non-functional): `system.pendingManeuverSpend` is an `ObjectField` (combatant.mjs L23), and Foundry's update semantics for ObjectField merge rather than replace, so `combatant.update({ "system.pendingManeuverSpend": {} })` at combat.mjs L564 leaves the `{impede:true, position:false}` `selection` sub-object intact. Re-entry of the mailbox hook is idempotent (the `spent` gate at combat.mjs L588 short-circuits), so this is cosmetic — the spec therefore asserts the two definitive functional outcomes (pendingImpede stamped + source volley spent) rather than mailbox erasure. Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/mos-position.spec.mjs` — 2 MoS Position; verify +2D to whole team's next action (spec stages a Kill conflict — party captainA health=3/fighter=3 MANEUVERS vs GM captainB health=2/fighter=3 DEFENDS on volley 0, party ATTACKS via non-captain charB fighter=3 vs GM DEFENDS on volley 1, all unarmed, PRNG 0.001 then 0.5 → 2 vs 0 successes, margin=2. Asserts the position pipeline: versus resolution card carries `flags.tb2e.maneuverSpend = { margin:2, combatId, combatantId:captainA, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:0 }` (versus.mjs L174-182); clicking `spend-maneuver` opens the dialog with exactly 2 combos (SPEND_COMBINATIONS[2] at maneuver-spend-dialog.mjs L15-18 = impede+position) — disarm/rearm/impedePosition radios all absent; selecting `position` + submit writes the mailbox (maneuver-spend-dialog.mjs L265-271) which triggers GM-side `_onUpdateDescendantDocuments` (combat.mjs L455-456) → `#applyManeuverSpend` (combat.mjs L559-699) which stamps `round.effects.pendingPosition[partyGroupId] = { amount:2, targetVolley:1 }` keyed on the winner's OWN group (L627-633), leaves `pendingImpede[gmGroupId]` absent (selection.impede=false), and sets `round.effects.maneuverSpends[0].spent = true` (L665-671). After resolving V0 the position entry is preserved (targetVolley=1 ≠ just-resolved volleyIndex=0 per `consumeResolvedManeuverEffects` combat.mjs L715-720). On V1 the panel renders the party roll button wired to charB via `data-combatant-id` (panel-resolve.hbs L103-108) — asserted explicitly to prove team-wide Position keys on groupId, not combatantId. charB's Attack roll dialog shows the Position modifier row (localized from `TB2E.Conflict.Maneuver.Position`), summary pool = fighter(3) − unarmed(1) + position(2) = 4D, and the posted roll message carries `flags.tb2e.roll.poolSize = 4` with a `{ type:"dice", value:+2, source:"conflict", label:/Position/ }` entry in `flags.tb2e.roll.modifiers` (tb2e-roll.mjs L1431 serializes `allModifiers`; the position was injected into testContext at conflict-panel.mjs L1890-1898 then flowed through `rollTest` contextModifiers at tb2e-roll.mjs L1255-1258 → pool math at L1316-1319). Same mailbox-ObjectField quirk as mos-impede (cosmetic, idempotent via the `spent` gate at combat.mjs L588 — spec asserts functional outcomes rather than mailbox erasure). Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/mos-disarm-weapon.spec.mjs` — 3 MoS Disarm of opponent's weapon; verify weapon removed from that combatant's assignment (spec stages a Kill conflict — party captainA health=3/fighter=3 ARMED with weapon Item "E2E Party Blade" MANEUVERS vs GM captainB health=2/fighter=3 ARMED with weapon Item "E2E GM Blade" DEFENDS on volley 0. Both sides armed so there's no unarmed -1D penalty (conflict-panel.mjs L1944 gate) — party pool = health(3) = 3D; GM pool = health(2) = 2D. PRNG 0.001 then 0.5 → 3 vs 0 successes, margin=3. Asserts the disarm pipeline: versus resolution card carries `flags.tb2e.maneuverSpend = { margin:3, combatId, combatantId:captainA, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:0 }` (versus.mjs L174-182); clicking `spend-maneuver` opens the dialog with exactly 4 combos (SPEND_COMBINATIONS[3] at maneuver-spend-dialog.mjs L19-24 = impede+position+impedePosition+disarm) — rearm and impedeDisarm (both cost 4) absent. Selecting `disarm` reveals the disarm-target `<select>` (toggled by the radio-change listener at maneuver-spend-dialog.mjs L200-206); option values are `"<combatantId>|<itemId>"` (hbs L43), the spender (captainA) is excluded by the opponents-only filter at maneuver-spend-dialog.mjs L102, and the GM captain's weapon item appears as `${captainB}|${gmWeaponItemId}`. Submitting writes the mailbox with `selection = { impede:false, position:false, disarm:{ targetCombatantId, targetItemId } }` (maneuver-spend-dialog.mjs L250-255, L265-271) which triggers GM-side `_onUpdateDescendantDocuments` (combat.mjs L455-456) → `#applyManeuverSpend` (combat.mjs L559-699) which: (1) pushes the weapon id onto `target.system.disabledItemIds` (L643-647); (2) detects `target.system.weaponId === selection.disarm.targetItemId` and drops the weapon into `combat.system.droppedWeapons[opponentGroupId]` as `{ itemId, itemName, sourceCombatantId: targetId }` (L650-659) keyed on the victim's group; (3) marks `round.effects.maneuverSpends[0].spent = true` (L665-671); (4) unequips the target by calling `setWeapon(targetId, "", "")` post-update (L676-678 → L268-274) which clears both the combatant's `system.weapon`/`weaponId` AND the actor mirror `system.conflict.weapon`/`weaponId`. Since selection.impede and selection.position are both false for a disarm-only combo, `pendingImpede[gmGroupId]` and `pendingPosition[partyGroupId]` stay absent (L619-625/L627-633 branches skipped) — spec asserts both remain null. Same mailbox-ObjectField quirk as mos-impede (cosmetic; idempotent via the `spent` gate at combat.mjs L588 — spec asserts functional outcomes rather than mailbox erasure). Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/mos-disarm-trait.spec.mjs` — Disarm of a trait usage; verify trait flag cleared for remainder of conflict (spec stages a Kill conflict mirroring L480 — party captainA health=3/fighter=3 ARMED "E2E Party Blade" MANEUVERS vs GM captainB health=2/fighter=3 ARMED "E2E GM Blade" DEFENDS on V0, PRNG 0.001 then 0.5 → 3 vs 0 successes, margin=3. GM captainB ALSO owns a L1 trait Item (`system: { level: 1, beneficial: 1 }`) created via `addTraitToActor` — this is the disarm target. Asserts the trait-disarm pipeline: resolution card carries the MoS-3 spend payload (same shape as L480); spend dialog opens with 4 combos at MoS 3; selecting `disarm` reveals the target `<select>` whose options include `${captainB}|${gmTraitItemId}` with a `(trait)` kind suffix (maneuver-spend-dialog.mjs L138-140 enumerates `items.filter(i => i.type === "trait")` and joins into the `items` list at L144; template L43 renders `value="{{../combatantId}}|{{this.id}}"` and the `({{this.kind}})` suffix); captainA (the spender) is absent from options (opponents-only filter at L102). Submitting writes the mailbox with `selection.disarm = { targetCombatantId: captainB, targetItemId: gmTraitItemId }` (L250-255). GM hook `#applyManeuverSpend` (combat.mjs L559-699) takes the TRAIT branch: (1) L643-647 pushes the trait id onto `target.system.disabledItemIds` — same field, same code path as weapon disarm; (2) L650 branch `target.system.weaponId === selection.disarm.targetItemId` is FALSE (traitId !== weaponId), so the droppedWeapons write at L651-659 is SKIPPED and `disarmedTargetUnequip` stays null; (3) L665-671 marks `round.effects.maneuverSpends[0].spent = true`; (4) L676-678 `setWeapon(targetId, "", "")` is NOT called (disarmedTargetUnequip=null). Spec captures a baseline snapshot of the target's `weaponId`/`weapon`/actor mirror BEFORE disarm (= gmWeaponItemId/gmWeaponName) and asserts the post-spend poll returns the same values unchanged — proving the trait branch doesn't touch the equipped weapon. Also asserts `droppedWeapons[gmGroupId]` count stays 0 (no weapon dropped), the trait Item itself still exists on the actor (`actor.items.get(traitId)` truthy — only its id is disabled, not deleted), and `pendingImpede[gmGroupId]`/`pendingPosition[partyGroupId]` stay absent (disarm-only combo). Downstream "flag cleared for remainder of conflict" asserted by re-running the exact filter from `_buildTraitData` (tb2e-roll.mjs L338-340): `(actor.itemTypes.trait || []).filter(item => !disabled.has(item.id))` — post-filter set excludes the disarmed trait, proving every subsequent conflict roll dialog (conflict-panel.mjs L1938/1982 forwards `disabledItemIds` through `testContext`) will suppress it. Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/mos-rearm-dropped-weapon.spec.mjs` — 4 MoS Rearm; pick from `droppedWeapons`; verify weapon re-equipped (spec stages a Kill conflict — party captainA health=4/fighter=3 ARMED with a placeholder weapon Item "E2E Placeholder Blade" and ALSO owning a second weapon Item "E2E Dropped Blade" that is NOT equipped, vs GM captainB health=2/fighter=3 ARMED with "E2E GM Blade". Armed captainA maneuver pool = health(4) = 4D; armed captainB defend pool = health(2) = 2D (no unarmed -1D on either side per conflict-panel.mjs L1944 gate). PRNG 0.001 then 0.5 → 4 vs 0 successes, margin=4 — the exact MoS cost of the rearm combo. **Pre-seeds** `combat.system.droppedWeapons[partyGroupId] = [{ itemId:droppedWeaponItemId, itemName:droppedWeaponName, sourceCombatantId:cmb.captainA }]` matching the shape written by the disarm branch at combat.mjs L654-658 — this stands in for a prior disarm whose consumption is this spec's actual scope (disarm→drop is already covered at TEST_PLAN L480 mos-disarm-weapon). Asserts the rearm pipeline: versus resolution card carries `flags.tb2e.maneuverSpend = { margin:4, combatId, combatantId:captainA, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:0 }` (versus.mjs L174-182); clicking `spend-maneuver` opens the dialog with exactly 6 combos (SPEND_COMBINATIONS[4] at maneuver-spend-dialog.mjs L25-32 = impede+position+impedePosition+disarm+impedeDisarm+rearm — rearm and impedeDisarm both cost 4 and are ONLY available at MoS≥4). Selecting `rearm` reveals the rearm-target `<select>` (toggled by the radio-change listener at maneuver-spend-dialog.mjs L200-206 via the `data-combo-section="rearm"` gate); option values are `"<itemId>|dropped"` (hbs L66) for the dropped-pool optgroup and `"<itemId>|own"` (hbs L73) for the spender's own carried weapons (minus currently-equipped — maneuver-spend-dialog.mjs L154-156). The currently-equipped placeholder is excluded from options entirely (not in dropped pool, not in ownWeapons filter); the pre-seeded dropped weapon appears as `${droppedWeaponItemId}|dropped`. Submitting writes the mailbox with `selection = { impede:false, position:false, rearm:{ itemId:droppedWeaponItemId, fromDropped:true } }` (maneuver-spend-dialog.mjs L256-261, L265-271) which triggers GM-side `_onUpdateDescendantDocuments` (combat.mjs L455-456) → `#applyManeuverSpend` (combat.mjs L559-699) rearm branch (L679-696): (1) resolves the item via `spender.actor?.items?.get(itemId)` (L682); (2) calls `setWeapon(spender.id, name, itemId)` (L684 → L268-274) which writes BOTH the combatant `system.weapon`/`weaponId` AND the actor mirror `system.conflict.weapon`/`weaponId`; (3) since fromDropped===true, splices the chosen itemId out of `combat.system.droppedWeapons[partyGroupId]` (L685-693) — pool goes from 1 entry to 0 entries; (4) marks `round.effects.maneuverSpends[0].spent = true` (L665-671). Since selection.impede and selection.position are both false for a rearm-only combo, `pendingImpede[gmGroupId]` and `pendingPosition[partyGroupId]` stay absent (L619-625/L627-633 branches skipped) — spec asserts both remain null. Same mailbox-ObjectField quirk as mos-impede/mos-disarm (cosmetic; idempotent via the `spent` gate at combat.mjs L588 — spec asserts functional outcomes rather than mailbox erasure). Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/mos-impede-disarm-combo.spec.mjs` — 4 MoS Impede+Disarm combo (spec stages a Kill conflict — party captainA health=4/fighter=3 ARMED "E2E Party Blade" MANEUVERS vs GM captainB health=2/fighter=3 ARMED "E2E GM Blade" DEFENDS on V0, PRNG 0.001 then 0.5 → 4 vs 0 successes, margin=4 — the exact MoS cost of the impedeDisarm combo. Asserts the combo pipeline: versus resolution card carries `flags.tb2e.maneuverSpend = { margin:4, combatId, combatantId:captainA, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:0 }` (versus.mjs L174-182); clicking `spend-maneuver` opens the dialog with exactly 6 combos (SPEND_COMBINATIONS[4] at maneuver-spend-dialog.mjs L25-32 = impede+position+impedePosition+disarm+impedeDisarm+rearm). Selecting `impedeDisarm` (cost 4, `impede:true, disarm:true` at L30) triggers the radio-change listener (maneuver-spend-dialog.mjs L200-206) which unhides the `[data-combo-section="disarm"]` block via `applyVisibility` (L189-194) because `combo.disarm` is truthy; the `rearm` section stays hidden (combo.rearm is falsy — spec asserts `disarmSection` visible AND `rearmSection` hidden simultaneously). Disarm-target options include `${captainB}|${gmWeaponItemId}` and exclude captainA (opponents-only filter at L101-103). Submitting writes the mailbox with `selection = { impede:true, position:false, disarm:{ targetCombatantId, targetItemId } }` — note `position:false` is explicit in the combo's selection shape (maneuver-spend-dialog.mjs L246-255) even though impedeDisarm includes no position flag. GM hook `#applyManeuverSpend` (combat.mjs L559-699) has NO dedicated impedeDisarm branch — it simply runs the `selection.impede` branch (L619-624 stamps `pendingImpede[gmGroupId] = { amount:1, targetVolley:1 }`) AND the `selection.disarm` branch (L643-647 pushes `gmWeaponItemId` onto `target.system.disabledItemIds`, L650-659 drops the weapon into `combat.system.droppedWeapons[gmGroupId]` as `{ itemId, itemName, sourceCombatantId:captainB }`, L676-678 `setWeapon(targetId, "", "")` clears both combatant `system.weapon`/`weaponId` AND actor mirror `system.conflict.weapon`/`weaponId` — L268-274) in sequence. `selection.position` is falsy so L627-633 is skipped and `pendingPosition[partyGroupId]` stays absent — spec asserts both Impede stamping AND Position absence in the same poll to prove the combo is impede+disarm, not position. `maneuverSpends[0].spent = true` (L665-671). Same mailbox-ObjectField quirk as mos-impede/mos-disarm (cosmetic; idempotent via the `spent` gate at combat.mjs L588). Stable 3×3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/mos-carries-across-rounds.spec.mjs` — Impede/Position persist into next round per conflict-panel tracking (spec stages a Kill conflict — party captainA health=4/fighter=3 vs GM captainB health=2/fighter=3, all unarmed, with V0/V1 fast-forwarded by directly stamping `volleys[0..1].revealed=true`+stub results and bumping `system.currentAction=2` via `combat.update()`; this isolates the V2 spend path without rolling V0/V1. On V2 party MANEUVERS vs GM DEFENDS, PRNG 0.001 then 0.5 → 3 vs 0 successes, margin=3 — the exact cost of the `impedePosition` combo. Asserts the cross-round pipeline: versus resolution card carries `flags.tb2e.maneuverSpend = { margin:3, combatId, combatantId:captainA, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:2 }` (versus.mjs L174-182); clicking `spend-maneuver` opens the dialog with exactly 4 combos (SPEND_COMBINATIONS[3] at maneuver-spend-dialog.mjs L19-24 = impede+position+impedePosition+disarm) — rearm and impedeDisarm (both cost 4) absent. Selecting `impedePosition` (cost 3, `impede:true, position:true` at L22) and submitting writes the mailbox with `selection = { impede:true, position:true }` (maneuver-spend-dialog.mjs L265-271) which triggers GM-side `_onUpdateDescendantDocuments` (combat.mjs L455-456) → `#applyManeuverSpend` (combat.mjs L559-699). Because `volleyIndex===2`, `isBoundary=true` at L581 and `targetRound = roundNum+1 = 2` at L582; since round 2 doesn't exist yet (`rounds[2]` is undefined), `useCarry = isBoundary && !targetRoundData` is true at L602, so the code takes the CARRY branch (L603-613) instead of the in-round branch (L614-634). That carry branch stamps BOTH bags on the SOURCE round's effects: `round1.effects.carryImpede[gmGroupId] = 1` (L606-609) keyed on the opponent's group, and `round1.effects.carryPosition[partyGroupId] = 2` (L610-613) keyed on the winner's own group. Spec polls and asserts all 5 outcomes simultaneously: `carryImpede[gmGroupId]===1`, `carryPosition[partyGroupId]===2`, `pendingImpede[gmGroupId]===null`, `pendingPosition[partyGroupId]===null` (the in-round bags must stay empty — proves the useCarry branch was taken, not the in-round one), and `maneuverSpends[2].spent===true` (L665-671). Then drives the round transition by calling `combat.advanceRound()` directly (same entry point as the panel's "New Round" button — conflict-panel.mjs L2239-2248). `advanceRound` (combat.mjs L810-878) increments `currentRound` to 2, builds a fresh `rounds[2]` with all actions=`[null,null,null]`, all locked=false, 3 unrevealed volleys, and empty effects (L844-853). Then the carry-drain loops (L862-871) read the PREVIOUS round's `carryImpede`/`carryPosition` bags and stamp `rounds[2].effects.pendingImpede[gmGroupId] = { amount:1, targetVolley:0 }` (L864) and `rounds[2].effects.pendingPosition[partyGroupId] = { amount:2, targetVolley:0 }` (L869) — keyed to V0 because that's the new round's first action. Spec asserts the full round-2 state: `currentRound===2`, `volleys[0].revealed===false`, `volleys[0].result===null`, party/GM actions arrays all-null, and the two pending effects matching `{ amount, targetVolley:0 }`. This proves the complete SG p.69 boundary-spend flow — V2 maneuver margin CAN carry an Impede/Position penalty/bonus into the NEXT round's V0, even when the spend happens before the GM creates that round. Same mailbox-ObjectField quirk as mos-impede (cosmetic, idempotent via the `spent` gate at combat.mjs L588 — spec asserts functional outcomes rather than mailbox erasure). Stable 3×3 on `--repeat-each=3`.)

---

## 18. Conflict: HP & KO

### Agent briefing

**Files:**
- `module/documents/combat.mjs`, `combatant.mjs`
- `module/dice/help.mjs` — help blocked when KO'd (line 57)
- Mailbox: `flags.tb2e.pendingConflictHP`
- CLAUDE.md synthetic token rules — use `combatant.actor` always

**Checkboxes:**

- [ ] ~~skipped~~ `tests/e2e/conflict/hp-damage-reduces.spec.mjs` — losing action reduces HP by margin; `combatant.actor.system.conflict.hp.value` updated (spec written + `test.fixme` — this is the canonical HP-auto-write checkbox, and the production gap flagged by TEST_PLAN L453/L455/L456/L457 is still unfixed as of this writing pass. Re-confirmed: `resolveActionEffect` conflict-roll.mjs L155-178 returns `{type:"damage",amount:margin}` for attacks/feints and `{type:"restore",amount:margin}` for versus-winning defends but is imported only as dead code at conflict-panel.mjs L3 — no call site. `_executeVersusResolution` versus.mjs L137-267 posts the resolution card with `winnerId`/margin but does not mutate HP. `#onResolveAction` conflict-panel.mjs L2003-2096 writes `round.volleys[i].result` via `combat.resolveVolley` combat.mjs L772-782, reads current HP for the round-summary card at L2067-2077 (telling — it assumes HP already reflects volley outcome), but never mutates HP itself. The only HP write paths are manual GM roster input conflict-panel.mjs L341-360, the `pendingConflictHP` mailbox tb2e.mjs L193-204 (TEST_PLAN L501 scope), and initial disposition distribution combat.mjs L219-242. The spec stages a complete Kill conflict — party captain fighter=3/health=4 ATTACKS vs GM captain fighter=2/health=2 DEFENDS, both characters (NOT monsters — avoids the Nature override at conflict-roll.mjs L49-53), both unarmed with -1D weapons penalty via `__unarmed__` (conflict-panel.mjs L1944-1948). Disposition distributed via `distributeDisposition` to party-captain HP=4, GM-captain HP=4. PRNG stubs 0.001/0.5 → 2 vs 0 successes, margin=2. Asserts the full resolve pipeline runs (reveal → both sides roll versus with `isVersus=true` testContext per conflict-panel.mjs L1992 → both Finalize via `_handleFinalize` post-roll.mjs L506-522 → `_executeVersusResolution` posts resolution card with `winnerId=partyCaptainId`, margin=2) and then ASSERTS THE HP WRITE: `combatant.actor.system.conflict.hp.value` on the GM captain combatant drops from 4 to 2 (4 − margin), and the party captain's HP stays at 4 (no cascade per SG p.69 / DH p.123 — attack vs defend damage is one-directional). HP is read via `combatant.actor` not `game.actors.get(combatant.actorId)` per CLAUDE.md §Unlinked Actors (rule holds uniformly even for linked character combatants). Narrower than TEST_PLAN L453: that spec asserts pipeline mechanics (reveal card, roll-card metadata, winnerId, auto-advance) AND duplicates the HP assertion under the same fixme; this spec (L500) owns the HP assertion exclusively as the canonical place to flip to green. Fix shape: natural hook is end of `_executeVersusResolution` — both paired roll messages carry conflict testContext (isConflict, conflictAction, combatId, combatantId, groupId, opponentGroupId per conflict-panel.mjs L1983-1993). Walk combatants on the loser-group (attack/feint damage) or winner-group (defend-restore, SG p.69), call `resolveActionEffect` with winner's action + margin, and apply via the `pendingConflictHP` mailbox (CLAUDE.md §Mailbox Pattern) so non-GM writers work — alternately wire in `#onResolveAction` by reading the paired resolution-card flag. When that lands, drop `test.fixme` here AND at TEST_PLAN L453 and flip both to `- [x]`. Stable 3×3 on `--repeat-each=3` (all 3 iterations skip cleanly via fixme; no flake).
- [x] `tests/e2e/conflict/hp-player-mailbox.spec.mjs` — player-side HP change goes via `pendingConflictHP`; GM hook processes (spec written + green 6/6 on `--repeat-each=3`). Mailbox payload `flags.tb2e.pendingConflictHP = { newValue, targetActorId? }` per tb2e.mjs L193-204. Two sub-tests cover the two branches: (1) self-write — actor writes `{ newValue }` on itself; hook resolves `targetActor = actor` (L196 default), clamps to `[0, targetActor.system.conflict.hp.max]` (L198-199), writes `system.conflict.hp.value` (L200), then `unsetFlag("tb2e", "pendingConflictHP")` on the writer (L201). (2) targetActorId route — party-captain-editing-another-player path explicitly called out at tb2e.mjs L195; captain writes `{ newValue, targetActorId: allyId }` on herself, hook resolves `targetActor = game.actors.get(targetActorId)` (L196), applies to ally, clears flag on writer (NOT on target — L201 uses the `actor` hook arg). HP field declared at character.mjs L161-169 (`system.conflict.hp.{value,max}`, both initial 0); test seeds `max=4`/`max=3` to avoid the clamp-to-zero silent case. All Playwright sessions auth as GM (tests/e2e/test.mjs L14-20 via auth.setup.mjs); non-GM write branch is simulated via `page.evaluate` + `actor.update({"flags.tb2e.pendingConflictHP": ...})` — matches grind/apply-condition-mailbox.spec.mjs L179-185 idiom (bundled update, not setFlag). HP read via `combatant.actor` uniformity rule (CLAUDE.md §Unlinked Actors) — linked character combatants only here; synthetic-token parity is L505's scope. All three mailbox effects polled (`expect.poll`) to absorb the async chain update→hook→target-update→unsetFlag. Out of scope (owned elsewhere): auto-apply damage from resolve pipeline L500 (still fixmed), KO-at-zero L502, swap/help/synthetic parity L503-L505.
- [ ] ~~skipped~~ `tests/e2e/conflict/hp-ko-at-zero.spec.mjs` — HP hitting 0 marks combatant `knockedOut` (spec written + `test.fixme` — production gap. The `knockedOut` field exists as `BooleanField({ initial: false })` at module/data/combat/combatant.mjs L7 and is READ widely: conflict-panel.mjs L426/L915/L978/L1032/L1099/L1214/L1219/L1423/L1449/L1741 (scripting/arming/swap/captain/roster gates), conflict-tracker.mjs L149 (tracker body), tb2e.mjs L211 (pendingCaptainReassign refuses KO'd), templates panel-roster.hbs L4, panel-weapons.hbs L9/L13, panel-script.hbs L47-48, tracker-body.hbs L25 (disabled states). BUT grep for any WRITER of `system.knockedOut` (patterns `knockedOut\s*=` / `knockedOut:\s*true` / `"system\.knockedOut"`) returns zero matches against `module/`. The `pendingConflictHP` mailbox (tb2e.mjs L193-204) — the only auto-wired HP writer today — writes `system.conflict.hp.value` via `actor.update()` and clears the flag, but never touches `combatant.system.knockedOut`; no `updateActor`/`updateCombatant` hook observes the HP→0 transition and flips it. The one place that couples HP<=0 and knockedOut is the DISPLAY derivation at conflict-panel.mjs L1099 (`knockedOut: c.system.knockedOut || (hp.max > 0 && hp.value <= 0)`) — OR'd with a derived predicate so consumers paper over the missing flip locally. The same HP<=0 predicate (with NO knockedOut coupling) is the sole gate for help-blocking at module/dice/help.mjs L57; tests/e2e/roll/help-blocked-when-ko.spec.mjs (§3) directly sets `hp = { max, value: 0 }` and never touches `system.knockedOut` — further evidence it's read-only dead weight today. Spec fixture (minimal — uses the L501 mailbox idiom, NOT the full resolve pipeline which is L500's fixme): seeds character with `conflict.hp = { value: 1, max: 1 }`, creates a conflict Combat with that actor as combatant (combatant starts `knockedOut === false` per schema default L7), writes `flags.tb2e.pendingConflictHP = { newValue: 0 }` on the actor → GM hook applies synchronously (we are GM), HP lands at 0. Then asserts `combatant.system.knockedOut === true` (**the fixmed assertion**). Reverse path: writes `{ newValue: 1 }` → asserts knockedOut clears. Narrow scope: only the HP→KO flip assertion. Out of scope: swap (L503), help-blocked (L504 duplicate of §3), synthetic parity (L505), upstream auto-damage (L500). Fix shape: inside the GM hook tb2e.mjs L193-204 after `targetActor.update(...)` resolves, find combatant(s) whose `actorId === targetActor.id` in any active isConflict combat and set `system.knockedOut = true` when `newVal === 0 && max > 0` (matching help.mjs L57 predicate), clear it when `newVal > 0`. GM-owned combatant update so direct `combatant.update()` works (no secondary mailbox needed). When the L500 resolve-auto-damage fix also lands via the mailbox, this gap closes for free. Stable 3/3 skipped on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/hp-ko-swap-mid-volley.spec.mjs` — panel-resolve "swap" replaces KO'd combatant; verify `swapActionCombatant` call (spec stages a Kill conflict — party captain + alt characters, GM captain (Bugbear) + mook (Goblin) monsters, all unarmed with `__unarmed__`, flat-disposition distributed party={captain:4, alt:3}/gm={monA:4, monB:4} via `distributeDisposition`. Party scripted attack/defend/feint = captain/alt/captain; GM scripted maneuver/attack/defend = monA/monB/monA. Both sides locked via `combat.lockActions`, then `combat.beginResolve()` flips `phase="resolve"` + `currentAction=0` so the resolve tab's pre-reveal swap DOM (panel-resolve.hbs L36-53) is eligible for V0. Precondition: with everyone at full HP, `needsSwap` is false for every side (conflict-panel.mjs L1214) — spec asserts `.resolve-ko-warning`/`.resolve-swap-select` have count 0 on both party AND GM side of V0, and that the captain actor carries no `pendingConflictHP` flag. **Act**: simulates the player-side HP write via `captain.update({"flags.tb2e.pendingConflictHP": { newValue: 0 }})` — a single bundled `update()` so the `changes` diff visible to the GM `updateActor` hook at tb2e.mjs L193 contains the mailbox key (idiomatic per hp-player-mailbox.spec.mjs L162 / grind/apply-condition-mailbox.spec.mjs L179-185, NOT setFlag). GM hook drain path (tb2e.mjs L193-204, shared with L501's scope) resolves `targetActor = actor` (no targetActorId → defaults to writer per L196), clamps newValue into `[0, max=4]` at L198-199, writes `system.conflict.hp.value = 0` via `targetActor.update()` at L200, then `.then()`-chains `actor.unsetFlag("tb2e", "pendingConflictHP")` at L201. Spec polls both effects independently: HP lands at 0 AND mailbox flag clears on the writer. **Then asserts the paper-over `needsSwap` flip**: conflict-panel.mjs L1213-1214 reads `actorHp = combatant.actor.system.conflict.hp.value ?? 1` and computes `needsSwap = isCurrent && !volley.result && (actorHp <= 0 || combatant?.system.knockedOut)` — because the predicate is OR'd and `actorHp === 0` satisfies the first disjunct, this spec goes GREEN without depending on the L502 production gap (`combatant.system.knockedOut` has zero writers in `module/` — spec does NOT assert on that field). `.resolve-pre-swap` + KO warning + swap select surface on the party side of V0; GM side stays count-0 (monster HP untouched). `swapCandidates` population (L1215-1222) asserted by reading all `<option>` values from the party swap select — returns `['', cmb.alt]` exactly: leading empty placeholder (panel-resolve.hbs L44), `alt` included (same-side L1217, not self L1219, not KO'd L1219, hp.value > 0 L1220), captain self-excluded by the `c.id !== entry.combatantId` clause at L1219, monsters excluded by the `c._source.group === group.id` filter at L1217. Lock invariant (`round.locked[partyGroupId]===true && [gmGroupId]===true`) captured through the HP mutation — `pendingConflictHP` only touches the Actor, never the Combat doc. **Act 2 — DOM swap**: `panel.selectResolveSwap(0, partyGroupId, cmb.alt)` fires a `change` event on `.resolve-swap-select[data-group-id=partyGroupId]`; the listener at conflict-panel.mjs L363-373 reads `combat.system.currentAction=0` (L367), the group id (L368), and the selected value (L369) and calls `combat.swapActionCombatant(0, partyGroupId, cmb.alt)` (L371). That method (combat.mjs L413-424, isGM-gated at L414) deep-clones `system.rounds`, mutates `round.actions[partyGroupId][0].combatantId = cmb.alt` in place (L422), and persists via `this.update({"system.rounds": rounds})` (L423). Verifying the `swapActionCombatant` CALL: no spy needed — combat.mjs L503 `#applyActions` gates writes to locked rounds, so `setActions` is a no-op here; no other path in `module/` mutates `actions[groupId][actionIndex].combatantId` on a locked round. Observing the `combatantId` flip via `expect.poll` (POM helper in ConflictPanel.mjs L922-934) constitutes proof of the call. Post-swap assertions: full party-actions deep-clone equals `[{attack, alt}, {defend, alt}, {feint, captain}]` — only V0 changed (`actionIndex` scoping at combat.mjs L420-422), V1 and V2 completely untouched; GM-side actions full deep-clone equals the original `gmActions` array unchanged (the swap is scoped to a single `(groupId, actionIndex)` pair — combat.mjs L420); `round.locked` still `{p:true, g:true}` on both sides — `swapActionCombatant` only writes `system.rounds.<roundNum>.actions`, preserving the sibling `locked` map. **Retraction**: the swap UI disappears post-swap — V0 now points at alt whose hp.value=3 > 0, so `needsSwap` re-evaluates false on the next render (actor resolved via `combatant.actor` at L1196/L1213 now reads alt's actor). Spec asserts `.resolve-ko-warning` + party `.resolve-swap-select` both return to count 0. Mailbox flag remains cleared (swap doesn't touch it). Captain HP stays at 0 — the swap is a combatant re-route, NOT an HP restore; no code path in `module/` restores HP on the KO'd combatant from a swap. Uniformly reads HP via `combatant.actor` / `game.actors.get()` (linked character combatants — synthetic-token parity is L505's scope). Scope distinction vs TEST_PLAN L431 (`script-independent-ko-sub.spec.mjs`, §15): L431 drives KO by direct `actor.update({"system.conflict.hp.value":0})` BEFORE the resolve tab reads `needsSwap`; L503 (this spec) drives KO MID-VOLLEY through the `pendingConflictHP` mailbox — the canonical non-GM edit path per CLAUDE.md §Mailbox Pattern — exercising both the mailbox drain chain and the resolve panel's reaction to a mid-volley HP change. Stable 3/3 on `--repeat-each=3`.)
- [x] `tests/e2e/conflict/hp-help-blocked-when-ko.spec.mjs` — KO'd combatant cannot help on rolls (already covered in §3 but duplicate here for conflict-specific surface). Spec stages a full Kill conflict — party captain (fighter=3, roller) + alt (fighter=2, would-be helper) + GM captain monster (Bugbear) + mook (Goblin), all unarmed via `__unarmed__`, flat-dispo `{captain:4, alt:3}` / `{monA:4, monB:4}` via `distributeDisposition`. Scripting locks party V0=attack/captain so the party roll button on V0 is the captain's fighter roll. Both sides locked, resolve phase entered. Then drives alt's HP to 0 via the `flags.tb2e.pendingConflictHP = { newValue: 0 }` mailbox (tb2e.mjs L193-204 — shared idiom with L501/L503), polls for hp.value=0 and flag-clear. No PRNG stub — the assertion fires on dialog contents pre-submission (cancelled), same discipline as §3 L160. **Surface distinction vs §3 L160 (critical — justifies the duplicate checkbox)**: both specs exercise the SAME predicate `isBlockedFromHelping` (help.mjs L57: `conflictHP?.max > 0 && conflictHP.value <= 0`), but through DIFFERENT entry paths into `getEligibleHelpers` (help.mjs L76+). §3 L160 drives a standalone Will ability test — no `candidates` override, so `getEligibleHelpers` walks `canvas.scene.tokens` at L91-101 and filters by conflict team. §18 L504 (this spec) drives a CONFLICT action roll — `#onRollAction` at conflict-panel.mjs L1970-1980 builds `memberCombatants` (same-group Combatants, roller excluded by `c.id !== combatantId` at L1972) and passes them as `testContext.candidates`, which shortcuts the scene-token walk at help.mjs L84-85 (`if (candidates) pool = candidates`). The Combatant objects are normalized to actors via `raw.actor ?? raw` at L121 before reaching `isBlockedFromHelping` at L129. So §3 exercises the scene-token pool + team filter; §18 L504 exercises the conflict-group Combatant `candidates` override. A future refactor that moved the KO check to the scene-token filter (or to `_findBestHelpPath`) would leave §3 green but fail this spec — catching the regression on the conflict surface specifically. Per the agent briefing's outcome taxonomy, this is **outcome (A)** — conflict rolls reuse the same `rollTest` + `_showRollDialog` template, but the `candidates` array building path is wholly distinct from the scene-token walk and warrants its own coverage. **Concrete assertions**: (1) pre-KO control — direct `getEligibleHelpers` call mirroring conflict-panel.mjs L1970-1972 returns exactly `[{id: altId, helpVia: 'fighter'}]` (alt qualifies via the same-skill path at help.mjs L281-289 with fighter=2>0); (2) mailbox-drain effects poll green (alt hp.value→0, flag cleared); (3) post-KO control — `isBlockedFromHelping(alt.actor)` returns `{blocked: true, reason: 'TB2E.Help.BlockedConflictKO'}` (L57's conflict-KO branch, not the earlier dead/afraid branches at L54-55 — alt was created with `conditions.{afraid,dead} = false` specifically to isolate the KO gate); (4) post-KO `getEligibleHelpers` returns `[]`; (5) reveal V0 → click party `rollAction` button → RollDialog opens → `.helper-toggle[data-helper-id=altId]` has count 0 AND `.roll-dialog-helpers` section has count 0 (both — first proves alt's toggle isn't rendered, second proves alt was filtered OUT of `availableHelpers`, not just visually hidden: `hasHelpers` false at tb2e-roll.mjs L418 → `{{#if hasHelpers}}` guard at roll-dialog.hbs suppresses the section entirely); (6) dialog summary pool = 2 (fighter=3 minus unarmed -1D at conflict-panel.mjs L1944-1948 — confirms the conflict-modifier pipeline still ran and no help landed). Dialog cancelled for teardown — no roll submitted. **Green without depending on L502**: the help predicate gates on HP only (`conflictHP.value <= 0 && max > 0`), NOT on `combatant.system.knockedOut` — so the L502 production gap (`knockedOut` has zero writers in `module/`) is orthogonal and this spec lands green without that fix. All Playwright sessions auth as GM (auth.setup.mjs); mailbox drain + reveal + rollAction handlers are GM-gated and fire synchronously in-session. HP read via `combatant.actor` uniformity rule (CLAUDE.md §Unlinked Actors — linked characters here; synthetic-token parity is L505). Stable 3/3 on `--repeat-each=3`.
- [x] `tests/e2e/conflict/hp-synthetic-token-parity.spec.mjs` — unlinked monster's HP is written to synthetic actor, not world actor; verify world actor unchanged (regression test for CLAUDE.md gotcha) (spec written — 2 scenarios, 1 green + 1 `test.fixme` on an empirically confirmed production gap. **Scenario 1 (synthetic self-write — GREEN 3/3)**: imports Bugbear world actor, activates a fresh tagged scene, creates a TokenDocument via `actor.getTokenDocument(...).toObject()` with `actorLink=false` force-set (packs/_source/monsters/Bugbear_a1b2c3d4e5f60005.yml L76-77 already declares `prototypeToken.actorLink: false` — we override explicitly too so the spec doesn't silently drift if pack defaults change), wires a Combatant carrying `{actorId, tokenId, sceneId}` matching conflict-panel.mjs L2148-2156/L2189-2197 (the real panel add path) — the tokenId+sceneId pair is what causes Foundry's `combatant.actor` getter to resolve to `token.actor` (synthetic) rather than `game.actors.get(actorId)` (world). Identity invariants asserted up front: `combatant.actor !== game.actors.get(monsterId)` but `.id` matches (same template), synthetic.uuid contains `Scene.<sId>.Token.<tId>` while world.uuid is bare `Actor.<mId>` — this nails down the "same template, different runtime instance" CLAUDE.md describes. Seeds synthetic to 3/3 via `combatant.actor.update(...)` (world stays at schema default 0/0 per monster.mjs L48-49, explicitly asserted), writes `flags.tb2e.pendingConflictHP = { newValue: 1 }` on the synthetic. Polls (a) synthetic `hp.value → 1`, (b) mailbox flag cleared on synthetic, (c) **world hp.value stays at 0** — the cardinal regression assertion. If a future refactor swapped the hook to `game.actors.get(actor.id)` for `targetActor`, this assertion fails. **Scenario 2 (captain cross-write via targetActorId — `test.fixme` with `{annotation: {type: "issue"}}`)**: empirically verified live production gap. Captain (linked character, 4/4) writes `{newValue:1, targetActorId: monsterWorldId}` on herself. tb2e.mjs L196 resolves `targetActor = game.actors.get(pendingHP.targetActorId)` → returns the WORLD actor, not the synthetic. Diagnostic run during authoring: with world seeded max=5/value=5 and synthetic 3/3, the cross-write drops WORLD 5→1 while SYNTHETIC stays at 3. Spec seeds world to 5/5 deliberately — with the default `max=0` the hook clamp at tb2e.mjs L198-199 would pin `newValue=1` to 0 and the buggy world-write would be invisible (0→0). fixme asserts the DESIRED behavior (synthetic lands at 1, world untouched at 5) so flipping fixme→test after a fix is a one-line edit. Fix shape (per annotation): walk `game.combats` for a combatant whose `actorId === targetActorId` AND carries a `tokenId`; prefer its `combatant.actor` (synthetic). Fall back to `game.actors.get(targetActorId)` only when no synthetic match exists. This closes the gotcha without breaking L501's linked-character targetActorId path. Scope distinction vs L501 (hp-player-mailbox): L501 exercises mailbox routing for LINKED characters (self-write + targetActorId); L505 (this spec) owns the UNLINKED-monster synthetic-token surface exclusively — catches regressions where someone "normalizes" the hook to route everything through `game.actors.get`. HP read via `combatant.actor` uniformity rule per CLAUDE.md §Unlinked Actors — world actor read via `game.actors.get(monsterId)` is explicitly permitted here because asserting on world-template state IS the point of the regression test. Cleanup removes the scene (cascade-deletes TokenDocuments), world actors, and any orphan combats via tag. Stable 3/3 on `--repeat-each=3` — 3 greens + 3 fixme-skips.)

---

## 19. Conflict: Team, Helping, Artha

### Agent briefing

**Files:**
- `module/dice/conflict-roll.mjs` — synergy, helpers
- Actions can be helped by teammates not scripted this volley
- Fate / Persona available in conflict roll dialogs (`showPersona` flag in tb2e-roll.mjs lines 395–397)

**Checkboxes:**

- [x] `tests/e2e/conflict/team-synergy.spec.mjs` — teammate synergy on resolve adds +1D to the rolling teammate's action (spec written + green 6/6 on `--repeat-each=3`, two sibling scenarios: GM-direct + mailbox). **Rules source**: SG p.87 ("Synergy", under Fate) — "When helping another player on a test, the helper may spend one fate point before the dice are cast to learn from the experience. [...] If the player rolling the dice passes the test, the helper marks a passed test for advancement for the skill or ability with which they helped, not necessarily the one being tested. [...] All help must obey the standard helping rules." (../reference/rules/fate-persona/scholars-guide-fate-persona.md L141-148). **Clarifying the checkbox's "+1D" phrasing**: per SG p.87's "All help must obey the standard helping rules" clause, synergy is coupled to helping — a helper offering synergy is ALSO contributing their +1D help die (DH p.63). Production UI enforces the coupling: clicking `.helper-synergy-btn` in the roll dialog auto-engages the sibling `.helper-toggle` (tb2e-roll.mjs L695-705) and adds `.synergy-active` to the row (L707), so the synergy path always produces BOTH the +1D help contribution AND the advancement logging; the spec asserts BOTH halves. **Surface**: Kill conflict (config.mjs L202-211 — attack rolls skill:fighter), party captainA (fighter=3, fate=0, the ROLLER) + altHelper (fighter=2, fate=3, the synergy donor) + GM captain Bugbear + mook Goblin, all unarmed via `__unarmed__` (conflict-panel.mjs L1944-1948 bakes a -1D "Fists" dice modifier). Party V0=attack/captainA, GM V0=attack/monA — **attack:attack is INDEPENDENT** (config.mjs L408), so both sides roll vs Ob 0 with no versus-resolution card; lands in `_handleIndependentRoll` (tb2e-roll.mjs L1487-1540) which is the clean synergy surface (versus chains through `_handleVersusRoll` L1566+ would require `_handleVersusFinalize` + pairing — out of scope here). Alt scripted on V1/V2 as filler so `lockActions` gate opens — that scripted role on OTHER volleys does NOT prevent them from helping V0 per `memberCombatants` filter at conflict-panel.mjs L1970-1972 (group + roller-exclusion only; no scripting filter). Disposition flat (captainA=4, alt=3, monA=4, monB=4) so nobody starts KO'd (help.mjs L57 gate needs `hp.value > 0` when `max > 0`). PRNG stub `CONFIG.Dice.randomUniform = () => 0.001` → all-6s; with pool = fighter(3) − 1 unarmed + 1 help = 3D → 3 successes vs Ob 0 → **pass**, which exercises the `_logAdvancement` pass branch (tb2e-roll.mjs L192-204 → `skills.fighter.pass` bumps by 1). **Scenario A (GM-direct synergy)**: dialog baseline pool = 2 (fighter 3 − 1 unarmed); click `dialog.toggleHelperSynergy(altId)` (new POM method in RollDialog.mjs) — tb2e-roll.mjs L690-708 auto-engages the helper toggle (.active + helperBonus bump at L703) AND adds `.synergy-active` to the row (L707); spec asserts both post-conditions plus summary pool bumps 2→3. Submit → `_handleIndependentRoll` posts roll-result card with `flags.tb2e.helpers[0] = { id: altId, helpVia: 'fighter', helpViaType: 'skill', synergy: true, name }` (via `mapHelpersForFlags` at roll-utils.mjs L164-168) and `flags.tb2e.roll = { pass: true, successes: 3, finalSuccesses: 3, obstacle: 0, poolSize: 3 }`; chat card renders `.roll-card-helpers` block with `button[data-action="synergy"][data-helper-id=altId]` (roll-result.hbs L141-151, emitted because `_buildSynergyHelpers` at tb2e-roll.mjs L1549-1560 filters `h.synergy && !helperSynergy[h.id] && game.actors.has(h.id)`). Alt baseline asserted pre-click: `fate.current=3, fate.spent=0, fighter.pass=0, fighter.fail=0, pendingSynergy flag absent`. Click synergy button via native `el.click()` (same pattern as `clickFinalize`/`clickFateLuck` — handler wired in `activatePostRollListeners` post-roll.mjs L33-56). `_handleSynergy` (L376-402) takes GM branch at L397-398 (we auth as GM per auth.setup.mjs) → `_processSynergy` (L410-457): deducts 1 fate from alt (L429-432), calls `_logAdvancement` (tb2e-roll.mjs L192-204) with `type='skill', key='fighter', pass=true` → alt's `skills.fighter.pass` goes 0→1 (below the advancement cap so the increment lands at L202), marks `flags.tb2e.helperSynergy.<altId> = true` on the message (L444-446), re-renders card via `_reRenderChatCard` (post-roll.mjs L857-906). Polled end state: `fate.current=2, fate.spent=1, fighter.pass=1`, message's `helperSynergy` map has `altId: true`, synergy button count=0 on re-render (filtered out by `!helperSynergy[h.id]` at L1551), `pendingSynergy` flag on alt is null (GM direct path never writes the mailbox — the key behavior that distinguishes A from B). **Scenario B (mailbox-path synergy)**: re-stages a fresh identical conflict (independent test; sibling describe-level `afterEach` clears chat + combats between scenarios so no cross-contamination). Same dialog + submit produces the same roll-result card. Instead of clicking the chat-card synergy button, writes `actor.update({"flags.tb2e.pendingSynergy": { messageId }})` on alt — the bundled-update idiom matching L501/L503/L504 for `pendingConflictHP`, which ensures the GM hook at tb2e.mjs L185-188 sees the key in the `changes` diff (setFlag would also work per CLAUDE.md §Mailbox Pattern, but the diff-shape path is what the production non-GM branch at post-roll.mjs L400 triggers via setFlag internally). Hook fires `processSynergyMailbox` (post-roll.mjs L466-471) which runs `_processSynergy` then `actor.unsetFlag("tb2e", "pendingSynergy")` at L470. Three polled effects asserted: (a) flag drained (`pendingSynergy` → null), (b) fate spent + advancement logged (same `{fate.current:2, fate.spent:1, fighter.pass:1}` end state as Scenario A), (c) message's `helperSynergy` map has `altId: true`. Both scenarios converge on `_processSynergy` — A proves the GM-direct branch (L397-398), B proves the mailbox branch (L399-401) which is the CLAUDE.md §Mailbox Pattern surface: both yield identical end state. **Why this exists as two scenarios inside one describe**: the rules requirement (SG p.87 "synergy = fate spend + advancement logged") has ONE semantic behavior, but production has TWO code paths depending on user role (GM-direct vs mailbox-drain) — collapsing to one scenario would miss one of them. Harness is always GM (auth.setup.mjs), so B exercises the mailbox branch by writing the flag directly rather than through a non-GM session; this matches the L501/L503/L504 precedent for mailbox specs. **Explicit non-scope**: L521 (unscripted-teammate helping — alt IS scripted on V1 here but helping doesn't depend on scripting scope; L521 owns the "teammate scripted on a DIFFERENT volley can still help V0" regression surface explicitly), L522 (Fate: Luck — reroll-6s post-roll, a different fate spend with its own card surface), L523 (Persona in conflict — a different artha spend), versus-mode conflict rolls (attack vs defend — versus chain through `_handleVersusRoll` is a different code path; independent attack:attack isolates synergy's surface). **POM changes**: (1) `RollDialog.mjs`: added `helperRow(id)` + `helperSynergyButton(id)` locators + `toggleHelperSynergy(id)` action (expands collapsed helpers section, clicks the star-icon button, asserts both `.synergy-active` on row and `.active` on sibling toggle). (2) `RollChatCard.mjs`: added `synergySection` locator + `synergyButton(id)` + `clickSynergy(id)` (latter unused by this spec — it scopes to the default `this.root.last()` which doesn't match when multiple messages are live; this spec overrides `card.root` to the specific messageId and uses a local locator). Stable 3/3 on `--repeat-each=3` — 6/6 passing.
- [x] `tests/e2e/conflict/team-helper-from-unscripted.spec.mjs` — unscripted teammate can help a scripted action (spec written + green 3/3 on `--repeat-each=3`). **Rules source**: SG p.71 "Helping Your Team" — "One can help even if one hasn't been assigned an action." (../reference/rules/helping-conflict/scholars-guide-helping-conflict.md L34-41, specifically L39). Also SG p.71 Team Actions bullet "If a team has more than three characters, three of them act this round. The others can help." — the staging models exactly that: captainA on all 3 volley slots, alt with NO action slot, yet alt is a legal helper. **Surface**: Kill conflict (config.mjs L202-211 — attack rolls skill:fighter), party captainA (fighter=3, the ROLLER) + altHelper (fighter=2, the UNSCRIPTED helper) + GM captain Bugbear + mook Goblin, all unarmed via `__unarmed__` (conflict-panel.mjs L1944-1948 bakes -1D "Fists"). **Scripting shape (the core L521 distinction)**: `partyActions = [{attack,captain}, {defend,captain}, {feint,captain}]` — captainA on ALL three slots, alt's combatantId appears NOWHERE in `round.actions[partyGroupId]`. `#applyLockActions` (combat.mjs L534) validates only `length===3 && every(a.action && a.combatantId)` with NO uniqueness constraint, so captain-x3 locks cleanly. GM V0=attack/monA → **attack:attack INDEPENDENT** (config.mjs L408) — both sides roll vs Ob 0, no versus-resolution card, lands in `_handleIndependentRoll` (tb2e-roll.mjs L1487-1540). Flat disposition (captainA=4, alt=3, monA=4, monB=4) so nobody starts KO'd (help.mjs L57 gate green). PRNG stub `CONFIG.Dice.randomUniform = () => 0.001` → all-6s; pool = fighter(3) − 1 unarmed + 1 help = 3D → 3 successes vs Ob 0 → pass. **Filter path under test**: `#onRollAction` at conflict-panel.mjs L1970-1972 builds `memberCombatants = combat.combatants.filter(c => c._source.group === groupId).filter(c => c.id !== combatantId && c.actor)` — group + roller-exclusion only, NO scripting filter. That list passes to `rollTest` as `testContext.candidates` (L1980), shortcuts the scene-token walk at help.mjs L84-85. For alt (fighter=2), `_findSkillHelpPath` same-skill branch at L281-289 fires → helper record emitted. The fixture control call (direct `getEligibleHelpers` mirroring the production `memberCombatants` build) returns exactly `[{id: altId, helpVia: 'fighter', helpViaType: 'skill'}]` — if a future refactor added a `c.system.actions[volleyIndex]?.combatantId === c.id` filter (to enforce "only scripted teammates can help"), this control fails first. **Concrete assertions**: (1) alt's V0/V1/V2 action slots control — `round.actions[partyGroupId]` deep-equals `[captain, captain, captain]` and does NOT contain alt.combatantId; proves the staging. (2) Pre-roll `getEligibleHelpers` returns alt exactly once with the same-skill helpVia. (3) Dialog: `.helper-toggle[data-helper-id=altId]` rendered with count 1 in NON-active state (`.not.toHaveClass(/active/)`) — the unscripted teammate's toggle exists in the conflict roll dialog. (4) Dialog baseline pool = 2 (fighter 3 − 1 unarmed). (5) `dialog.toggleHelper(altId)` engages the toggle; summary pool bumps 2→3 via `helperBonus` increment (tb2e-roll.mjs L683). (6) Post-submit roll-result card: `flags.tb2e.testContext = {isConflict:true, conflictAction:'attack', groupId:partyGroupId}`; `flags.tb2e.roll = {pass:true, successes:3, finalSuccesses:3, obstacle:0, poolSize:3}`; **the L521 core contract** — `flags.tb2e.helpers = [{id:altId, name:altName, helpVia:'fighter', helpViaType:'skill', synergy:false}]` (via `mapHelpersForFlags` at roll-utils.mjs L164-168; `synergy:false` since we toggled help only, NOT synergy). (7) `.roll-card-helpers` section has count 0 — `_buildSynergyHelpers` at tb2e-roll.mjs L1549-1551 filters `h.synergy`, so alt (synergy=false) is excluded → section not rendered. Distinguishes L521's "help-only" surface from L520's synergy surface. (8) Alt's script state unchanged post-roll: `round.actions[partyGroupId]` still `[captain, captain, captain]` — helping does NOT consume an action slot per SG p.71 L39. (9) Alt's skills.fighter.pass/fail both stay 0 — helpers don't log advancement without synergy (only SG p.87 synergy path bumps `.pass`/`.fail`, and we didn't use it — L520's scope). **Scope distinctions**: vs L520 team-synergy — that spec's alt IS scripted on V1/V2 and helps via the synergy path (coupled help+synergy); this spec's alt is scripted on NO volley and helps WITHOUT synergy. vs L160 help-blocked-when-ko — standalone skill test scene-token pool + KO gate; this spec exercises the conflict `candidates`-override path without the KO gate. vs L504 hp-help-blocked-when-ko — same `candidates` override path but tests the KO-filter branch (help.mjs L57); this spec tests the "unscripted-teammate-qualifies" branch of the same pool (i.e. the ABSENCE of a scripting filter on `memberCombatants`). **POM changes**: none — reused `RollDialog.helperToggle`/`toggleHelper` (unchanged from L520's additions). Stable 3/3 on `--repeat-each=3`.
- [x] `tests/e2e/conflict/artha-fate-in-conflict.spec.mjs` — spend Fate during conflict roll; verify reroll (spec written + green 3/3 on `--repeat-each=3`). **Rules source**: SG p.85 "Fate → Luck" — "A player may spend one fate point for luck after they've rolled. Pick up one new die for each 6 rolled. Roll these dice and count new successes. If more 6s come up, pick up new dice for each 6 and keep going!" (../reference/rules/fate-persona/scholars-guide-fate-persona.md L131-133). DH p.47 ("Fate") describes the same post-roll reroll-6s spend. **Surface distinction vs §3 `tests/e2e/roll/fate-reroll.spec.mjs`**: §3 covers the GENERIC fate-reroll surface — a standalone ability/skill roll from the character sheet. Same chat-card `data-action="fate-luck"` handler (post-roll.mjs L48 → `_handleFateLuck` L116-175), same reroll mechanics (spend 1 fate, append luck dice tagged `isLuck:true`, recalc successes, flip `luckUsed`). L522 (this spec) is the CONFLICT-SPECIFIC surface: the roll initiates via `#onRollAction` at conflict-panel.mjs L1847+ which seeds `testContext` with conflict metadata at L1974-1994 (`{isConflict, candidates:memberCombatants, conflictAction, combatId, combatantId, groupId, opponentGroupId, roundNum, volleyIndex}`). That metadata survives through `_buildRollFlags` at tb2e-roll.mjs L1461-1479 onto the chat message's `flags.tb2e.testContext`. The distinguishing assertion: after `_handleFateLuck` runs its PARTIAL update (post-roll.mjs L158-171 uses dot-path keys for `flags.tb2e.roll.{diceResults,successes,finalSuccesses,pass}` and `flags.tb2e.luckUsed` only — NEVER touches testContext), the conflict metadata remains intact byte-for-byte so downstream conflict consumers (maneuver spend buttons, versus finalize, action-context cards) still see the same action context they saw pre-spend. A future refactor that collapsed the partial update to a full-message replacement (e.g. `flags.tb2e: {...}`) would wipe testContext — §3 wouldn't catch it (testContext is null on standalone rolls), but this spec would via the `postSpend.testContext === preSpend.testContext` deep-equal assertion. **Surface**: Kill conflict (config.mjs L202-211 — attack → skill:fighter), party captainA (fighter=3, health=4, **fate=2** — the ROLLER; fate>=1 required to unlock the Luck button per `hasFate` gate at roll-utils.mjs L131, fate=2 makes the -1 decrement assertion unambiguous), GM captain Bugbear + mook Goblin (monsters, different group). All unarmed via `__unarmed__` (conflict-panel.mjs L1944-1948 bakes -1D "Fists"). Party V0=attack/captainA, GM V0=attack/monA → attack:attack INDEPENDENT (config.mjs L408): both sides roll vs Ob 0, no versus chain, lands in `_handleIndependentRoll` (tb2e-roll.mjs L1487-1540). Flat disposition (captainA=4, monA=4, monB=4). No helpers engaged — isolates the fate-reroll surface from synergy (L520) and from help (L521). **PRNG stub sequence (mid-test swap, same pattern as §3 L158-160)**: (1) Initial roll: `CONFIG.Dice.randomUniform = () => 0.001` → Foundry `Math.ceil((1-u)*6)` yields face 6 on every die → pool fighter(3)−1 unarmed = 2D → 2 suns = 2 successes vs Ob 0, pass. (2) Post-click reroll: swap to `() => 0.999` → face 1 on every reroll die → 2 suns trigger `evaluateRoll(2)` → both dice face-1 (no success, not a sun) → cascade loop at post-roll.mjs L141-146 exits after one pass with 0 new successes. (Can't stay at 0.001 — all-6s stub would recurse forever since `evaluateRoll` clamps pool to max(n,1) so cascade always has ≥1 die. 0.999 pins "reroll ran but added zero successes" as the cleanest boundary: if a regression silently skipped the reroll, diceLen would stay 2 AND luckUsed stay false — both flags flip together.) **Concrete assertions (14 total)**: (A) captainBefore `{fateCurrent:2, fateSpent:0}`; (B/C) post-submit, card located by `flags.tb2e.testContext.{isConflict,conflictAction,actorId}` filter; pre-spend testContext shape via `toMatchObject` — `isConflict:true, conflictAction:'attack', combatId, combatantId:cmb.captain, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1 (1-indexed after beginResolve), volleyIndex:0`; (D) pre-spend roll flags exact `{poolSize:2, successes:2, finalSuccesses:2, obstacle:0, pass:true}`; (E) `luckUsed:false` pre-spend; (F) DOM: `card.diceResults` count === 2; (G) Fate: Luck button VISIBLE on the CONFLICT roll card — proves `hasFate && hasSuns` both hold on the conflict surface (roll-result.hbs L110-117 template gate); (H) post-click, poll captainA `{fateCurrent:1, fateSpent:1}` (post-roll.mjs L149-152); (I/J/K/L) poll `luckUsed:true` (post-roll.mjs L161), then `diceLen:4` (2 original + 2 luck), roll flags unchanged at `{poolSize:2, successes:2, finalSuccesses:2, obstacle:0, pass:true}` (successes stayed 2 because all reroll dice were face-1), `originalSuns:2` (first 2 dice preserved), `luckSliceIsLuck:true` (all 2 appended dice carry `isLuck:true` per post-roll.mjs L155), `luckSliceSuccesses:0`, `luckSliceSuns:0`; (M) **the L522 distinguishing assertion** — `postSpend.testContext` deep-equal to `preSpend.testContext` via `JSON.parse(JSON.stringify(...))` snapshot before/after. If `_handleFateLuck` ever mutated testContext (or wrote a full-object replacement), this assertion would be the sole one catching it. (N) Post-render: button count 0 (template `{{#unless luckUsed}}` guard + post-roll.mjs L174 `_reRenderChatCard`), 4 dice rendered in DOM. **Card scoped to messageId** via `card.root = page.locator(\`li.chat-message[data-message-id="${messageId}"] ...\`).first()` override + local `fateLuckButton`/`diceResults` rebind — same idiom as team-synergy.spec.mjs L671-677 to dodge chat double-render when multiple messages are live (the default `.last()` scope would match any intervening roll card). **POM changes**: none — reused `RollChatCard.fateLuckButton`/`diceResults` locators (unchanged from §3's additions), reused `RollDialog.getSummaryPool`/`submit`/`modeInput`/`getObstacle`. Uses `combatant.actor` uniformity rule per CLAUDE.md §Unlinked Actors — linked character captain + unlinked monsters; no HP is read in this spec so the synthetic-actor surface isn't exercised. Stable 3/3 on `--repeat-each=3`. All Playwright sessions auth as GM (auth.setup.mjs); `_handleFateLuck` owner gate (post-roll.mjs L119 → `game.actors.get(actorId)` resolves + GM is owner) passes in-session. Green without any production gaps.
- [x] `tests/e2e/conflict/artha-persona-in-conflict.spec.mjs` — spend Persona +1D during conflict roll (spec written + green 3/3 on `--repeat-each=3`). **Rules source**: SG p.88 "Spending Persona → Advantage" — "A player may spend up to three persona points on a single roll before throwing the dice. Each persona point adds +1D to the roll." (../reference/rules/fate-persona/scholars-guide-fate-persona.md L163-165). Pre-roll one-shot spend — distinct from the two POST-roll artha buttons (Fate: Luck L522, Of Course §3 wise-aid-persona) and from Channel Your Nature (SG p.88 — 1 Persona adds Nature rating, gated separately by the `channelNature` flag at tb2e-roll.mjs L1452; this spec does NOT toggle it). **Surface distinction vs §3**: §3 has no equivalent spec for the pre-roll Persona Advantage stepper — this is a net-new surface. L523 (this spec) exercises it on the CONFLICT roll dialog path: the production code at tb2e-roll.mjs L395-397 gates `showPersona = isCharacter && !isResourcesOrCircles`, and attack rolls on a character captain in a Kill conflict satisfy both (attack skill is `fighter` per config.mjs L202-211). **Surface**: Kill conflict (config.mjs L202-211), party captainA (fighter=3, health=4, **fate=0, persona=2** — the ROLLER; fate=0 locks "persona path, not fate path"; persona=2 rather than 1 so a hypothetical double-decrement regression would surface as current=0 rather than clamping silently at 0 via the `Math.max` guard at tb2e-roll.mjs L1376) + GM Bugbear captain + Goblin mook (monsters, different group). All unarmed via `__unarmed__` (conflict-panel.mjs L1944-1948 bakes -1D "Fists"). Party V0=attack/captainA, GM V0=attack/monA → attack:attack INDEPENDENT (config.mjs L408): both sides roll vs Ob 0, no versus chain, lands in `_handleIndependentRoll` (tb2e-roll.mjs L1487-1540). Flat disposition (captainA=4, monA=4, monB=4). No helpers engaged — isolates the persona-advantage surface from synergy (L520) and help (L521). **PRNG stub**: `CONFIG.Dice.randomUniform = () => 0.001` → all-6s, every die success (no mid-test swap — persona is a PRE-ROLL one-shot, no chat-card button to re-trigger). Pool = fighter(3) − 1 unarmed + 1 persona = 3D → 3 successes vs Ob 0, pass. **Production call graph**: `#onRollAction` (conflict-panel.mjs L1847+) → `rollTest` seeds testContext at L1978-1993 → `_showRollDialog` sets `showPersona` at L395-397, `personaAvailable = actor.system.persona.current` at L396 → template renders persona section (roll-dialog.hbs L163-199) → `+` stepper wired at tb2e-roll.mjs L820-829 clamps `personaState.advantage` into [0, min(3, personaAvailable − channel cost)], re-renders modifier list (adds +1D source='persona' per advantage point at L606-613), refreshes summary → submit → `_applyPreRollActorChanges` L1369-1408 computes `personaCost = advantage + (channelNature ? 1 : 0)` (L1374), writes `system.persona.current = max(0, current − cost)` (L1376) and `system.persona.spent = spent + cost` (L1377) via partial `actor.update(updates)` (L1398) — dot-path-keyed, so other actor state is untouched → `_buildRollFlags` preserves each persona modifier verbatim on the card via `allModifiers.map(m => ({...m}))` at L1431 (no dedicated persona-spend message flag — the spend is on the actor, not the card). **Concrete assertions (14 total)**: (A) captainBefore `{personaCurrent:2, personaSpent:0, fateCurrent:0, fateSpent:0}`; (B) **CONFLICT SURFACE** — `dialog.personaSection` count===1 (would fail if a refactor broke `showPersona` on the conflict path or passed a non-character actor through); (C) baseline `dialog.getSummaryPool() === 2` (fighter 3 − 1 unarmed); click `dialog.incrementPersonaAdvantage(1)` — new POM helper; (D) stepper value asserted inside the helper at "1" (catches a clamp regression where `personaAvailable < 1` would pin below 1); (E) `dialog.getSummaryPool() === 3` (live modifier list re-rendered — tb2e-roll.mjs L826-827); submit, locate the card by `flags.tb2e.testContext.{isConflict,conflictAction,actorId}` filter; (F) testContext full-shape `toMatchObject` — `{isConflict:true, conflictAction:'attack', combatId, combatantId:cmb.captain, groupId:partyGroupId, opponentGroupId:gmGroupId, roundNum:1, volleyIndex:0}` — same regression surface as L522 (a future refactor dropping testContext on the persona-spend path would be caught here); (G) roll flags exact `{poolSize:3, successes:3, finalSuccesses:3, obstacle:0, pass:true}` — poolSize reflects the persona +1D; (H) `diceResults.length === 3` (persona contribution landed in the rolled pool, not just the summary); (I) exactly ONE persona modifier survived onto the card — `{source:'persona', type:'dice', value:1}` (on-card evidence of the spend via L1431); (J) card DOM `diceResults` count === 3 (scoped to messageId via root-override idiom from L522 L671-677 to dodge chat double-render); (K) `flags.tb2e.channelNature === false` AND `luckUsed === false` (orthogonal flag surfaces are off); (N) `card.fateLuckButton` count === 0 — fate=0 → hasFate false → template gate at roll-result.hbs L110-117 suppresses the button (locks down "persona, not fate" on the card surface); (L) poll `captain.actor.system.persona` → `{current:1, spent:1}` (partial update at L1376-1377 fired); (M) `fate.current === 0, fate.spent === 0` — persona spend did NOT accidentally decrement fate (orthogonal-fields check). **Scope distinctions**: vs L520 team-synergy — that is synergy path (fate+advancement coupled with help); this is solo pre-roll persona advantage. vs L521 team-helper-from-unscripted — that's unscripted-teammate help path; no persona. vs L522 artha-fate-in-conflict — that's POST-roll fate reroll-6s; this is PRE-roll persona +1D. vs §3 wise-aid-persona — that's POST-roll persona reroll-all-wyrms; this is pre-roll +1D advantage. vs §3 nature-tax-decrement — that's Channel Your Nature (also SG p.88, different sub-mechanic); this spec leaves `channelNature` OFF. **POM changes**: added `RollDialog.incrementPersonaAdvantage(n)` — expands collapsed `.roll-dialog-persona` section, clicks the `+` stepper `n` times, asserts `.stepper-value[data-field='personaAdvantage']` lands at exactly `n` (catches silent clamp). Clearly reusable for future persona specs (channel-nature-in-conflict, persona-advantage-cap-at-3, etc.). Uses `combatant.actor` uniformity rule per CLAUDE.md §Unlinked Actors — linked character captain + unlinked monsters; no HP is read in this spec so the synthetic-actor surface isn't exercised. Stable 3/3 on `--repeat-each=3` — green without any production gaps.

---

## 20. Conflict: Resolution & Compromise

### Agent briefing

**Files:**
- `conflict-panel.mjs` — `resolveConflict`, `endConflict`, `nextRound`
- `templates/conflict/panel-resolution.hbs`, `chat/conflict-compromise.hbs`, `conflict-round-summary.hbs`
- Rule: DH pp.125–127 (compromise determined by remaining HP)

**Checkboxes:**

- [ ] ~~skipped~~ `tests/e2e/conflict/resolution-victory.spec.mjs` — full Kill-conflict staging (setup→disposition→weapons→scripting→lock→beginResolve) with party captain+alt winner at HP 8/8 (100%) and Bugbear+Goblin loser at HP 0/6 via direct `actor.update({"system.conflict.hp.value":0})` (script-independent-ko-sub idiom, L431); clicks `panel-resolve.hbs` L163 `data-action="resolveConflict"` button (added `resolveConflictButton` / `clickResolveConflict` POM helpers) → `#onResolveConflict` (conflict-panel.mjs L2256-2303) computes `calculateCompromise` (combat.mjs L885-904, percent=1.0→level "minor"), renders `conflict-compromise.hbs`, posts chat card, calls `beginResolution()`, flips tab to "resolution"; green on: resolution-tab banner `.winner` + `.resolution-compromise.level-minor` + `.resolution-compromise-label` "No Compromise" (panel-resolution.hbs L18-19 `noCompromise` gate, fed by conflict-panel.mjs L1365 `comp.remaining === comp.starting`), chat message count+1, `.conflict-compromise-card` winner name non-blank, team HP readouts "8/8"+"0/6"; **`test.fixme` with annotation**: `templates/chat/conflict-compromise.hbs` L11-15 renders `<div class="card-compromise compromise-{{level}}">{{label}}</div>` unconditionally — no `{{#if noCompromise}}` branch, so for the 100%-HP "no compromise" case the card posts `compromise-minor` / "Minor Compromise" contradicting SG pp.74-76 RAW; verified empirically (rendered markup `<div class="card-compromise compromise-minor">Minor Compromise</div>`); fix: pass `noCompromise` from `#onResolveConflict` into the card render context + add `{{#if noCompromise}}` branch emitting `TB2E.Conflict.Compromise.None` with a `compromise-none` class; rules cite DH p.254 + SG pp.74-76 ("Compromise" grades), noCompromise predicate at conflict-panel.mjs L1365
- [x] `tests/e2e/conflict/resolution-minor-compromise.spec.mjs` — winner at high HP, loser at 0; verify minor compromise (Kill-conflict staging cloned from L538 — full wizard setup→disposition(flat 8/6)→weapons(`__unarmed__`)→scripting→lock→`beginResolve()` — with party captain+alt seeded to HP 4+4 via `distributeDisposition`, then `actor.update({"system.conflict.hp.value":3})` on the alt to drop party to 7/8 (the `script-independent-ko-sub` L431 idiom), and Bugbear+Goblin driven to 0/6 the same way; click L538-landed `resolveConflictButton` POM → `#onResolveConflict` (conflict-panel.mjs L2256-2303) computes `calculateCompromise` (combat.mjs L885-904, percent=0.875 → level "minor" via the `>0.5` branch at L899), posts `conflict-compromise.hbs`, calls `beginResolution()`, flips tab to "resolution"; green on: resolution-tab `.resolution-banner.winner`, `.resolution-compromise.level-minor`, `.resolution-compromise-label` text "Minor Compromise" (panel-resolution.hbs L20-23 else branch, reached because `noCompromise` is false — conflict-panel.mjs L1365 `comp.remaining (7) !== comp.starting (8)`), `.resolution-compromise-detail` "7/8 (88%)" (L1363 `Math.round(0.875 * 100)`), chat message count+1, `.conflict-compromise-card` with winner name non-blank, team HP readouts "7/8"+"0/6" (conflict-compromise.hbs L18-23), `.card-compromise.compromise-minor` class + "Minor Compromise" text (card template L11-15 — unlike L538's fixmed no-compromise edge, the `noCompromise` branch gap is not triggered here because `comp.remaining !== comp.starting`, so the card is RAW-correct); rules cite SG p.75 "If the winner ends the conflict with more than half of their starting disposition, they owe the loser a minor compromise" + DH p.254
- [x] `tests/e2e/conflict/resolution-major-compromise.spec.mjs` — winner at low HP, loser at 0; verify major compromise (Kill-conflict staging cloned from L539 — full wizard setup→disposition(flat 8/6)→weapons(`__unarmed__`)→scripting→lock→`beginResolve()` — with party captain+alt seeded to HP 4+4 via `distributeDisposition`, then direct `actor.update({"system.conflict.hp.value":N})` to shave captain→1 and alt→0 (party total 1/8, the `script-independent-ko-sub` L431 idiom) and drive Bugbear+Goblin to 0/6 the same way; party group total=1 is NOT `atZero` per combat.mjs L922 so party still wins while gm (total 0) is the sole loser; click L538-landed `resolveConflictButton` POM → `#onResolveConflict` (conflict-panel.mjs L2256-2303) computes `calculateCompromise` (combat.mjs L885-904, percent=0.125 → level "major" via the else branch at L901 — since `0.125 > 0.25` is false), posts `conflict-compromise.hbs`, calls `beginResolution()`, flips tab to "resolution"; green on: resolution-tab `.resolution-banner.winner`, `.resolution-compromise.level-major` (NOT level-minor / level-half), `.resolution-compromise-label` text "Major Compromise" (panel-resolution.hbs L20-23 else branch reached because `noCompromise` is false — conflict-panel.mjs L1365 `comp.remaining (1) !== comp.starting (8)` — label localized from `TB2E.Conflict.Compromise.Major` at lang/en.json L558), `.resolution-compromise-detail` "1/8 (13%)" (L1363 `Math.round(0.125 * 100)` = 13), chat message count+1, `.conflict-compromise-card` with winner name non-blank, team HP readouts "1/8"+"0/6" (conflict-compromise.hbs L18-23), `.card-compromise.compromise-major` class (NOT compromise-minor / compromise-half) + "Major Compromise" text (card template L11-15 generic `compromise-{{compromise.level}}` class — unlike L538's fixmed no-compromise edge, the `noCompromise` branch gap is not triggered here because `comp.remaining !== comp.starting`, so the card is RAW-correct); rules cite SG p.76 L519 "If the winner's disposition is reduced to just a few points at the end of the conflict, they owe the loser a major compromise" + DH pp.125-127
- [x] `tests/e2e/conflict/resolution-end-conflict-clears-state.spec.mjs` — end conflict; verify tracker removed, combatants' conflict.hp cleared, panel closed (staging cloned from L540 — full wizard setup→disposition(flat 8/6)→weapons(`__unarmed__`)→scripting→lock→`beginResolve()` — with party captain+alt HP seeded to 1+0 and Bugbear+Goblin seeded to 0+0 via the `actor.update({"system.conflict.hp.value":N})` L395-397 script-independent-ko-sub idiom; `clickResolveConflict()` (L538 POM helper) flips phase to "resolution" and lands on the resolution tab; new `endConflictButton` / `clickEndConflictAndConfirm` POM helpers on ConflictPanel.mjs target panel-resolution.hbs L72 `button.setup-next-btn[data-action="endConflict"]` (GM-gated by L70 `{{#if isGM}}`) and handle the `DialogV2.confirm` via `dialog.application.dialog button[data-action="yes"]` (session-reset.spec.mjs L108-110 idiom); dispatches to `ConflictPanel.#onEndConflict` (conflict-panel.mjs L2328-2386) which takes the `combat.system.phase === "resolution"` branch at L2340 (skip chat card — compromise card already posted on resolution entry by `#onResolveConflict` L2296-2299), calls `this.close()` (L2384) + `combat.endConflict()` (L2385 → combat.mjs L967-969 `this.delete()`), triggering `_preDelete` (combat.mjs L941-961) which iterates `this.combatants`, resolves `combatant.actor`, and writes `system.conflict.hp.{value,max}=0` + (schema-conditional at L956-958) `system.conflict.{weapon,weaponId}=""` on each participating actor; green on: `game.combats.get(combatId)` becomes null, `game.combat` becomes null, all four actors' `system.conflict.hp` equals `{value:0, max:0}` (schema default from character.mjs L162-165 / monster.mjs L47-50), character actors additionally have `conflict.weapon`/`weaponId` cleared to "" while monsters are not asserted (their schema lacks those fields so _preDelete skips them, combat.mjs L955-958), `ConflictPanel` instance no longer in `foundry.applications.instances` (Map+Object.values fallback from ManeuverSpendDialog.mjs L107-108) with `#conflict-panel` DOM element gone, tracker footer reverts from "End Conflict"/"Open Playbook" (tracker-footer.hbs L15-27 `hasCombat` branch) to "Create Conflict" (L4-13 `{{#unless hasCombat}}` branch) via the `hasCombat=false` flip at conflict-tracker.mjs L85 driven by the `deleteCombat` hook; rules cite DH pp.125-127 and SG pp.68-69 (conflict HP is a per-conflict disposition pool, must not leak into the adventure phase); stable across `--repeat-each=3`
- [x] `tests/e2e/conflict/resolution-round-summary.spec.mjs` — end of round posts conflict-round-summary card with actions + outcomes (Kill-conflict staging cloned from L540 — full wizard setup→disposition(flat 8/6)→weapons(`__unarmed__`)→scripting→lock→`beginResolve()` — with party captain+alt HP at 4+4 and Bugbear+Goblin at 3+3 via `distributeDisposition` so nobody hits 0 and the conflict does NOT end (`checkConflictEnd().ended === false`), isolating the end-of-round card from the end-of-conflict compromise cards covered by L538-L541; distinct-action scripting per volley (party attack/defend/feint, gm feint/maneuver/attack) so the rendered `.card-summary-side.action-{key}` classes are unique per row and ordering is assertable; fast-forwarded V0/V1 via the L484 idiom (direct `round.volleys[0..1].{revealed,result}` writes seeding `result.sides` with known `{action,actionLabel,combatantName}` payloads + `system.currentAction = 2` bump so the V2 Mark Resolved button is the only `.resolve-action.current`-gated button rendered, panel-resolve.hbs L31/L117-126); reveal V2 via existing `button[data-action="revealAction"]`, then click V2 `button[data-action="resolveAction"]` to dispatch `ConflictPanel.#onResolveAction` (conflict-panel.mjs L2003-2096) with `actionIndex === 2` — the round-summary post gate at L2046-2089 (the ONLY call site for `conflict-round-summary.hbs` across the module; `combat.advanceRound` L810-878 has no `ChatMessage.create` call, and `#onNextRound` L2239-2248 just delegates to `advanceRound`, so the card is posted when the ROUND ENDS not when a new round BEGINS); green on: `round.volleys[2].result = {resolved:true, interaction:"none" (feint:attack at config.mjs L416), sideCount:2}`, exactly one new chat message delta post-click, `.conflict-summary-card` visible inside the scoped `li.chat-message[data-message-id=X]` with `.first()` double-render guard, `.card-title` textContent "Round 1 — Summary" (template L5, `round={{currentRound}}` from conflict-panel.mjs L2010/L2081, CSS `text-transform:uppercase` stripped via `el.textContent`), three `.card-summary-action` rows in V1/V2/V3 order with per-row `.card-summary-num` = "1"/"2"/"3" (template L9, `actionNum: i+1` at L2063), per-row `.card-summary-side` `.action-{attack|defend|feint|maneuver}` classes matching the scripted pairings (template L11), per-side textContent "`<combatantName>: <Attack|Defend|Feint|Maneuver>`" (template L12, `combatantName` from `combat.combatants.get(entry.combatantId).name` at L2021 for V2 and the seeded sides for V0/V1, localized labels from lang/en.json L487-490), two `.card-summary-change` rows with textContent sorted `["GM Team: 6/6", "Party Team: 8/8"]` (template L21, default group names `PCTeam`/`NPCTeam` → "Party Team" / "GM Team" per lang/en.json L456-457 and combat.mjs L27-32, disposition builder at conflict-panel.mjs L2067-2077 sums `c.actor.system.conflict.hp.{value,max}` per CLAUDE.md §Unlinked Actors), and the resolve tab reverting to the `button[data-action="nextRound"]` nav button (panel-resolve.hbs L167-171 `allResolved` branch) with no `resolveConflictButton` because `checkConflictEnd()` is false; rules cite DH p.127 (round/volley structure) + SG pp.68-69 (disposition pool); stable across `--repeat-each=3`

---

## 21. Monster & NPC Sheets

### Agent briefing

**Files:**
- `module/applications/actor/monster-sheet.mjs` (if exists), `templates/actor/monster/*.hbs`
- `module/applications/actor/npc-sheet.mjs` (if exists), `templates/actor/npc/*.hbs`
- `module/data/actor/monster.mjs`, `npc.mjs`
- Packs: `monsters` (42), `npcs` (43)

**Checkboxes:**

- [x] `tests/e2e/sheet/monster-open.spec.mjs` — imports Kobold from `tb2e.monsters`, renders sheet, asserts Nature (`templates/actors/monster-body.hbs` line 9), 3 conflict dispositions (`module/data/actor/monster.mjs` lines 30-33), and weapons loadout (monsters have no trait items — `weapons` is the sheet's loadout concept)
- [x] `tests/e2e/sheet/monster-nature-roll.spec.mjs` — click Nature surface on monster-body (`templates/actors/monster-body.hbs` line 7 `data-action="rollNature"` → `MonsterSheet.#onRollNature` at `module/applications/actor/monster-sheet.mjs` line 177); imports Kobold (Nature 2) from `tb2e.monsters`, stubs all-6s, asserts dialog pool = 2, submits default Ob 1, asserts chat card shape (poolSize/baseDice/successes = 2, pass = true) + speaker + `flags.tb2e.roll` metadata + `directNatureTest`/`withinNature` flags (roll-result.hbs "within — no tax" notice), finalizes (no-op for monsters per `_logAdvancement` early-return at `module/dice/tb2e-roll.mjs` line 193), confirms `system.nature` unchanged
- [x] `tests/e2e/sheet/npc-open.spec.mjs` — imports Alchemist from `tb2e.npcs`, opens the sheet, asserts identity (stock/class/goal inputs — `templates/actors/npc-body.hbs` lines 4-17), raw abilities (`system.abilities.nature|will|health.rating` per `module/data/actor/npc.mjs` lines 13-32), might, skill/wise row counts + values, and trait-item row count. Historical ENOENT at `module/applications/actor/npc-sheet.mjs:43` (flagged TEST_PLAN line 66) is no longer present — line 43 now correctly references `templates/actors/npc-body.hbs`. `page.on('pageerror')` guards against regression.
- [x] `tests/e2e/sheet/npc-edit-basics.spec.mjs` — imports Alchemist from `tb2e.npcs`, opens the sheet, and edits the header name input (`templates/actors/npc-header.hbs` line 18), the description textarea (`templates/actors/npc-body.hbs` line 216 → `system.description` "GM notes" per `module/data/actor/npc.mjs` line 75), and the identity strip (stock/class/goal — `npc-body.hbs` lines 4-17 → `npc.mjs` lines 8-10). Persistence model: `NPCSheet.DEFAULT_OPTIONS.form.submitOnChange = true` (`module/applications/actor/npc-sheet.mjs` line 27) — each `.fill()` + blur auto-submits the AppV2 form. Round-trips verified against the world actor AND against the DOM after a close + re-render.

---

## 22. Items (Non-Magic)

### Agent briefing

**Files:**
- `module/data/item/weapon.mjs`, `armor.mjs`, `gear.mjs`, `supply.mjs`, `container.mjs`
- `module/applications/item/gear-sheet.mjs`
- Inventory slots: head, neck, hand-L, hand-R, torso, belt, feet, pocket + custom container slots

**Checkboxes:**

- [x] `tests/e2e/item/weapon-sheet.spec.mjs` — creates a character with an unassigned weapon (`module/data/item/weapon.mjs`), opens the inventory tab (`CharacterSheet.openInventoryTab`), fires the per-row `editItem` action (`templates/actors/tabs/character-inventory.hbs` line 319 → `module/applications/actor/character-sheet.mjs` `#onEditItem` line 1725), and edits weapon-specific fields via the shared `GearSheet` (`module/applications/item/gear-sheet.mjs`, `templates/items/gear-sheet.hbs`). TB2E weapons have no `damage`/`weight` fields (see `weapon.mjs` — only `wield`, `conflictBonuses`, `skillBonuses`, `specialRules` + inventory fields); the spec adapts the checkbox by editing the closest analogs: `system.cost` (slot cost / burden, `_fields.mjs` line 27 — "weight" analog), `system.wield` (1H↔2H, `weapon.mjs` line 15), `system.conflictBonuses.attack.value` ("damage" analog; `weapon.mjs` lines 16-21), and `system.specialRules`. Persistence via `submitOnChange: true` (`gear-sheet.mjs` line 17) — fill + blur round-trips through the data model, and values are re-verified in the DOM after a close + re-render of the item sheet. New POMs at `tests/e2e/pages/ItemSheet.mjs` — `ItemSheet` (shared item-sheet surface: name/cost/damaged/value/quantity/description) sized for reuse by `armor-sheet.spec.mjs` / `container-custom-slots.spec.mjs` / `supply-quality-portions.spec.mjs`, plus a `WeaponItemSheet` subclass for the weapon-only fieldset.
- [x] `tests/e2e/item/armor-sheet.spec.mjs` — creates a character with an unassigned armor item (`module/data/item/armor.mjs`), opens the inventory tab (`CharacterSheet.openInventoryTab`), fires the per-row `editItem` action (`templates/actors/tabs/character-inventory.hbs` line 319 → `character-sheet.mjs` `#onEditItem` line 1725), and edits the armor-only fieldset in the shared `GearSheet` (`templates/items/gear-sheet.hbs` lines 110-132). TB2E armor has no `protection`/`burden` fields — the spec adapts the checkbox by editing the closest analogs: `system.armorType` (leather|chain|plate|helmet|shield; `armor.mjs` lines 10-13, `CONFIG.TB2E.armorTypes` in `module/config.mjs` lines 100-106), `system.absorbs` ("protection" analog per DH p.112 — armor rating of 1s-3s absorbed from attack successes; `armor.mjs` line 14), `system.cost` ("burden" analog from the shared `inventoryFields`, `_fields.mjs` line 27 — Buyer's Guide slot cost DH pp.72-74; slot-assignment mechanics covered by `inventory-slots.spec.mjs`), and `system.specialRules` (`armor.mjs` line 15). Persistence via `submitOnChange: true` (`gear-sheet.mjs` line 17) — fill/selectOption + blur round-trips through the data model, and values are re-verified in the DOM after a close + re-render. Extends `tests/e2e/pages/ItemSheet.mjs` with an `ArmorItemSheet` subclass (armorType/absorbs/specialRules locators) — sibling to the existing `WeaponItemSheet`.
- [x] `tests/e2e/item/container-custom-slots.spec.mjs` — creates a character with an embedded `backpack` container placed in `system.slot: "torso"` (`module/data/item/container.mjs`), then verifies two tiers of the custom-slot contract. (1) Seed-time: the inventory tab renders a dynamic slot group keyed by `system.containerKey` (`templates/actors/tabs/character-inventory.hbs` line 165 → `[data-slot-group]`) with `.container-group` class and exactly `system.containerSlots` empty `.inventory-slot[data-slot-index]` cells — the group + cell-count is assembled by `module/applications/actor/character-sheet.mjs` lines 429-448 from the equipped container whose `system.slot ∈ #FIXED_SLOTS` (line 15), `!dropped`, `!lost`, `quantityMax === 1` (line 432), and non-liquid (line 437). The group header exposes the `dropItem`/`removeFromSlot` buttons keyed by `containerId` (inventory.hbs lines 172-175). (2) Edit-time: the spec opens the container's `GearSheet` (new `ContainerItemSheet` POM in `tests/e2e/pages/ItemSheet.mjs` — targets the `#if isContainer` fieldset at `templates/items/gear-sheet.hbs` lines 135-168: `system.containerType` / `system.containerSlots` / `system.containerKey`), edits `containerSlots` from 5 → 3, blurs (auto-submits via `submitOnChange: true`, `module/applications/item/gear-sheet.mjs` line 17), re-renders the character sheet, and asserts the slot-group cell count updates to match. Final data-model assertion locks in `containerKey` / `containerSlots` / `containerType` / `slot`. Out of scope (per DH pp.71-74 and the briefing): bundle mechanics (`quantityMax > 1` — covered by `inventory-bundle-split.spec.mjs`), liquid containers (don't produce slot groups — `character-sheet.mjs` line 437), and slot-assignment mechanics (covered by `inventory-slots.spec.mjs`).
- [x] `tests/e2e/item/supply-quality-portions.spec.mjs` — covers the **item-sheet edit surface** for supplies (complement to `tests/e2e/sheet/inventory-supplies.spec.mjs` at §2 line 122, which owns the sheet-side `consumePortion` decrement loop and hungry-clear side-effect). Creates a character with an unassigned supply (`module/data/item/supply.mjs`), opens the inventory tab, fires the per-row `editItem` action (`templates/actors/tabs/character-inventory.hbs` line 319 → `module/applications/actor/character-sheet.mjs` `#onEditItem` line 1725), and edits the supply-only fieldset in the shared `GearSheet` (`templates/items/gear-sheet.hbs` lines 197-244). TB2E supplies have no "quality" field — the checkbox wording is adapted to the actual schema: "quality" → `system.supplyType` (food|light|spellMaterial|sacramental|ammunition|other; `supply.mjs` lines 10-13, `CONFIG.TB2E.supplyTypes` in `module/config.mjs` lines 147-154), "portions" → `system.quantity` / `system.quantityMax` (shared `inventoryFields`, `module/data/item/_fields.mjs` lines 30-31; per RAW DH pp.71-72 a supply's portion count is its `quantity`). The spec also round-trips `system.nameSingular` (supply.mjs line 16) and sanity-checks the light-supply inputs (`system.turnsRemaining`, `system.lit` — supply.mjs lines 14-15) render with their seeded values. Persistence via `submitOnChange: true` (`gear-sheet.mjs` line 17) — fill/selectOption + blur round-trips through the data model, and values are re-verified in the DOM after a close + re-render. A second test then seeds a 3/3 food supply directly in the belt slot and fires `consumePortion` once as a smoke check that the edited portion counter is the same field the runtime handler drives (full decrement-to-zero and hungry-clear coverage remains in `inventory-supplies.spec.mjs`). Extends `tests/e2e/pages/ItemSheet.mjs` with a `SupplyItemSheet` subclass (supplyType / turnsRemaining / nameSingular / lit locators) — sibling to `WeaponItemSheet` / `ArmorItemSheet` / `ContainerItemSheet`.

---

## Execution order suggestion

Roughly lowest-dependency first so earlier specs establish POMs/fixtures later specs reuse:

1. §2 Character Sheet (foundation POMs for sheet interactions)
2. §3 Rolls (roll dialog / chat-card POMs)
3. §4 Advancement, §5 Versus (build on §3)
4. §21 Monster/NPC sheets (simple coverage, expands fixture set)
5. §22 Items, §8 Compendiums, §9 Loot Tables
6. §6 Spells, §7 Invocations
7. §10 Grind, §11 Nature Crisis
8. §12–§20 Conflict (largest area; ~40 specs; do last because it depends on characters, items, rolls, mailboxes)
9. §1 Character Creation Wizard (very long flow; deferred because the iconic-characters drag already gives us populated characters for earlier tests)

---

## Out of scope for this plan

- Visual regression / screenshot snapshots (could be added later under a separate project)
- Performance/load (single-browser, single-Foundry harness)
- Module compatibility (other Foundry modules installed)
- Cross-browser (Firefox/WebKit) — chromium only until the suite stabilizes
- Multi-client (two browsers simultaneously joined) — mailbox pattern still testable via `page.evaluate` to force player-side writes and a hook observer in GM context

---

## Tally

| Section | Unchecked specs |
|---|---|
| 1. Character Creation | 7 |
| 2. Character Sheet | 12 |
| 3. Rolls | 10 |
| 4. Advancement | 5 |
| 5. Versus | 4 |
| 6. Spells | 7 |
| 7. Invocations & Relics | 6 |
| 8. Compendiums | 6 |
| 9. Loot Tables | 4 |
| 10. Grind Tracker | 5 |
| 11. Nature Crisis | 2 |
| 12. Conflict: Setup | 5 |
| 13. Conflict: Disposition | 5 |
| 14. Conflict: Weapons | 3 |
| 15. Conflict: Scripting | 5 |
| 16. Conflict: Resolve | 7 |
| 17. Conflict: Maneuver MoS | 7 |
| 18. Conflict: HP & KO | 6 |
| 19. Conflict: Team/Artha | 4 |
| 20. Conflict: Resolution | 5 |
| 21. Monster/NPC Sheets | 4 |
| 22. Items | 4 |
| **Total unchecked** | **127** |
| Infrastructure (checked) | 3 |
